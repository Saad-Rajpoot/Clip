"""Technology / systems / mechanism motion-graphics primitives.

Section C (part) of the MG library expansion. Each module exports a SPEC dict + a
render(out_path, *, ...) -> dict on the shared `look` system (charcoal stage,
charcoal-floor fades, palettes, typography, easing). Pure-local, deterministic,
footage-first. Original designs — engineering/explainer GRAMMAR (layered tiers,
animated traversal, cutaways, staged results), no reference asset reproduced.
"""
