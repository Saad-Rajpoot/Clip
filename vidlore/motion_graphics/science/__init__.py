"""Science / Engineering Explainer motion-graphics family (V3.1).

Grounded in a frame-audit of real explainer references (Branch Education electron-
microscope + computer-hardware teardowns, Jared Owen 3D-printer); see
research/motion_graphics_expansion/science_explainer_family/. The existing-coverage
audit found Vidlore already owns the explainer grammar (part-labels, mechanism
steps, block diagrams, scale compares) EXCEPT the labeled cross-section — so this
family is exactly one grounded primitive, not a padded set.

Original Vidlore 2D compositions on the `look` charcoal stage — NEVER the
references' 3D models, assets, fonts, branding, or layouts.
"""
from . import labeled_cross_section  # noqa: F401
