# ClipStudio Deep Review — Confirmed Findings (2026-06-10)

Multi-agent review: 8 module deep-reads -> 8 finder lenses -> adversarial verification.
Raw findings: 114 -> deduped 63 -> **confirmed 60** (refuted 3).

## HIGH (6)

### build_video's plan_beats failure path crashes with ZeroDivisionError instead of degrading to 1 clip/scene
- **Where:** `vidlore/clipstudio/build.py:530`  ·  **Lens:** correctness+correctness+robustness+matching-quality+integration+ffmpeg-media+ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** If plan_beats raises, the first try/except (build.py:387-389) correctly falls back to nbeats=[1,...] and logs '1 clip/scene'. But the visual-budget loop then runs: iteration 0 sets _nb=[0]*len(segments) (line 494), plan_beats raises again, 'except Exception: break' (lines 500-501) exits with _nb all zeros, and line 519 UNCONDITIONALLY overwrites nbeats with min(_nb, _D) = all zeros — making the earlier fallback dead code. At line 530 'slice_dur = ndur.get(seg.index, seg.est_duration) / k' divides by k=0 and build_video crashes. The advertised no-plan_beats degradation path cannot work as written; any plan_beats incompatibility/exception kills the whole render at stage 6/9.
- **Verified because:** Confirmed against the real code in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/build.py. (1) The code says exactly what the claim says: line 494 `_nb = [0] * len(segments)`; lines 500-501 `except Exception: break` exits the budget loop BEFORE line 502's `_nb = [max(1, kk) for kk in _nb]` normalization; line 519 `nbeats = [min(_nb[_pos], _D[segments[_pos].index]) ...]` is at loop-exit indentation with no success flag, so it unconditionally overwrites the line-389 fallback `nbeats = [max(1, k) ...]` with all zeros (`_D` values are >= 1 via `max(1, len(cl))`, so min(0, _D) = 0). Line 530 `slice_dur = ndur.get(seg.index, seg.est_duration) / k` then divides a float (est_duration: float, models.py:139) by k=0 -> ZeroDivisionError. (2) Reachable: iteration 0 sets `_energies_eff = list(_eng)`, so the line-496 call has byte-identical arguments to the line-383 call — any deterministic plan_beats failure (e.g. an older installed vidlore.assemble whose plan_beats lacks the energies/roles kwargs; build.py uses the absolute import `from vidlore.assemble import plan_beats`, and the line-388 log 'plan_beats unavailable' shows this failure is explicitly anticipated) raises in both places. A transient failure on a later iteration also crashes the same way because line 494 re-zeroes `_nb` each iteration before the try. No guard exists between line 519 and the division, and callers orchestrate.py:92 and orchestrate.py:207 invoke build_video with no try/except, so the exception kills the entire run at the final assemble stage after all expensive earlier stages. The advertised 1-clip/scene degradation path is dead code. Only inaccuracy found: the stage label is '6/6'/'9/9' in the actual logs, not '6/9' — immaterial.
- **Fix:** Make the budget loop fail open to the already-computed fallback instead of zeros: track success with a flag and only overwrite nbeats when the loop actually produced beats. Concretely, before the `for _it in range(40):` loop set `_nb = list(nbeats)` (seeding it with the line-389 fallback), change line 501's handler from `except Exception: break` to `except Exception: _nb = list(nbeats); break` (or set `_ok = False` and guard line 519 with `if _ok:`), and as belt-and-braces change line 529-530 to `k = max(1, nbeats[pos])` so no future code path can reintroduce a zero divisor. This preserves the intended degradation (1 clip/scene with the '1 clip/scene' log) whenever plan_beats raises, while leaving the visual-budget capping behavior unchanged on the success path.

### Verifier repair never updates sel.beat_windows — the rejected clip still airs in the final video
- **Where:** `vidlore/clipstudio/verify.py:135`  ·  **Lens:** correctness+matching-quality+integration  ·  **Verifier confidence:** high
- **Problem:** When the AI verifier replaces a failed pick, it updates source_id/shot_index/in_point/out_point/signals/confidence and re-cuts clips/seg_NNN.mp4, but does NOT touch sel.beat_windows. match.py:439-445 puts the original (now rejected) pick FIRST in beat_windows, and build_video cuts its on-screen footage from beat_windows (build.py:533 'windows_avail = list(getattr(sel, "beat_windows", []) ...)'), only using sel.clip_path as a last-resort fallback (build.py:573). So the verifier-rejected shot is still the scene's primary aired clip — the entire verify+repair stage is silently defeated in the rendered output. Additionally the cut_selection return value is ignored (verify.py:146), so if the re-cut fails, even seg_NNN.mp4 on disk silently remains the rejected footage while metadata claims the new shot; sel.identity_score also keeps the old shot's score while sel.identity is replaced (line 142).
- **Verified because:** Confirmed against the real code. verify.py:134-146 promotes the alternate by setting sel.source_id/shot_index/in_point/out_point/signals/confidence/source_url/identity and calling _cut.cut_selection(proj, sel, cfg), but never touches sel.beat_windows — a project-wide grep shows beat_windows is written only at match.py:445 (wins = [chosen cand] + alternates, chosen FIRST) and read only in build.py. Pipeline order (orchestrate.py:185-209) is match → cut → verify+repair → build_video, and build_video (build.py:533) cuts each scene's on-screen footage from `windows_avail = list(getattr(sel, "beat_windows", []) or [])`; sel.source_id is used only as an Option-B fill when windows_avail is empty (build.py:551) and sel.clip_path (the file the verifier re-cuts) only as a last-resort fallback (build.py:572-575). build.py never consults sel.verifier. The sort at build.py:540 is stable with fresh windows tying at (0,-1), so windows_avail.pop(0) yields beat_windows[0] — the rejected pick — for the scene's first beat; for k=1 scenes the verified replacement never airs at all (it sits later in beat_windows since alternates are included). So the rejected clip still airs as the scene's primary footage while the QC report records a successful replacement. Secondary points also verified: cut.py:20-48 returns Optional[Path] (None on ffmpeg failure or empty output) and verify.py:146 discards it with no flag/log, leaving the old seg_NNN.mp4 (rejected footage) on disk under metadata claiming the new shot; and sel.identity_score (models.py:188, set at match.py:435) retains the rejected shot's score while sel.identity is overwritten at verify.py:142. No guard, try/except, or downstream filter mitigates any of this.
- **Fix:** In verify.py's promotion block, capture the rejected window before overwriting sel (old_sid, old_in = sel.source_id, sel.in_point), then after setting the new fields rewrite sel.beat_windows: drop any window matching (old_sid, ~old_in) (windows are [source_id, in, out] triples rounded to 3 decimals, so match source_id and abs(w[1]-old_in) < 0.01), dedupe out any existing copy of the promoted window, and insert [alt.source_id, round(alt.in_point,3), round(alt.out_point,3)] at position 0 so the verified shot becomes the scene's primary beat clip. Also capture the re-cut result: p = _cut.cut_selection(proj, sel, cfg); if p is None, do not count the swap as successful — log it, append a flag reason (e.g. "verifier_recut_failed"), and either revert the promotion or continue to the next alternate; on success set sel.clip_path = str(p). Finally, update sel.identity_score from ashot.identities[0].get("score", 0.0) when ashot.identities is non-empty (mirroring match.py:432-435), resetting it to 0.0 otherwise.

### ingest_sources destructively REPLACES proj.sources — incremental ingest drops all previously ingested sources
- **Where:** `vidlore/clipstudio/ingest.py:265`  ·  **Lens:** correctness+robustness+concurrency-resources+integration  ·  **Verifier confidence:** high
- **Problem:** ingest_sources sets proj.sources to only the current batch: 'proj.sources = [by_id[i] for i in order if i in by_id]'. Running produce() again on an existing project with a new --add/--sources spec wipes every previously ingested SourceVideo record (permissions, checksums, provenance) from project.json — their index files remain on disk but match._load_pool iterates proj.sources, so all earlier footage vanishes from the matching pool and the ledger. download.py:180-189 contains the explicit fix for this exact bug ('the old proj.sources = [...] silently discarded every previously-indexed source') with an append-preserving merge; ingest.py was never given the same fix.
- **Verified because:** Confirmed against the real code. ingest.py:262-266 reads exactly as claimed: `by_id = {sv.id: sv for sv in results}; order = [_source_id(sp) for sp in specs]; proj.sources = [by_id[i] for i in order if i in by_id]; proj.save()` — proj.sources is rebuilt solely from the current batch; pre-existing entries are never consulted, despite the comment above claiming "merge into project (replace by id)". The path is reachable with no guards: the sole caller is orchestrate.py:46 (produce), which at lines 35-36 loads an existing project.json via ClipProject.load (restoring prior sources) and unconditionally calls ingest_sources whenever source_specs is non-empty; cli.py builds specs only from the current invocation's --sources/--add flags. So re-running produce on an existing project with a new --add wipes every previously ingested SourceVideo record (permission, permission_note, checksum, provenance extra) from project.json silently. Downstream, match._load_pool (match.py:98), index_all (index.py:303), build.py:95, and ledger.py:177 all iterate proj.sources only, so prior footage disappears from the matching pool and the compliance ledger even though its index files remain on disk. download.py:180-189 contains the exact append-preserving fix with a comment explicitly naming this bug ("the old `proj.sources = [...]` silently discarded every previously-indexed source"); ingest.py never received it.
- **Fix:** Mirror download.py's merge in ingest_sources (ingest.py:262-266): compute new_in_order = [by_id[i] for i in order if i in by_id], then new_ids = {sv.id for sv in new_in_order}, kept = [s for s in proj.sources if s.id not in new_ids], and set proj.sources = kept + new_in_order before proj.save(). This preserves all previously ingested sources (with their permission assertions, checksums, and provenance), refreshes any source re-specified in the current batch by id, and is identical to the old behavior for a fresh project where proj.sources is empty. The return value becomes the full merged list, matching download_candidates' contract; the only caller (orchestrate.produce) ignores the return value, so this is safe. Add a regression test that ingests batch A, then batch B into the same project, and asserts both A and B ids are present in proj.sources after the second call.

### Entity/anchor force-included candidates are appended past the download limit — the 'must download' guarantee is dead under default config
- **Where:** `vidlore/clipstudio/discover.py:426`  ·  **Lens:** correctness  ·  **Verifier confidence:** high
- **Problem:** discover_sources fills `final` to discover_target (default 18, discover.py:370-371) and then APPENDS entity-coverage extras (line 395) and anchor-coverage candidates (line 426) at the END of the list — at indices >= 18. But download truncates 'candidates[:limit]' (download.py:142) with limit = max_sources + discover_coverage_extra from orchestrate.py:163 (CLI default 6+6=12, portal 8+6=14). Because orchestrate.py:154 uses max(cfg.discover_target, max_sources), discover_target stays 18 for any max_sources < 18, so every force-included coverage/anchor candidate is silently truncated and never downloaded — directly contradicting the comments at discover.py:400-402 ('the scene(s) the whole video is ABOUT must be downloaded') and orchestrate.py:161-162 ('Download the full discovered set INCLUDING the entity-coverage overflow'). For a single-scene deep-dive, the raw footage of the one anchor scene can be entirely absent from the project. orchestrate.py:154 also mutates the caller's cfg in place (ratcheting discover_target across runs for a shared ClipConfig).
- **Verified because:** Every element of the claim verifies against the real code. (1) /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/discover.py lines 363-371 fill `final` from the ranked `kept` list and `break` at `len(final) >= cfg.discover_target` (default 18, config.py:103). Entity-coverage extras are then appended at the END (`final.append(extra)`, line 395) and anchor-coverage candidates likewise (`final.append(c)`, line 426), i.e. at indices >= 18 whenever discovery is healthy enough to fill the base slice. `discover_sources` returns `final` (line 433) with no reordering. (2) The sole caller is `produce_auto` in orchestrate.py: line 154 `cfg.discover_target = max(cfg.discover_target, max_sources)` keeps the target at 18 for any max_sources < 18; line 163 `dl_limit = max_sources + cfg.discover_coverage_extra`; line 165 passes candidates straight to `download_candidates`, which truncates at download.py:142 `queue = candidates if not limit else candidates[:limit]`. Real entry-point defaults: cli.py:55 `--max-sources` default 6 (dl_limit=12) and web.py:199 portal default 8 (dl_limit=14). Arithmetic check: extras survive only when max_sources + 6 > max(18, max_sources), i.e. max_sources >= 13 — so under both shipped defaults (and any max_sources <= 12) exactly ZERO force-included entity/anchor candidates are ever downloaded. (3) No guard exists anywhere else: nothing reorders or pins the forced candidates before the slice, and download_candidates has no notion of priority. This directly contradicts the stated intent at discover.py:400-402 ("the scene(s) the whole video is ABOUT must be downloaded") and orchestrate.py:161-162 ("Download the full discovered set INCLUDING the entity-coverage overflow") — the `+ discover_coverage_extra` download budget was reserved precisely for these extras but the budget window [0:12] can never reach them at indices [18:]. The cfg-mutation side-claim is also accurate: line 154 mutates a caller-supplied ClipConfig in place. One honest mitigation: the anchor force-include only fires for candidates NOT already in `final` (discover.py:421-423), and the relevance scorer gives anchor-token candidates up to +0.45 (discover.py:241-244) plus 2x search depth (line 317), so anchor footage often ranks inside the top 12 naturally. But the fallback was written exactly for the runs where ranking + per-channel cap drop those clips — and in those runs it always silently fails. The entity-coverage guarantee (under-represented characters) is 100% dead in all default configurations.
- **Fix:** Make the discover target equal the download budget head so the appended overflow lands inside the truncation window: in orchestrate.py produce_auto, replace lines 154/163 with a non-mutating copy, e.g. `cfg = dataclasses.replace(cfg, discover_target=max_sources)` (fixes both the dead-guarantee math and the in-place ratchet of a shared ClipConfig); then the base fill is max_sources items, entity extras land at indices [max_sources, max_sources+coverage_extra) which is exactly inside `dl_limit = max_sources + cfg.discover_coverage_extra`. Because anchor-coverage force-includes (up to 4 per anchor scene, discover.py:404-429) can exceed the coverage_extra budget, additionally make the limit respect pins: tag forced candidates (e.g. `c.forced = True` when appended at discover.py:395 and 426) and in download_candidates compute the queue as pinned-first — `pinned = [c for c in candidates if getattr(c, "forced", False)]; rest = [c for c in candidates if c not in pinned]; queue = (rest[:max(0, limit - len(pinned))] + pinned) if limit else candidates` — preserving discovery order within each group. Add a regression test that builds 25 candidates with 3 forced at the tail, calls download_candidates with limit=12 under a stubbed downloader, and asserts all forced candidates are in the downloaded queue.

### Unauthenticated web portal performs privileged downloads using the owner's logged-in browser cookies and API quota; bindable to all interfaces
- **Where:** `vidlore/clipstudio/web.py:218`  ·  **Lens:** security+integration  ·  **Verifier confidence:** high
- **Problem:** The Flask portal has no authentication of any kind on any route. POST /create takes attacker-supplied topic/script/movie/voiceover and hard-codes policy="approved_testing" (web.py:220), which download.policy_permission turns into permission=fair_use_claim for every auto-discovered source so they all download. The download path uses hd_download with `--cookies-from-browser chrome` (hd_download.py:137 and 213), i.e. it impersonates the machine owner's logged-in YouTube session, and it spends the owner's Claude/Gemini API quota during analyze/verify. The bind host is fully attacker-influencable via env (web.py:254 host = os.environ.get("VIDLORE_CLIPSTUDIO_HOST", "127.0.0.1")), so setting VIDLORE_CLIPSTUDIO_HOST=0.0.0.0 exposes all of this to anyone on the network with zero auth: they can drive arbitrary yt-dlp downloads through the owner's authenticated browser session, burn API spend, and push up to 200 MB uploads (web.py:33).
- **Verified because:** Every claimed fact is confirmed in the real code. (1) No authentication: grep over web.py shows only 5 route decorators (/, /create [POST], /job/<jid>, /status/<jid>, /download/<jid>) and no @app.before_request, no token/key/login/abort(401|403) check anywhere. (2) Hard-coded policy: web.py:218-221 spawns _run_job with kwargs ...policy="approved_testing"...; the caller supplies topic/script/movie/voiceover but cannot override the policy. (3) policy_permission (download.py:31-32) returns ("fair_use_claim", True, "user approved-for-testing policy") UNCONDITIONALLY for every candidate under approved_testing, so download_candidates (download.py:147-154) marks all discovered sources allowed and downloads them; orchestrate.py:165 passes the policy straight through. (4) Privileged cookie impersonation: hd_download.py:213 and the probe at :137 invoke yt_dlp with "--cookies-from-browser", COOKIES_BROWSER, where COOKIES_BROWSER defaults to "chrome" (hd_download.py:54) — i.e. it uses the machine owner's logged-in YouTube session. (5) API spend: produce_auto runs analyze_script (orchestrate.py:145) and optional AI verify, consuming the owner's LLM provider quota per request. (6) Network bind: web.py:254 host = os.environ.get("VIDLORE_CLIPSTUDIO_HOST", "127.0.0.1").strip() is passed verbatim to app.run(host=host,...) at web.py:256 with no allowlist, so VIDLORE_CLIPSTUDIO_HOST=0.0.0.0 exposes all routes to the network. (7) 200 MB upload cap at web.py:33. The failure mode is fully reachable: the only exception handling in this path (try/except in _run_job and _download_one) affects job success, not access control. The single nuance vs. the claim: the DEFAULT bind is 127.0.0.1 (localhost-only), so full network exposure is conditional on an operator/attacker-influenceable env var rather than on by default; the no-auth defect itself is still exploitable by any local user/process on the host regardless of that env var.
- **Fix:** Add mandatory authentication to the portal and remove the foot-gun bind. Concretely: (a) Generate or require a shared secret at startup (e.g. token = os.environ["VIDLORE_CLIPSTUDIO_TOKEN"] or a random token printed once on boot) and enforce it with an @app.before_request hook that compares a constant-time hmac of a header/cookie/query token and returns abort(401) on mismatch for all routes except a health check. (b) Refuse to bind to a non-loopback host unless auth is configured: if host not in ("127.0.0.1","localhost","::1") and no token is set, raise/exit; default should stay 127.0.0.1. (c) Stop hard-coding policy="approved_testing" in web.py:220 — drive it from explicit, authenticated, per-request operator input (default to "block"), so an unauthenticated/remote caller cannot auto-approve arbitrary downloads. (d) Optionally gate the cookies-from-browser path (hd_download.py) behind an explicit opt-in env flag so the owner's authenticated YouTube session is never used to service portal requests by default. Together these close the no-auth privileged-download / API-spend / SSRF-style cookie-impersonation chain.

### Leftover DASH video-only fragment is picked as the downloaded file — silent no-audio source kills ASR/dialogue-lock
- **Where:** `vidlore/clipstudio/hd_download.py:247`  ·  **Lens:** ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** After the retry loop, the 'produced file' is found by globbing '<stem>.*' and taking the FIRST path in sorted order with a video extension. yt-dlp writes separate DASH streams as '<stem>.f616.mp4' (video-only) + '<stem>.f140.m4a' before merging into '<stem>.mp4'. Lexicographically '<stem>.f616.mp4' < '<stem>.mp4' ('f' 0x66 < 'm' 0x6d after the common '<stem>.' prefix), so if an interrupted/timed-out run leaves a fragment behind, the video-only fragment is selected as the final media — even when a merged file also exists. download.py:115-116 and ingest.py:200-201 use the identical pattern. A video-only source then yields zero ASR words in index.transcribe_words, so transcript scoring and dialogue-lock (the strongest scene signal, w_dialogue=0.55) silently score 0.0 for that entire source, with no error anywhere — exactly the failure mode hd_download's own _merge_ffmpeg_dir docstring (lines 154-160) says must never happen.
- **Verified because:** Every element of the claim checks out against the real code. (1) The code says exactly what is claimed: hd_download.py:247-250 reads `for f in sorted(stem.parent.glob(stem.name + ".*")): if f.suffix.lower() in (".mp4", ".mkv", ".webm", ".mov"): produced = f; break`, and the identical pattern exists at download.py:115-116 and ingest.py:200-201 (`next((p for p in sorted(...glob(f"{sid}.*")) if p.suffix.lower() in _VIDEO_EXTS), None)`, with `.mp4` in `_VIDEO_EXTS` at ingest.py:25). Empirically verified: `sorted(['x.f140.m4a','x.mp4','x.f616.mp4','x.info.json'])` yields the f616 fragment before the merged `x.mp4`, and `Path('x.f616.mp4').suffix == '.mp4'`, so the video-only DASH fragment is selected first. (2) Reachable, two ways, with no guard anywhere. First, inside download_hd the retry loop only `break`s on returncode 0 but the glob runs unconditionally after the loop — so if every attempt fails/times out (subprocess.run timeout=600 kills yt-dlp; format selector `bv*+ba` downloads video first, so `<stem>.f616.mp4` is complete and renamed out of `.part` while audio is mid-download), the function returns the video-only fragment as a SUCCESS dict; the callers (download.py:64, ingest.py:153) check only that the path exists and mark the source SOURCE_OK. Second, a leftover fragment from a previously interrupted HD run coexisting with a later successfully merged `<stem>.mp4` is picked over the merged file by the legacy-path globs (yt-dlp deletes fragments only after its own successful merge, not stale ones from a differently-formatted earlier attempt). I searched for guards: probe() (ingest.py:70-118) extracts only video-stream info, never checks for an audio stream; verify.py is a vision-only keyframe verifier (no audio mention); transcribe_words (index.py:102-113) returns [] on any failure/silence with a bare `except: return []`; _assign_transcript no-ops on empty words, so every shot transcript is "", _dialogue_match scores 0.0, and config.py:49 confirms `w_dialogue: 0.55` — the largest single weight in match.py:243-245 — silently zeroed for the whole source. This is precisely the failure mode the module's own _merge_ffmpeg_dir docstring (hd_download.py:154-160) declares must never happen. (3) The severity is honest as a silent-data-corruption pipeline bug, though "critical" would overstate it: it requires a trigger (timeout/interrupt/merge failure leaving a fragment), so high, not critical.
- **Fix:** Three-part fix. (a) In hd_download.py, after the retry loop, only treat the download as successful if the last attempt returned 0 — return None (fallback) otherwise instead of globbing whatever is on disk. (b) Make the file-selection robust at all three sites (hd_download.py:247, download.py:115, ingest.py:200): exclude yt-dlp intermediate fragments with a regex like `re.search(r"\.f\d+$", f.with_suffix("").name)` (skip those), and prefer the exact merged name `<stem>.mp4` first, falling back to newest-mtime rather than lexicographic order. (c) Add a cheap audio-stream guard before declaring SOURCE_OK: probe() already opens the container with PyAV — extend it to return `has_audio` (`any(s.type == "audio" for s in c.streams)`) and have download_hd / _download_one / _ingest_url mark a no-audio result as failed (or delete stale `<stem>.f*` fragments before starting each download attempt, which also prevents the cross-run contamination case).

## MEDIUM (39)

### SystemExit from produce_auto is never caught in the web worker thread — job spins forever
- **Where:** `vidlore/clipstudio/web.py:67`  ·  **Lens:** correctness+robustness+concurrency-resources+integration  ·  **Verifier confidence:** high
- **Problem:** orchestrate.produce_auto signals failures by raising SystemExit (orchestrate.py:144 'no narration script', :159 'source discovery found nothing', :168 'no usable sources under policy'). web._run_job wraps the call in 'except Exception', but SystemExit derives from BaseException, so it is not caught; and because the code runs in a non-main daemon Thread, Python's threading.excepthook silently ignores SystemExit. _JOBS[jid]['done'] is never set, no error is recorded, and the portal job page polls '/status' forever showing 'running…' with a stuck progress bar. Any job that finds no sources or whose downloads are all blocked hangs indefinitely with zero diagnostics.
- **Verified because:** Confirmed against the actual code. orchestrate.py raises SystemExit inside produce_auto at line 144 ('no narration script provided.'), line 159 ('source discovery found nothing — check connectivity / movie name.'), and line 168 ('no usable sources under policy=...'). web.py _run_job (lines 54-74) wraps the produce_auto call in 'except Exception as e:' only — there is no finally block, no BaseException handler, and _run_job is the direct target of a daemon threading.Thread (web.py:218). SystemExit derives from BaseException, so it escapes the handler, and CPython's default threading.excepthook silently ignores SystemExit in non-main threads (documented behavior), so nothing is even printed to stderr. _JOBS[jid]['done'] is never set, /status keeps returning done:false, and the job page's poll() loop (web.py:147-169) shows 'running…' forever. Reachability verified: the line-144 path is guarded (create() at web.py:193 redirects on empty script), but lines 159 and 168 are fully reachable from the portal — discover.py's _ytsearch and _archive_search (lines 133-173) swallow all exceptions with 'except Exception: pass' and return [], so any network/yt-dlp failure yields zero candidates; and download.py marks sources failed/blocked, so all-failed downloads trigger line 168. The claim is accurate in every checkable detail.
- **Fix:** Best fix: stop raising SystemExit from library code. In orchestrate.py, define a PipelineError(RuntimeError) and replace the five 'raise SystemExit(...)' calls (lines 52, 65, 144, 159, 168) with 'raise PipelineError(...)'; cli.py can convert it to SystemExit at the CLI boundary (e.g. 'except PipelineError as e: raise SystemExit(str(e))') to preserve CLI exit behavior. web.py's existing 'except Exception' then catches it and records the error normally. Defense in depth in web.py: change '_run_job''s handler to 'except BaseException as e:' (or add 'except SystemExit as e' before it) and/or add a 'finally:' that sets j['done']=True with a generic error if no result was recorded, so any future BaseException cannot leave a job stuck in 'running…' forever.

### Manual produce() 'usable sources' gate counts download_failed sources — pipeline renders an all-black video instead of aborting
- **Where:** `vidlore/clipstudio/orchestrate.py:50`  ·  **Lens:** correctness+robustness+integration  ·  **Verifier confidence:** high
- **Problem:** produce() gates on 'not s.is_blocked', but SourceVideo.is_blocked (models.py:72-74) only checks status==SOURCE_BLOCKED or permission in PERMISSION_BLOCKS. A source with a valid permission whose download failed (status='download_failed') counts as usable. index_all then skips every non-'ok' source (index.py:303-304), match._load_pool is empty (match.py:98-99 filters status != SOURCE_OK), match emits empty selections with confidence 0.0 (match.py:276-279), cut produces nothing, and build_video substitutes black placeholder clips (build.py:573-575) — so a project where every permitted download failed silently produces a black final.mp4 instead of failing the 'no usable sources' gate. produce_auto does it correctly (orchestrate.py:166 filters status == "ok"); the two pipelines disagree.
- **Verified because:** Verified against the real code. orchestrate.py:50-53 gates produce() on `usable = [s for s in proj.sources if not s.is_blocked]`, and models.py:72-74 defines is_blocked as `self.status == SOURCE_BLOCKED or self.permission in PERMISSION_BLOCKS` where SOURCE_BLOCKED = "blocked_no_permission" and PERMISSION_BLOCKS = {"unverified"} (models.py:25-29). A source with a valid permission whose download failed gets status = SOURCE_FAILED ("download_failed") at ingest.py:127 (missing local file) or ingest.py:203 (yt-dlp failure) — the permission gate at ingest.py:220 runs before download and only blocks unverified — so such a source has is_blocked == False and counts as usable, and the SystemExit never fires. Every downstream stage then silently no-ops: index_all skips status != SOURCE_OK (index.py:303-305); match._load_pool filters status != SOURCE_OK (match.py:98-100); the empty pool produces empty ClipSelections with source_id="" and confidence 0.0 and returns without raising (match.py:276-279); cut_all filters `if s.source_id` so todo is empty (cut.py:54); grep confirms no raise/SystemExit anywhere in cut.py, ledger.py, or build.py; and build.py:572-575 substitutes _placeholder_clip, which is literally an ffmpeg `color=c=black:s=1280x720:d=3` black clip (build.py:22-31). The CLI caller (cli.py:72) adds no extra guard. produce_auto at orchestrate.py:166 uses the correct filter `s.status == "ok"`, confirming the two pipelines disagree. The gate's own error message ("every source is blocked or failed") even claims to cover the failed case the predicate misses. Severity is medium rather than high: it requires ALL permitted downloads to fail, ingest logs `[download_failed]` per source via the progress callback, and the ledger flags all segments for review with confidence 0.0 — but the run still wastes a full TTS+render and delivers a misleading all-black final.mp4 instead of the intended abort.
- **Fix:** In produce() at orchestrate.py:50, mirror produce_auto's filter by also requiring downloaded status: import SOURCE_OK from .models and change the gate to `usable = [s for s in proj.sources if not s.is_blocked and s.status == SOURCE_OK]` (the existing SystemExit message "every source is blocked or failed" already describes this behavior correctly, so it needs no change). Optionally also extend the warning block above it to log failed sources separately, e.g. `failed = [s.id for s in proj.sources if s.status == SOURCE_FAILED]` with its own log line, so users see why sources were excluded. Do NOT change SourceVideo.is_blocked itself — it is a permission-semantics property used for the "blocked (permission unverified)" log at orchestrate.py:47-49, and folding download failures into it would mislabel them.

### Claude model id is forwarded verbatim to the Gemini fallback — the verifier's LLM fallback is dead
- **Where:** `vidlore/clipstudio/verify.py:99`  ·  **Lens:** correctness+robustness+matching-quality+config-platform+integration  ·  **Verifier confidence:** high
- **Problem:** verify_and_repair pins model = eng_cfg.anthropic_model ('claude-sonnet-4-6') and passes it into llm.complete for every vision verdict. complete()'s _try_gemini forwards that same model string into _gemini_complete, which calls Vertex generate_content(model=(model or _GEMINI_MODEL)) — an Anthropic model id Vertex rejects with a non-transient error (_is_transient at llm.py:147-152 matches none of '429/rate/overload/timeout/...' for a model-not-found error), so the Gemini call aborts on attempt 1 and complete() returns ''. Result: whenever the Claude path fails (no key, auth error, outage), the 'bidirectional fallback' promised in llm.py:1-8 never serves the verifier — verify_frame returns None, every selection gets verifier={'status':'error'}, and the QC pass silently does nothing. Any caller passing an explicit provider-specific model has the same problem; verify.py is the only such caller today.
- **Verified because:** Confirmed against the real code. verify.py:99 pins `model = eng_cfg.anthropic_model` (defaulted to a non-empty "claude-sonnet-4-6" at vidlore/config.py:31 and :159, so it can never be falsy) and passes it through verify_frame into _llm.complete (verify.py:58-60). In llm.py, _try_gemini (183-186) forwards that same string to _gemini_complete, whose `generate_content(model=(model or _GEMINI_MODEL))` (115-117) therefore sends "claude-sonnet-4-6" to Vertex — the `or _GEMINI_MODEL` default is dead because model is truthy. Vertex genai rejects it with a 404 ClientError whose text matches none of _is_transient's keywords (llm.py:147-152: 429/rate/overload/529/timeout/connection/5xx/unavailable/resourceexhausted), so _with_retry returns None on attempt 1 and complete() returns "". verify_frame then finds no JSON in "" and returns None without raising (its own retry loop only catches exceptions), so verify_and_repair sets verifier={"status":"error"} for every selection while still returning available:True and logging "0 replaced, 0 unresolved". Reachability is unguarded: orchestrate.py:196 calls verify_and_repair in the pipeline; has_llm (llm.py:196-201) returns True when only Gemini is usable. Two concrete triggers: (a) missing/revoked Anthropic key or Claude outage with working Vertex creds — the documented fallback (llm.py:1-8) never serves the verifier; (b) VIDLORE_CLIPSTUDIO_LLM_PROVIDER=gemini with no Anthropic key — Gemini runs first, fails instantly on the Claude model id, and the "mandatory" Phase 6 verifier is fully dead despite a healthy Gemini backend. I also verified the claim that verify.py is the only complete() caller passing model= (segment.py:165 and analyze.py:129,174 omit it). Minor overstatement only: per-clip status:"error" is recorded for the QC report, so the failure is masked in the summary/return value rather than completely invisible; and the impact is degraded QC (no clip verification/repair), not a crash — the pipeline still produces a video.
- **Fix:** Remove the model pin in verify.py: delete `model = eng_cfg.anthropic_model` (line 99) and stop passing model into verify_frame/_llm.complete (or pass model=""). This is behavior-preserving on the Claude path because _claude_complete (llm.py:128) already resolves `model or eng_cfg.anthropic_model or _CLAUDE_MODEL` when model is empty, and it revives the Gemini fallback since _gemini_complete's `model or _GEMINI_MODEL` then picks the proper Gemini default. For defense in depth against future callers, also harden llm.py so provider-specific ids never cross providers — e.g. in _try_gemini use `model if model and model.lower().startswith("gemini") else ""` (and symmetrically ignore gemini-* ids in _claude_complete), or change complete() to accept separate claude_model=/gemini_model= parameters.

### Anchor-continuity bonus is baked into reported confidence — zero-signal shots report confidence 0.45 and dodge low-confidence flagging
- **Where:** `vidlore/clipstudio/match.py:252`  ·  **Lens:** correctness+matching-quality+matching-quality  ·  **Verifier confidence:** high
- **Problem:** _score_pool adds anchor_bonus (default 0.45 in single-scene mode, match.py:306) directly into `base`, and `base` becomes the candidate score and the selection's reported confidence (match.py:407 'qual = round(min(1.0, base), 4)', :427). A shot from an anchor-titled source with clip01=0, no transcript, no face match reports confidence ≈ 0.45 — above min_confidence=0.42 — so ledger.evaluate_flags never raises FLAG_LOW_CONFIDENCE for it and the review surface shows it amber/ok. This contradicts the code's own comment (match.py:342-344) that `base` is pure match QUALITY decoupled only from diversity penalties: anchor continuity is a selection preference, not match quality, yet it inflates the audit metric.
- **Verified because:** Confirmed against the real code. match.py:251-252 adds anchor_bonus into `base` inside _score_pool ('if anchor_bonus and anchor_sids and ps.sid in anchor_sids: base += anchor_bonus'); match.py:306 defaults it to 0.45 in single-scene mode; match.py:407 ('qual = round(min(1.0, base), 4)') and :427 ('confidence=round(min(1.0, base_q), 4)') make that bonus-inflated base the candidate score and reported confidence. The code's own comments contradict this: match.py:342-344 says base is pure 'match QUALITY (drives the reported confidence + flagging)' with selection-only adjustments split into `adj`, and the anchor comment (match.py:246-250) describes the bonus as a continuity preference while 'dialogue-lock / CLIP still decide WHICH shot... fits each beat'. The failure mode is reachable exactly where it matters: ledger.py:69-71 applies FLAG_LOW_CONFIDENCE (threshold 0.42, config.py:44) only when the verifier never ran (verifier_present, ledger.py:51), and verify.py:91-95 skips verification with status 'unavailable' when there is no LLM key — while analyze.py:235-248 still derives video_type='single_scene' and anchor_scenes heuristically (_derive_anchors, analyze.py:219) with no LLM. So a zero-signal anchor-source shot (clip01=0, no transcript, no face) reports confidence 0.45 > 0.42, dodges FLAG_LOW_CONFIDENCE, and review.py:30/:43 renders it amber with an 'ok' badge. Without the bonus the same pick would report 0.0 and be flagged FLAG_NO_CANDIDATE (ledger.py:40-41). The inflated value also propagates into the JSONL ledger, mean_confidence in the QC summary (ledger.py:170), and alternate scores that become sel.confidence on verifier replacement (verify.py:140). Severity is medium rather than high: when an LLM key exists the AI verifier is the authoritative gate (ledger.py:67-69) and confidence flagging is bypassed anyway; there is no crash or security impact. The damage is confined to no-LLM single-scene runs — but that is precisely when human review is the only safety net, and this bug blinds it.
- **Fix:** Keep the anchor-continuity bonus on the selection side only, mirroring the existing base/adj decoupling. In _score_pool (match.py:215-266), stop mutating `base`: compute `bonus = anchor_bonus if (anchor_bonus and anchor_sids and ps.sid in anchor_sids) else 0.0`, append `(base, bonus, sig, ps)` to `scored`, and sort by `base + bonus` so anchor preference still drives ranking. In match_segments, unpack the bonus and fold it into the selection score only — `adj = max(0.0, base + bonus - pen + 0.08 * (ps.shot.quality - 0.5))` (match.py:406) — while `qual = round(min(1.0, base), 4)` (match.py:407) and `confidence=round(min(1.0, base_q), 4)` (match.py:427) stay bonus-free pure match quality. Optionally record `sig["anchor_bonus"] = bonus` so the ledger still shows that continuity preference influenced the pick. With this change a zero-signal anchor shot reports confidence 0.0, correctly triggering FLAG_NO_CANDIDATE/FLAG_LOW_CONFIDENCE in ledger.evaluate_flags and showing red in review.html.

### Produced-file detection globs <sid>.* — stale files and video-only DASH fragments (<sid>.fNNN.mp4) are accepted as successful downloads
- **Where:** `vidlore/clipstudio/download.py:115`  ·  **Lens:** correctness+robustness+concurrency-resources  ·  **Verifier confidence:** high
- **Problem:** After the retry loop (whether or not any attempt succeeded), _download_one declares success if ANY file matching '<sid>.*' with a video extension exists. Two failure modes: (a) lexicographic sorting puts yt-dlp's intermediate format files first — '<sid>.f616.mp4' sorts before '<sid>.mp4' ('f' < 'm'), so after an interrupted merge the video-only fragment is selected as the source's local_path; ASR gets a silent file, transcripts are empty, and dialogue-lock (the strongest matching signal) is dead for that source with no error recorded. (b) If every retry fails but a stale/partial file from an earlier run matches the glob, the source is still marked SOURCE_OK. ingest.py:200-204 has the identical pattern for the manual path.
- **Verified because:** Confirmed against the real code. download.py:115-118 reads exactly as quoted: `produced = next((p for p in sorted(proj.sources_dir.glob(f"{sid}.*")) if p.suffix.lower() in _VIDEO_EXTS), None)`, and _VIDEO_EXTS (ingest.py:25) includes ".mp4". The glob `<sid>.*` matches yt-dlp intermediate format files `<sid>.f616.mp4` (suffix ".mp4"), and lexicographic sort puts "abc.f616.mp4" before "abc.mp4" ('f' < 'm'), so the video-only fragment wins next(). The glob runs unconditionally after the retry loop (lines 100-114 only set last_err and sleep on failure; there is no success flag), so a stale or fragment file is accepted as SOURCE_OK even when every attempt raised. Reachability: the first-choice format is bestvideo+bestaudio (line 86-87), which yt-dlp downloads as <sid>.fNNN.<ext> intermediates deleted only after a successful merge — a kill mid-merge or an ffmpeg PostProcessingError on all retries leaves them (the repo root contains last_ffmpeg_fail.txt, so merge failures occur in practice); source ids are deterministic sha1-of-URL slugs (_cand_id/_source_id), so prior-run leftovers collide with the same stem; grep shows no .f* cleanup anywhere in the package. .part partials are excluded by the suffix check, which is precisely why only completed video-only format files survive. Downstream silence is also confirmed: index.py:216 checks only status==SOURCE_OK and local_path truthy; transcribe_words (index.py:102-113) swallows all exceptions returning []; _dialogue_match (match.py:130-135) returns 0 on empty transcript — so dialogue-lock scores 0 for that source with no error recorded, and probe() returns valid metadata for an audio-less mp4 so nothing fails earlier. The identical pattern exists at ingest.py:200-204, plus an unclaimed third instance at hd_download.py:244-250. Severity caveat: triggering requires a prior abnormal event (interrupted merge, exhausted-retry ffmpeg failure, or stale files from an earlier run), and accepting a stale complete <sid>.mp4 on a failed re-run is arguably benign caching — so this is a real, silent correctness bug, but medium rather than high.
- **Fix:** In _download_one (download.py) and _ingest_url (ingest.py): (1) track success explicitly — set ok=True before the loop's break, and if not ok after the loop, set SOURCE_FAILED immediately without consulting the glob (this kills the stale-file false-OK); (2) when locating the produced file, prefer the exact merge target proj.sources_dir / f"{sid}.mp4" if it exists, and otherwise filter out yt-dlp intermediates with re.fullmatch(rf"{re.escape(sid)}\.f\d+\.[A-Za-z0-9]+", p.name) before applying the suffix check (this kills the fragment-sorts-first selection); better still, take the path yt-dlp itself reports via info.get("requested_downloads", [{}])[0].get("filepath") or YoutubeDL.prepare_filename(info) instead of globbing. Apply the same intermediate-file exclusion to the third instance at hd_download.py:244-250, and optionally delete leftover <sid>.f*.* files before each download attempt so prior-run debris cannot be picked up.

### Index resume cache is returned before the status check and ignores indexing options — auto mode silently reuses roster-less shots
- **Where:** `vidlore/clipstudio/index.py:211`  ·  **Lens:** correctness+robustness+concurrency-resources+config-platform  ·  **Verifier confidence:** high
- **Problem:** index_source returns the cached shots.json unconditionally when it exists (before the SOURCE_OK check at line 216 and without recording which options built it). produce() calls index_all with no references/faceid/roster (orchestrate.py:57) while produce_auto passes them (orchestrate.py:183). If a project is first indexed by the manual pipeline (or any earlier run before Face-ID refs existed), a later auto run reuses the cached shots whose face_ids/identities/ocr_text/ocr_names are all empty — Face-ID scoring (match.py w_face), the OCR junk gate, and the watermark detection silently do nothing for those sources, and there is no force flag in produce_auto to recover. Additionally the cache read 'json.loads(shots_file.read_text())' has no try/except, and the file is written non-atomically (line 295), so a crash mid-write leaves a corrupt cache that raises on every later run.
- **Verified because:** Verified against the real code. (1) /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/index.py:210-214 returns the cached shots.json keyed only on file existence and the force flag — 'if shots_file.exists() and not force: data = json.loads(shots_file.read_text(...)); return [Shot.from_dict(d) for d in data]' — recording nothing about which options (references/faceid/roster) produced it. (2) orchestrate.py:57 (produce) calls index_all with no references/faceid/roster, while orchestrate.py:183 (produce_auto) passes them but has NO force parameter anywhere in its signature (lines 107-113). Both entry points load the same project.json when it exists (orchestrate.py:35-36 and 132-133, resumability is by design), and source IDs are deterministic sha1-of-URL slugs (download.py:41-42), so reruns hit the same cache files. Reachable scenarios: produce() first then produce_auto() on the same project dir; or produce_auto run #1 with _faceid.available()==False (refs={}, faceid_obj=None makes do_faceid False at index.py:237) followed by reruns after installing faceid; or a roster change leaving stale ocr_names. The degradation is silent: match.py reads shots from disk via load_shots (match.py:101), and empty face_ids/ocr_names/ocr_text make the w_face term (match.py:234-244), the OCR junk gate (match.py:74), the watermark/text check (match.py:211), and selection identity (match.py:432-435) all no-op without any warning. (3) The cache read at index.py:212 has no try/except and the write at index.py:295 is a plain non-atomic write_text, so a crash mid-write leaves truncated JSON that raises JSONDecodeError on every later run; produce() can recover via force_index=True but produce_auto() has no recovery path short of manually deleting index/<sid>.shots.json. One minor overclaim: the 'cache returned before the SOURCE_OK check at line 216' point is technically true but practically moot, because index_all (index.py:304), the only caller, already skips non-OK sources before calling index_source. The core claim is accurate.
- **Fix:** In index_source, write a small capability record alongside the cache (either a top-level meta object in shots.json or a sidecar index/<sid>.index.meta.json) capturing {faceid: bool(faceid and references), n_refs: len(references or {}), ocr: bool(cfg.detect_ocr and roster), roster_hash: sha1 of sorted roster names}. On a cache hit, compare the stored record with the current call's capabilities and re-index (treat as force) when the current call requests capabilities the cached run lacked or the roster hash changed. Wrap the cache read in try/except (json.JSONDecodeError, OSError, KeyError) that logs and falls through to re-indexing instead of crashing. Make the write atomic: dump to shots_file.with_suffix('.tmp') then os.replace onto shots_file. Add a force_index: bool = False parameter to produce_auto (orchestrate.py:107) and thread it into the index_all call at line 183. Optionally move the cache check below the SOURCE_OK guard at index.py:216 for tidiness.

### OCR indexing is gated on a roster — manual pipeline never gets ocr_text, disabling match.py's ad/CTA/watermark gates
- **Where:** `vidlore/clipstudio/index.py:238`  ·  **Lens:** correctness  ·  **Verifier confidence:** high
- **Problem:** do_ocr = bool(cfg.detect_ocr and roster) only enables OCR when a roster of names exists (auto mode). But shot.ocr_text is consumed roster-independently: match._ocr_is_junk drops ad/news/CTA/watermark frames (match.py:210-224), _source_is_watermarked (match.py:71-75) drives both pool dropping and build's punch-in watermark crop (build.py:102). In the entire manual produce() pipeline (which passes no roster, orchestrate.py:57), ocr_text is always empty, so junk-frame filtering, watermark detection, and the subtitle penalty silently never fire — watermarked/CTA footage passes straight into the final video with no indication anything was skipped.
- **Verified because:** Confirmed against the real code. index.py:238 is exactly `do_ocr = bool(cfg.detect_ocr and roster)`, and the only place `sh.ocr_text` is ever written (index.py:281, `sh.ocr_text = txt[:200]`) is inside `if do_ocr:` — grep over the package shows no other writer. The manual pipeline `produce()` calls `index_all(proj, cfg, force=force_index, progress=progress)` at orchestrate.py:57 with no roster (default None), and cli.py:72 routes the non-auto CLI to produce(), so every shot in that pipeline has ocr_text == "" even though cfg.detect_ocr defaults True (config.py:94). The three roster-independent consumers then silently no-op: (1) match.py:210-212 `_ocr_is_junk` returns `bool(txt) and _OCR_JUNK.search(txt)` → always False, so the gate at match.py:223-224 never drops ad/news/CTA/watermark frames; (2) match.py:71-75 `_source_is_watermarked` counts `_OCR_JUNK.search(sh.ocr_text)` hits → 0, so pool drop-mode (match.py:102) and build's punch-in watermark crop (build.py:102 → wm_corners empty → crop_filter never set at build.py:564) never trigger; (3) the burned-in-subtitle penalty (match.py:257-259, `len(otext) >= 10`) never fires. There is no guard, warning, or alternate population path — VIDLORE_CLIPSTUDIO_OCR_GATE can only disable the gate further, and the index log (index.py:296) gives no OCR indication. The issue even extends slightly beyond the claim: in produce_auto, roster = analysis.actors (orchestrate.py:179), so an empty actor roster (non-film topics / heuristic degradation) disables OCR there too, despite junk/watermark gating being topic-independent. Severity note: this is silent quality/compliance degradation (watermarked/CTA footage can reach the final video unflagged), not a crash or data loss, and manual-pipeline sources are user-curated — so "medium" rather than high.
- **Fix:** Decouple OCR text extraction from the roster in index.py: change line 238 to `do_ocr = bool(cfg.detect_ocr)` (still followed by the `_ocr.available()` check), and make the roster gate apply only to name matching inside the loop — `sh.ocr_text = txt[:200]` unconditionally when OCR ran, then `sh.ocr_names = _ocr.names_in_text(txt, roster) if roster else []` (ocr.py:61 iterates the roster, so it must be guarded against None). This makes the junk-frame gate, watermark detection/crop, and subtitle penalty work in both pipelines while keeping name-card identification roster-dependent. Two follow-ups: (a) since index_source returns cached shots.json without re-running OCR (index.py:211-214), existing manual projects need force=True or a cache-invalidation note to pick up ocr_text; (b) optionally log when OCR is skipped (engine unavailable or detect_ocr off) in the index_source completion message so the skip is no longer silent.

### write_review crashes on a non-numeric verifier confidence (e.g. null) — kills the QC stage for the whole project
- **Where:** `vidlore/clipstudio/review.py:58`  ·  **Lens:** correctness+robustness  ·  **Verifier confidence:** high
- **Problem:** verify_frame stores the model's parsed JSON verdict verbatim with no type validation (verify.py:61-62, sel.verifier = v at :118). The prompt asks for '"confidence": 0.0-1.0' but if the model returns null or a string ('high'), review._card's f-string 'float(v.get("confidence",0)):.2f' raises TypeError/ValueError (dict.get returns the existing None, not the default). write_review is called unconditionally in both pipelines (orchestrate.py:83, :200) BEFORE build, so one malformed verdict aborts the entire run after all the expensive download/index/verify work.
- **Verified because:** Confirmed against the real code. review.py:57-58 contains exactly `ver_html = f'<span class="vok">✓ verified {float(v.get("confidence",0)):.2f}</span>'` in the `vd == "keep"` branch. verify.py:61-62 returns the raw `json.loads(m.group(0))` of whatever JSON-ish blob the vision model emits, with no type validation of "confidence", and verify_and_repair stores it verbatim on the selection (`v["status"] = "ok"; sel.verifier = v` at verify.py:117-118; same for alternates at :143-145). The dict.get semantics claim is correct and was verified by execution: `{'confidence': None}.get('confidence', 0)` returns None (the default applies only when the key is absent), so `float(None)` raises TypeError and `float('high')` raises ValueError. No guard exists anywhere on the path: `_card`/`cards`/`write_review` (review.py:33-172) have no try/except, and orchestrate.py calls `_review.write_review(proj, segs)` bare at line 83 (produce) and line 200 (produce_auto), both before the `do_build` block, so the exception aborts the run after the expensive download/index/match/cut/verify stages. ledger.finalize, which runs first, does not sanitize: evaluate_flags reads only status/verdict/correct_subject_visible from the verifier dict, and write_ledger/write_review_queue round sel.confidence (the numeric selection score), never the verifier dict's "confidence". The only caveat is that the trigger requires the LLM to return verdict "keep" with a non-numeric confidence despite the prompted schema — probabilistic, not deterministic — but the module's own retry/parse-fallback design shows model schema drift is an anticipated failure mode, and the malformed dict is even persisted to project.json via proj.save() (verify.py:160) before the crash point.
- **Fix:** Normalize the verdict at the source in verify.verify_frame (verify.py:62): after json.loads, coerce and clamp confidence — e.g. `d["confidence"] = max(0.0, min(1.0, float(d.get("confidence") or 0)))` wrapped in try/except (TypeError, ValueError) falling back to 0.0 — and validate `d.get("verdict") in ("keep", "replace")`, returning None otherwise so the existing `sel.verifier = {"status": "error"}` path handles it. This fixes all consumers (review.py, ledger.py) and the value persisted in project.json. As defense-in-depth, make review.py:58 tolerant by computing `conf` via the same guarded float coercion before the f-string, and optionally wrap the write_review calls at orchestrate.py:83 and :200 in try/except-with-log so a report-rendering failure can never abort the pipeline before build. Severity corrected to medium: unhandled full-run abort when triggered, but the trigger is a low-frequency off-schema LLM response and no project state is lost (saved before the crash point).

### int(o.get('i', -1)) runs outside the LLM try/except — a non-numeric beat index crashes produce_auto instead of falling back to heuristic
- **Where:** `vidlore/clipstudio/analyze.py:280`  ·  **Lens:** correctness+robustness  ·  **Verifier confidence:** high
- **Problem:** analyze_script only wraps _llm_analyze in try/except (lines 250-254). The beat-mapping comprehension at line 280 parses the LLM's per-beat 'i' field afterwards, unprotected: if any batch object has i='beat 3' (ValueError) or i=null (TypeError), the exception propagates out of analyze_script and crashes produce_auto at stage 1/9 — even though the function has a dedicated heuristic fallback designed for exactly this kind of LLM misbehavior. The stage-2 JSON arrays are parsed with a bare 'pass' on failure (lines 179-181) but field-level type errors slip through json.loads and explode here.
- **Verified because:** Confirmed against the real code. /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/analyze.py:280 reads exactly `by_i = {int(o.get("i", -1)): o for o in data.get("beats", []) if isinstance(o, dict)}` and sits outside the only try/except in analyze_script (lines 250-254, which wraps the _llm_analyze call only). The beat objects originate from `json.loads(mm.group(0))` at line 179 of raw LLM output; the try/except there (179-181) only catches JSON parse failures, so a well-formed array containing `{"i": "beat 3"}` (ValueError on int()) or `{"i": null}` (TypeError on int(None)) reaches line 280 unfiltered — the only sanitization is `isinstance(o, dict)`. The exception propagates: produce_auto calls analyze_script bare at /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/orchestrate.py:145 during stage "1/9 · analyze script" with no guard; the CLI crashes outright, and the web wrapper's catch-all (web.py:67) only marks the job FATAL — no recovery. This is plainly an oversight, not fail-fast design: every sibling field in the same block is defensively coerced (str() casts, .get() defaults, isinstance filters), a dedicated heuristic fallback exists three lines above (lines 255-257), and the file's own comments document exactly this class of LLM misbehavior ("the 107-beat Cersei failure", "the LLM returns anchor_scenes inconsistently under load"). Severity is medium rather than high: the prompt explicitly specifies "i":int and echoes integer indices, so the trigger is a low-probability LLM deviation, and the crash occurs at stage 1/9 before any expensive download/render work — but when it fires it kills the whole job where the code's contract is graceful degradation to the heuristic path.
- **Fix:** Make the beat-index parse per-object and tolerant, so one malformed beat is skipped instead of crashing the run. Replace line 280 in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/analyze.py with a loop: `by_i = {}` then `for o in data.get("beats", []): if not isinstance(o, dict): continue; try: by_i[int(o.get("i", -1))] = o; except (TypeError, ValueError): continue`. This is strictly better than wrapping the whole block and falling back to the heuristic, because all correctly-indexed beats keep their LLM enrichment and only the bad entries degrade (beats absent from by_i are already handled by the `if not o: continue` at lines 282-284). Optionally log a one-line warning with the count of skipped beats via the existing `log()` helper for observability.

### CLI flags silently dropped across modes: --voiceover ignored in manual mode; --no-enrich/--force-index ignored in auto mode
- **Where:** `vidlore/clipstudio/cli.py:72`  ·  **Lens:** correctness  ·  **Verifier confidence:** high
- **Problem:** Mode selection is implicit (any --sources/--add → produce(), else produce_auto()), and the flag sets don't match the calls: (1) --voiceover (cli.py:57) is only forwarded in the AUTO branch (cli.py:85); produce() has no voiceover parameter (orchestrate.py:22-26) and never passes one to build_video even though build_video fully supports it (build.py:320, 350-356) — a user supplying their own sources AND their own voiceover gets TTS instead, with no warning. (2) Symmetrically, --no-enrich and --force-index (cli.py:48-49) do nothing in AUTO mode: produce_auto has no enrich/force_index parameters and calls index_all without force (orchestrate.py:183), so '--force-index' on an auto re-run silently reuses stale caches.
- **Verified because:** Every cited line checks out. (1) cli.py:57 defines --voiceover on the single flat parser; the manual branch (cli.py:70-78, triggered by any --sources/--add) calls produce() without it, and produce() (orchestrate.py:22-26) has no voiceover parameter at all — its build_video call (orchestrate.py:92-94) omits voiceover, so build.py:350 ('if voiceover and Path(voiceover).exists()') is never true and the use_tts=True default produces TTS narration. build_video (build.py:317-321) fully supports voiceover, and only produce_auto (orchestrate.py:111, 209) forwards it. main() contains no check or warning on args.voiceover in the manual branch — the flag is silently swallowed. (2) cli.py:48-49 define --no-enrich/--force-index, but the auto branch (cli.py:81-87) forwards neither; produce_auto (orchestrate.py:107-113) has no enrich/force_index params, and its index_all call (orchestrate.py:183) passes no force=, which defaults to False (index.py:300-301), so --force-index on an auto re-run silently reuses cached indexes. produce_auto never calls enrich_with_llm (segments come from analyze_script), so --no-enrich is also a no-op there. No guards, argparse groups, or callers mitigate this; the web UI (web.py) only exercises auto mode and is unaffected by the manual-mode hole. The one severity caveat: the outcome is wrong/silently-degraded output and wasted render time, not data loss or a crash, so this is medium rather than high.
- **Fix:** Thread the flags through both paths and warn on inapplicable ones. (a) Add 'voiceover: Optional[str] = None' to produce()'s signature in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/orchestrate.py and pass 'voiceover=voiceover' in its build_video call (line ~92), then add 'voiceover=(args.voiceover or None)' to the produce() call in cli.py:72-78. (b) Add 'force_index: bool = False' to produce_auto()'s signature and pass 'force=force_index' to index_all at orchestrate.py:183, then forward 'force_index=args.force_index' from the auto branch in cli.py:81-87. (c) For --no-enrich in auto mode, either plumb an 'enrich' flag into analyze_script's LLM pass or, at minimum, have main() print a warning that --no-enrich is ignored in auto mode; likewise add a startup validation in main() that warns when a flag is supplied that the selected mode cannot honor, so future drift fails loudly instead of silently.

### Gemini-only setups burn ~5.6s of retry backoff per LLM call because the keyless Claude path returns '' and is retried as 'transient'
- **Where:** `vidlore/clipstudio/llm.py:180`  ·  **Lens:** correctness+robustness  ·  **Verifier confidence:** high
- **Problem:** _claude_complete returns '' immediately when no Anthropic key resolves (llm.py:123-126). _with_retry treats empty output as a retryable transient (llm.py:168-172), sleeping 0.8+1.6+3.2 ≈ 5.6s over the default 4 attempts before giving up. _try_gemini guards with gemini_available() but _try_claude has no availability check, so in a Gemini-only configuration EVERY complete() call (Claude-first by default) wastes ~5.6s before falling back — analyze alone makes 1+ceil(beats/18) calls, and verify makes one per selection plus per attempted alternate, silently adding minutes to a run.
- **Verified because:** Confirmed against /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/llm.py. (1) _claude_complete (lines 121-131) returns '' when no key resolves: `key = (getattr(eng_cfg, "anthropic_api_key", "") if eng_cfg else "") or os.environ.get("ANTHROPIC_API_KEY", ""); if not key: return ""`. (2) _with_retry (lines 163-178) treats blank output as retryable: `out = call(); if out and out.strip(): return out; if i < n - 1: _time.sleep(min(8.0, 0.8 * (2 ** i)))` — with the default 4 attempts (_retries(), lines 140-144) that is 0.8+1.6+3.2 = 5.6s of sleep before returning None. (3) _try_gemini guards with `if not gemini_available(): return None` (lines 183-186) but _try_claude (lines 180-181) has no equivalent guard, and `seq = (_try_claude, _try_gemini)` runs Claude first by default since _claude_first() is True unless VIDLORE_CLIPSTUDIO_LLM_PROVIDER is gemini/google/vertex (lines 29, 134-137, 188). (4) Not guarded elsewhere: callers gate only on has_llm(eng_cfg) (analyze.py:235, verify.py:91), which is True when Gemini alone is available (llm.py:196-201), so every complete() call in a Gemini-only default-provider setup enters the dead Claude path. One nuance verified: `import anthropic` at line 122 precedes the key check, and if the package were absent the ImportError would be non-transient and skip the sleeps — but requirements.txt line 6 installs anthropic>=0.39.0 unconditionally, so the silent ''-with-5.6s-backoff path is what standard installs hit. Call volume matches the claim: analyze.py makes 1 + ceil(beats/18) calls (lines 129, 152-175), verify.py one per selection plus per attempted alternate (lines 111, 123+), segment.py:165 adds more — easily minutes of pure sleep per run. Severity correction: this is latency-only (Gemini still serves every call correctly), so medium rather than anything higher.
- **Fix:** Mirror the Gemini guard in _try_claude: add a claude_available(eng_cfg) helper (returns bool((getattr(eng_cfg, "anthropic_api_key", "") if eng_cfg else "") or os.environ.get("ANTHROPIC_API_KEY", "")) — the same expression already used in has_llm and active_provider) and have _try_claude do `if not claude_available(eng_cfg): return None` before calling _with_retry, so a keyless setup falls through to Gemini instantly. Alternatively (or additionally), make _claude_complete raise a distinct non-transient error (e.g. RuntimeError("no anthropic key")) instead of returning '' — _is_transient won't match it, so _with_retry returns None on the first attempt with zero sleeps. The guard approach is preferable as it also avoids constructing the lambda and keeps retry semantics for genuinely-empty API responses intact.

### Shot-detection fallback uses duration 0.0 when probe fails — source silently indexed with zero shots
- **Where:** `vidlore/clipstudio/index.py:63`  ·  **Lens:** correctness+robustness  ·  **Verifier confidence:** high
- **Problem:** When PySceneDetect fails, detect_shots falls back to uniform chunks over 'total_dur or probe(path).get("duration", 0.0)'. ingest.probe returns {} on total failure (ingest.py:103), so duration is 0.0, the while loop body never executes, and the function returns an empty list — the source is written to the index with 0 shots and contributes nothing to matching, with no error (only an easy-to-miss '0 shots detected' progress line). A corrupt-but-present download (e.g. the fragment-file case) hits both failure paths at once and vanishes from the pool silently.
- **Verified because:** Confirmed against the real code. index.py:63-68 reads exactly as quoted: when PySceneDetect fails (index.py:59-60 swallows the exception, scenes=[]), the fallback computes `dur = total_dur or probe(path).get("duration", 0.0)`; ingest.probe returns {} on total failure (ingest.py:102-103: `except Exception: return {}`), so dur=0.0, the `while t < dur` loop never runs, `_merge_short([])` returns [], and detect_shots returns []. The path is reachable end-to-end with no guard: (a) a corrupt-but-present download gets SOURCE_OK with duration 0.0 — in download.py:115-127 and ingest.py:199-213, after all yt-dlp retries fail, the produced file is located via `glob(f"{sid}.*")` with suffix in _VIDEO_EXTS, which matches leftover yt-dlp fragment files (verified: Path("sid.f616.mp4").suffix == ".mp4" and the glob matches), probe() on the corrupt fragment fails both PyAV and ffprobe, yet `sv.status = SOURCE_OK` is set unconditionally; (b) index_all (index.py:303-305) filters only on status, so the source is indexed; (c) the only signal is the progress line "0 shots detected" (index.py:227) — no exception, no status change; (d) downstream match._load_pool (match.py:98-115) silently adds zero pool entries (grep found no zero-shot guard anywhere in the package; match.py:72 `if not shots: return False` is just the watermark check passing through). The claim even understates it slightly: index.py:295 persists the empty shots.json and the resume cache (index.py:211-214) returns "cached (0 shots)" on every later run, making the silent failure sticky unless force=True. Also, probe can succeed yet return duration 0.0 (PyAV path, ingest.py:78-80), which hits the same hole — the bug is broader than the {} case alone.
- **Fix:** Two-layer fix. (1) Treat zero shots as a failure, not a result: in index_source (index.py, after `bounds = detect_shots(...)` at line 226), if `not bounds`, log an explicit error, set `source.status = SOURCE_FAILED` / `source.error = "shot detection failed (corrupt file or zero duration)"`, persist the project, and return [] WITHOUT writing shots.json — so the resume cache doesn't permanently pin the empty result; optionally have detect_shots raise (or return None) when the fallback resolves dur <= 0 so the caller can distinguish "unreadable" from "genuinely no scenes". (2) Stop minting SOURCE_OK for unreadable downloads: in download.py:115-127 and ingest.py:199-213, after locating the produced file, require `probe(produced).get("duration", 0) > 0` (or a cheap ffmpeg decode check) before setting SOURCE_OK, otherwise mark SOURCE_FAILED with the last yt-dlp error; additionally exclude yt-dlp fragment files from the produced-file glob (skip names matching r"\.f\d+\.[^.]+$") so a leftover fragment is never mistaken for the merged output.

### Alternates are not score-ordered: a demoted previous-best is appended last or dropped, and the verifier only tries the first 3
- **Where:** `vidlore/clipstudio/match.py:413`  ·  **Lens:** correctness  ·  **Verifier confidence:** high
- **Problem:** Candidates are scanned in base-score order, but when a new pick demotes the previous best, that (highest-scoring) candidate is appended at the END of alternates — and silently dropped entirely if its source is already in seen_alt_sources or the list is full, even though it outscores every existing alternate. verify_and_repair walks sel.alternates strictly in list order with max_replacements=3 (verify.py:123-126), so the strongest replacement may be tried last or never, making verifier repairs pick weaker swaps than the data supports.
- **Verified because:** Confirmed against the real code. (1) Scan order: _score_pool sorts candidates by base score descending (match.py:265 'scored.sort(key=lambda x: x[0], reverse=True)'), so match_segments iterates highest-quality first. (2) match.py:412-417 is exactly as quoted: on demotion the previous best is appended at the END of alternates, and only if 'best[3].source_id not in seen_alt_sources and len(alternates) < cfg.candidates_per_segment' (default 6, config.py:86) — otherwise silently dropped. Both drop paths are reachable: when a candidate becomes best (line 415) its source is NOT added to seen_alt_sources, so a weaker same-source candidate can take the slot via the elif (line 416); when the best is later demoted, line 413's source check fails and the highest-base candidate is discarded while the weaker same-source shot remains. The 'list full' drop happens whenever 6 elif-alternates accumulate before demotion. (3) No guard elsewhere: sel.alternates preserves insertion order (match.py:429), grep finds no re-sort of alternates in verify.py/review.py/build.py/ledger.py, and verify_and_repair (verify.py:123-126) walks sel.alternates strictly in list order breaking after tried >= max_replacements (3); the only caller (orchestrate.py:196) uses the default 3. So with 6 alternates, a demoted top-base candidate at position 4-6 is never tried, and in the drop cases it does not exist at all — the verifier promotes the FIRST alternate passing the LLM 'keep' verdict, so order directly determines the swap. (4) Reachability: demotion of the top-base candidate is routine because adj subtracts reuse/recency/source-run/dup/brightness penalties (match.py:357-405) that grow across any multi-segment project, while the alternate's stored score is base quality. Minor claim overstatement: only the FIRST demoted best is guaranteed to outscore every existing alternate (a second demoted best may not), but the global base-max candidate is precisely the one pushed last or dropped, so the core claim stands. Severity is honest at medium rather than high: no crash or data loss, and any promoted alternate must still pass LLM frame verification — the harm is weaker-than-available swaps and unnecessary 'verifier_failed' flags.
- **Fix:** In match_segments (match.py:~412-417), stop maintaining alternate order via insertion. Simplest robust fix: during the loop, collect every non-best viable candidate (including demoted bests) into a list of (base, cand) without capping or source-dedup decisions inline; after the loop, build sel.alternates by sorting that list by base score descending, then applying source diversity (first occurrence per source_id, excluding the final best's source/shot) and truncating to cfg.candidates_per_segment. Equivalently minimal: keep the existing logic but (a) on demotion, insert the demoted best into alternates by its score (replacing the lowest-scoring entry, or the same-source weaker entry, if full or its source is taken) instead of append-or-drop, and (b) sort alternates by .score descending before assigning at match.py:429. Either variant guarantees verify_and_repair's first max_replacements=3 attempts (verify.py:123-126) hit the strongest candidates first.

### cfg.watermark_mode is ignored by match — setting it programmatically to 'drop' yields uncropped watermarked footage
- **Where:** `vidlore/clipstudio/match.py:96`  ·  **Lens:** correctness+matching-quality+matching-quality+config-platform  ·  **Verifier confidence:** high
- **Problem:** match._load_pool re-reads VIDLORE_CLIPSTUDIO_WATERMARK_MODE from os.environ directly, while build_video reads cfg.watermark_mode (build.py:394). The two can disagree: a caller setting cfg.watermark_mode='drop' (the documented ClipConfig field, config.py:58) without the env var gets match defaulting to 'crop' (watermarked sources KEPT in the pool) while build sees 'drop' and skips the punch-in crop (wm_corners={}) — so watermarked sources both stay in the cut AND are never cropped, the worst of both modes. The config field is effectively dead for the pool gate.
- **Verified because:** Verified against the real code. match.py `_load_pool(proj, progress=None)` (line 91) re-reads the env var directly at line 96: `wm_mode = os.environ.get("VIDLORE_CLIPSTUDIO_WATERMARK_MODE", "crop").strip().lower()`, and drops watermarked sources only when `gate_on and wm_mode == "drop"` (line 102). Its sole caller `match_segments` (match.py:269) receives `cfg: ClipConfig` but calls `_load_pool(proj, progress=progress)` (line 275) without it. Meanwhile build.py:394 reads `_wm_mode = (getattr(cfg, "watermark_mode", "crop") or "crop").lower()` and computes `wm_corners` only when `_wm_mode == "crop"` (lines 401-402); the punch-in crop filter at build.py:564 is applied only for `src.id in wm_corners`. config.py:58 documents `watermark_mode` as a ClipConfig field ("crop"|"drop"). Reachability confirmed: orchestrate.produce()/produce_auto() expose `cfg: Optional[ClipConfig]` and pass the same cfg to match_segments (orchestrate.py:74/187) and build_video (orchestrate.py:92/207); nothing syncs cfg.watermark_mode back into os.environ and no guard exists. So `ClipConfig(watermark_mode="drop")` without the env var yields: match keeps watermarked sources in the pool (env default "crop") while build skips cropping (wm_corners={}) — watermarked footage in the final cut, uncropped, exactly the worst-of-both outcome claimed. The cfg field is dead for the pool gate. Severity is tempered because no in-repo call site sets watermark_mode programmatically today, and env-var-driven usage is self-consistent (cfg's default reads the same env var), so the shipped CLI/web paths are unaffected — it is a latent but real API-contract bug with silent output degradation.
- **Fix:** Thread cfg into the pool gate: change `_load_pool` in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/match.py to `def _load_pool(proj: ClipProject, cfg: ClipConfig, progress=None)`, replace line 96 with `wm_mode = (getattr(cfg, "watermark_mode", "crop") or "crop").strip().lower()`, and update the single call site at match.py:275 to `_load_pool(proj, cfg, progress=progress)`. This makes match and build read the same source of truth (cfg.watermark_mode, whose default already comes from the env var via config.py:58), so programmatic 'drop' both excludes watermarked sources from the pool and correctly skips the crop pass, while env-only users see no behavior change.

### Missing-Claude-key empty result is retried as 'transient', burning ~5.6s of backoff on every single LLM call in Gemini-only setups
- **Where:** `vidlore/clipstudio/llm.py:125`  ·  **Lens:** robustness  ·  **Verifier confidence:** high
- **Problem:** _claude_complete returns '' when no API key resolves, and _with_retry treats empty output as a retryable transient (sleeping 0.8+1.6+3.2 = 5.6s across the default 4 attempts before giving up). _try_gemini guards with gemini_available(), but _try_claude has no equivalent key check, and Claude is the default primary. So in a Gemini-only deployment every complete() call deterministically wastes ~5.6s before reaching the working provider: a 107-beat analyze (1 + ceil(107/18) = 8 calls) loses ~45s, and a verify pass over 50 selections plus alternates loses several minutes — all silently.
- **Verified because:** Confirmed against /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/llm.py. (1) Lines 125-126: _claude_complete returns "" when no key resolves ('if not key: return ""'). (2) Lines 167-172: _with_retry treats blank output as retryable ('if out and out.strip(): return out' then '_time.sleep(min(8.0, 0.8 * (2 ** i)))'), sleeping 0.8+1.6+3.2 = 5.6s over the default 4 attempts (_retries() defaults to 4) before returning None. (3) Lines 180-186: _try_gemini prechecks gemini_available() but _try_claude has no key precheck; line 188 makes Claude primary by default (_PROVIDER defaults to "anthropic"). Reachability verified: anthropic>=0.39.0 is in requirements.txt so 'import anthropic' (line 122) succeeds and the no-key path is hit (an ImportError would instead fail fast since _is_transient doesn't match it). No guard elsewhere prevents this: callers in segment.py:154, verify.py:91, and analyze.py:235 gate only on has_llm(), which returns True when Gemini alone is available (llm.py:196-201) and does not skip or reorder the Claude attempt. The key resolution is deterministic per call, so every complete() in a Gemini-only deployment with the documented default config (module docstring explicitly advertises no-key Claude→Gemini fallback) wastes exactly 5.6s. One arithmetic nit in the claim: analyze.py BATCH=18 (line 152) plus 1 high-level call gives 1+ceil(107/18) = 7 calls for 107 beats (claim said 8), so ~39s not ~45s; the verify-pass estimate (verify.py:102-111 calls complete() per selection plus alternate re-verification at lines 120-130, several minutes for 50 selections) holds.
- **Fix:** Add a Claude availability precheck mirroring _try_gemini's guard. Extract the existing key-resolution expression (already duplicated in _claude_complete, has_llm, and active_provider) into a helper, e.g. 'def claude_available(eng_cfg=None) -> bool: return bool((getattr(eng_cfg, "anthropic_api_key", "") if eng_cfg else "") or os.environ.get("ANTHROPIC_API_KEY", ""))', then in complete() change _try_claude to 'if not claude_available(eng_cfg): return None' before calling _with_retry — exactly parallel to _try_gemini's gemini_available() check at llm.py:184. Optionally also make _claude_complete raise a distinct non-transient exception (e.g. RuntimeError("no anthropic key")) instead of returning "", so a missing key can never be mistaken for a transient empty response by _with_retry. This eliminates the deterministic 5.6s-per-call backoff in Gemini-only deployments while preserving the empty-output retry for genuinely transient truncations.

### All discovery search errors are swallowed bare — total network/yt-dlp failure is indistinguishable from 'no results'
- **Where:** `vidlore/clipstudio/discover.py:151`  ·  **Lens:** robustness  ·  **Verifier confidence:** high
- **Problem:** _ytsearch and _archive_search wrap their entire body in `except Exception: pass`. With no connectivity, an expired yt-dlp extractor, or an archive.org outage, all ~60 queries fail silently and discover_sources returns an empty list; the only user-visible outcome is orchestrate.py:159's generic SystemExit 'source discovery found nothing — check connectivity / movie name', with zero evidence of whether searches errored (and what the error was) versus genuinely returned nothing. In the web portal this combines with the uncaught-SystemExit bug to produce a forever-spinning job with no diagnostic at all.
- **Verified because:** Verified against the actual code. discover.py:_ytsearch (lines 139-152) is literally `try: with yt_dlp.YoutubeDL(opts) as y: info = y.extract_info(f"ytsearch{n}:{query}", download=False) ... except Exception: pass`, and _archive_search (lines 159-173) has the identical pattern around `requests.get("https://archive.org/advancedsearch.php", ...)`. No logging module is imported in discover.py and the exception is discarded with no counter, retry, or guard anywhere else (discover_sources at lines 316-320 just concatenates results per query; the only trace is the ambiguous progress line "discover: '<q>' → N cumulative", which reads 0 both for errors and for genuinely empty results). orchestrate.py:159 then raises exactly the claimed generic `SystemExit("source discovery found nothing — check connectivity / movie name.")`. The web-portal amplification is also real: web.py:67's job thread catches only `except Exception`; SystemExit derives from BaseException so it escapes, and I empirically confirmed the default threading excepthook silently swallows SystemExit (no stderr, job dict never gets done=True), producing a forever-spinning job whose log shows only zero-cumulative lines with no error cause. The claim's only minor overstatement is "no diagnostic at all" — the zero-cumulative log lines are visible — but the core assertion (total provider failure is indistinguishable from no results, and the actual error is never surfaced anywhere) is accurate.
- **Fix:** In discover.py, stop discarding the exceptions: change `except Exception: pass` in both _ytsearch and _archive_search to record the failure (e.g., return a (results, error) tuple or append to a module-level/passed-in error list, and log `f"discover: ytsearch failed for {query!r}: {e!r}"` via the existing progress callback or a logging.getLogger(__name__).warning). In discover_sources, count failed vs. empty queries and include that in the final log line; if every query errored, raise a distinct error such as `RuntimeError(f"all {len(queries)} discovery searches failed; last error: {last_err!r}")` so orchestrate.py:159's message can distinguish connectivity/extractor failure from a bad movie name. Separately (the compounding bug), web.py:54-74 should catch `except (Exception, SystemExit) as e` (or BaseException with re-raise of KeyboardInterrupt) in _run_job so a SystemExit from orchestrate.py marks the job done=False with the error string instead of leaving it spinning forever.

### Unguarded env-var parsing crashes build_video and module import (VISUAL_SIM, LETTERBOX_PX, QUERY_CAP, POT_PORT)
- **Where:** `vidlore/clipstudio/build.py:423`  ·  **Lens:** robustness+ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** Several raw os.environ parses lack the try/except that config.py's _f/_i helpers provide: build.py:423 `float(os.environ.get("VIDLORE_CLIPSTUDIO_VISUAL_SIM", "0.86"))` and build.py:634 `int(os.environ.get("VIDLORE_CLIPSTUDIO_LETTERBOX_PX", "132") or 132)` raise ValueError mid-build on a malformed value; discover.py:130 `int(os.environ.get("VIDLORE_CLIPSTUDIO_QUERY_CAP", "60"))` does the same in discovery; worst, hd_download.py:53 `POT_PORT = int(os.environ.get("VIDLORE_HD_POT_PORT", "4416"))` runs at IMPORT time and the import happens un-guarded inside _download_one (download.py:47), so the ValueError propagates through fut.result() (download.py:166) and crashes the entire download batch — including non-YouTube candidates — violating the module's own 'degrades gracefully' contract.
- **Verified because:** Verified against the real code and reproduced empirically. (1) build.py:423 `_SIMT = float(_os3.environ.get("VIDLORE_CLIPSTUDIO_VISUAL_SIM", "0.86"))` and build.py:634 `_bar_h = int(os.environ.get("VIDLORE_CLIPSTUDIO_LETTERBOX_PX", "132") or 132)` are both inside build_video (def line 317) with no try/except — the `or 132` only covers empty string, and the try at line 636 starts after the parse. (2) discover.py:130 `_qcap = int(_os.environ.get("VIDLORE_CLIPSTUDIO_QUERY_CAP", "60"))` in build_queries is called unguarded from discover_sources (line 305). (3) hd_download.py:53 `POT_PORT = int(os.environ.get("VIDLORE_HD_POT_PORT", "4416"))` executes at module import; download.py:47 `from . import hd_download as _hd` is the first line of _download_one, BEFORE the is_youtube check (line 57) and outside the try at 58-63 which wraps only download_hd. The ValueError propagates through fut.result() at download.py:166, aborting the as_completed loop — results from already-finished futures are dropped and proj.save() (line 190) never runs, so even non-YouTube downloads are lost. The same unguarded import exists at ingest.py:138 and discover.py:271; callers in orchestrate.py (lines 92/155/165/207) have no guards. Reproduced: VIDLORE_HD_POT_PORT=abc raises ValueError at module exec; VIDLORE_CLIPSTUDIO_VISUAL_SIM=0.86s raises ValueError. This contradicts hd_download.py's own docstring contract (lines 22-24, 'degrades gracefully... the tool keeps working') and the codebase convention in config.py:13-24, whose _f/_i helpers wrap ~30 other env vars in except (TypeError, ValueError). Severity is medium, not higher: the trigger requires a user to set a malformed value on an opt-in tuning env var (defaults are valid), but the impact is a hard crash at stage 9 of an expensive pipeline or loss of an entire in-flight download batch.
- **Fix:** Reuse the existing fallback helpers from config.py at all four sites. In build.py and discover.py, replace the raw parses with the config helpers (e.g. `from .config import _f, _i`; build.py:423 → `_SIMT = _f("VIDLORE_CLIPSTUDIO_VISUAL_SIM", 0.86)`, build.py:634 → `_bar_h = _i("VIDLORE_CLIPSTUDIO_LETTERBOX_PX", 132)`, discover.py:130 → `_qcap = _i("VIDLORE_CLIPSTUDIO_QUERY_CAP", 60)`), or inline `try: ... except (TypeError, ValueError): <default>` if avoiding the import. In hd_download.py:53, wrap the module-level parse: `try: POT_PORT = int(os.environ.get("VIDLORE_HD_POT_PORT", "4416")) except ValueError: POT_PORT = 4416` — this alone restores the documented graceful-degradation contract for the download batch. Optionally also harden the three call-site imports (download.py:47, ingest.py:138, discover.py:271) with a try/except that disables the HD path on import failure, so any future module-level error in hd_download degrades to the legacy 360p path instead of crashing the batch.

### Per-batch beat enrichment silently drops up to 18 beats on truncated/invalid LLM JSON
- **Where:** `vidlore/clipstudio/analyze.py:181`  ·  **Lens:** robustness+integration  ·  **Verifier confidence:** high
- **Problem:** Stage-2 batches extract JSON with the greedy regex r"\[.*\]" and on any parse failure execute a bare `pass` — the whole 18-beat batch keeps only heuristic values (no scene_query, quote, required_entity) with no warning, while the progress counter still advances as if enriched. The token budget makes this likely: max_tokens = min(8000, 600 + 18*180) = 3840 for 8 verbose fields per beat (~210 tokens/beat), so truncation mid-array (which breaks both the regex match and json.loads) is a realistic steady-state, silently degrading discovery queries and dialogue-lock for those beats.
- **Verified because:** Confirmed against the real code. analyze.py lines 174-182 are exactly as quoted: max_tokens=min(8000, 600 + len(chunk)*180) with BATCH=18 (line 152) gives 3840 tokens (~213/beat) for a 9-field-per-beat JSON array including verbose expected_visual, scene_query, and quote; the response is scanned with the greedy r"\[.*\]" regex, json.loads failures hit a bare `except Exception: pass`, there is no else branch for a regex miss, and the progress log on line 182 advances unconditionally. No guard exists elsewhere: llm.py complete() retries only on transient exceptions or fully empty output ("if out and out.strip(): return out", llm.py:168) and never checks stop_reason, so a truncated non-empty response passes through; truncation mid-array leaves no closing "]" (the schema has no nested arrays) so the regex misses and the entire 18-beat batch is dropped silently. The outer try/except in analyze_script (lines 250-257) only falls back to heuristic on exceptions or data=None — the bare-pass path returns truthy data, so no fallback or warning fires, and analysis.source (line 278) still reports the LLM provider. Beats from dropped batches hit `continue` at lines 283-284 and keep heuristic defaults. Severity is honest: scene_query drives discovery (discover.py:106) and matching (match.py:310,327), required_entity drives entity/face-lock (match.py:188-190, discover.py:110,383), and quote is the sole input to the _dialogue_match scene-lock (match.py:134) — and the file's own docstring (line 95, "the 107-beat Cersei failure") records that LLM truncation of this exact kind has already happened in this pipeline. The only soft spot is "realistic steady-state" (truncation at ~213 tokens/beat is plausible, not guaranteed), but the silent-drop path is also reachable via any malformed JSON, so the finding stands.
- **Fix:** In _llm_analyze's batch loop (analyze.py:176-182): (1) replace the silent failure paths with a logged warning via _log when the regex misses or json.loads raises, including batch range and a snippet of the bad output; (2) on failure, retry the batch once — either with a larger budget (e.g. max_tokens=min(8000, 600 + len(chunk)*350)) or by splitting the chunk in half (9+9), which also bounds the blast radius; (3) optionally salvage truncated output by trimming to the last complete object (rfind of "}," before the cut) and appending "]" before json.loads; (4) have _claude_complete surface resp.stop_reason == "max_tokens" (and the Gemini finish_reason equivalent) so complete()/callers can detect truncation instead of treating it as success; (5) after the loop, log enriched-vs-total beat counts (len(beat_out) vs len(beats)) so a degraded run is visible, and consider setting a flag on ScriptAnalysis when coverage is partial rather than reporting source="claude" unconditionally.

### shots.json written non-atomically and resume path parses it unguarded — corrupt cache bricks every later run
- **Where:** `vidlore/clipstudio/index.py:295`  ·  **Lens:** concurrency-resources  ·  **Verifier confidence:** high
- **Problem:** index_source persists the per-source shot index with a plain write_text (no temp-file + rename), and the resume path at the top of the function calls json.loads on it with no try/except, before any status check. Portal jobs run in daemon threads (web.py:221) that are killed abruptly on server shutdown, and indexing is the longest stage — a kill or crash mid-write leaves a truncated shots.json. Every subsequent run of the project then raises json.JSONDecodeError inside index_source (escaping as an unhandled exception out of index_all/produce_auto) until someone manually deletes the file; the resume feature turns a one-off interruption into a permanently broken project.
- **Verified because:** The cited code is accurate. index.py:295 writes shots.json with a plain `shots_file.write_text(json.dumps([s.to_dict() for s in shots], indent=1), ...)` — no temp-file+rename, and grep confirms no atomic-write pattern (no os.replace/.tmp) anywhere in vidlore/clipstudio. The resume path at index.py:211-214 gates only on `shots_file.exists() and not force` and calls `json.loads` with no try/except, so a truncated file is treated as a valid cache and raises json.JSONDecodeError. No caller guards it: orchestrate.py has zero try/except, and produce_auto (orchestrate.py:183) calls index_all with NO force argument, so auto mode has no escape hatch; orchestrate.py:132-133 explicitly resumes an existing project dir, and cli.py documents `-p PROJECT_DIR (created if absent)` reuse, so same-dir reruns are the intended workflow. load_shots (index.py:311-315, used by match/build/review/verify) is equally unguarded. The abrupt-termination writer is real too: web.py:218-221 runs the pipeline in a daemon thread killed at interpreter exit, and disk-full (ENOSPC mid-write leaves a partial file that still exists()) is a second corruption path. However, the claim overstates two things: (1) the portal never resumes — web.py:203-204 creates a fresh uuid project dir per job, so a killed portal job's corrupt shots.json is abandoned, never re-read; the bricking only manifests on CLI/programmatic reuse of the same project dir; (2) the vulnerable window is milliseconds (one buffered write per source) versus minutes-to-hours of indexing, so corruption is uncommon; and manual mode has --force-index as recovery (auto mode does not). Real bug, but 'bricks every later run' overstates reachability; medium, not high/critical.
- **Fix:** Two small changes in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/index.py. (1) Make the write atomic at line 295: write to a sibling temp file then rename — `tmp = shots_file.with_suffix(".json.tmp"); tmp.write_text(json.dumps([s.to_dict() for s in shots], indent=1), encoding="utf-8"); os.replace(tmp, shots_file)` (os.replace is atomic on the same filesystem; optionally fsync before replacing). (2) Treat a corrupt cache as a cache miss instead of crashing: wrap the parse at lines 211-214 in `try: data = json.loads(...) ... except (json.JSONDecodeError, OSError): log(f"index: {source.id} cache corrupt — re-indexing"); shots_file.unlink(missing_ok=True)` and fall through to re-index. Apply the same try/except (returning []) to load_shots at lines 311-315 since match.py, build.py, review.py, and verify.py all parse the same file unguarded. Consider the same temp+replace pattern for the np.save at line 294 and ClipProject.save (models.py:296) as a follow-up.

### project.json saved non-atomically after every stage — interrupted save corrupts the entire project manifest
- **Where:** `vidlore/clipstudio/models.py:296`  ·  **Lens:** concurrency-resources  ·  **Verifier confidence:** high
- **Problem:** ClipProject.save() rewrites the whole manifest with a single write_text. project.json is the bus for all pipeline state (sources, segments, selections, meta) and is rewritten after every stage by both pipelines and by verify/cut. A crash, OOM-kill, or daemon-thread teardown (portal shutdown) mid-write truncates it; ClipProject.load (models.py:316 `json.loads(manifest.read_text(...))`) then raises on the next run, and because orchestrate only checks `(project_dir / "project.json").exists()` (orchestrate.py:132) before calling load, the project can never be resumed or even restarted in place — the advertised 'saving the project after each stage so a run is resumable' guarantee is broken by the very mechanism that implements it.
- **Verified because:** Every element of the claim checks out against the actual code. (1) The code says exactly what was claimed: /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/models.py:294-297 is `def save(self): self.root_path.mkdir(parents=True, exist_ok=True); self.manifest_path.write_text(json.dumps(self.to_dict(), indent=2), encoding="utf-8")` — a single non-atomic truncate-and-write of project.json. A grep for `os.replace|os.rename|tempfile|atomic` across vidlore/clipstudio returns nothing, and a grep for `JSONDecodeError` returns nothing — there is no atomic-write helper and no decode-error handling anywhere in the package. (2) The failure mode is reachable and unguarded. save() is called repeatedly during long runs: orchestrate.py:70, 84, 149, 157, 201, plus ingest.py:266, download.py:190, cut.py:68, verify.py:160. Load is gated only on existence at orchestrate.py:35-36 and 132-133 (`if (project_dir / "project.json").exists(): proj = ClipProject.load(project_dir)`), and load() at models.py:316 does an unguarded `json.loads(manifest.read_text(...))`, so a truncated manifest raises json.JSONDecodeError on every subsequent run with no recovery path. The daemon-thread vector is also real: clipstudio/web.py:218-222 starts `_run_job` (which runs produce_auto) with `daemon=True`; daemon threads are killed abruptly when the Flask process exits, so server shutdown mid-write truncates the file — as would a crash, OOM-kill, or Ctrl-C. The orchestrate.py module docstring (lines 3-4) explicitly advertises "saving the project ... after each stage so a run is resumable", and this mechanism is what breaks resumability. (3) Severity is slightly overstated in one phrase: "can never be resumed or even restarted in place" — a user can manually delete the corrupt project.json and restart (downloaded sources and index artifacts remain on disk under sources/ and index/, keyed by content-derived ids), so it is not total data loss; and the corruption window per write is small relative to the run, making this probabilistic rather than guaranteed. It is a genuine durability defect, not a critical one.
- **Fix:** Make ClipProject.save() atomic: serialize to a temp file in the same directory and rename over the target, e.g. in models.py replace the write_text call with `tmp = self.manifest_path.with_suffix(".json.tmp"); tmp.write_text(json.dumps(self.to_dict(), indent=2), encoding="utf-8"); os.replace(tmp, self.manifest_path)` (optionally open/flush/fsync the temp file before replace for crash-on-power-loss durability; os.replace is atomic on POSIX and Windows). Additionally, harden ClipProject.load (models.py:312-319) to catch json.JSONDecodeError, rename the corrupt manifest to project.json.corrupt-<timestamp> and raise a clear, actionable error (or return a fresh project) instead of an unhandled traceback — and consider having clipstudio/web.py join its worker thread or mark it non-daemon so server shutdown does not kill a write in flight.

### _JOBS registry never evicted and jobs/outputs orphaned on restart; per-job project dirs grow disk unboundedly
- **Where:** `vidlore/clipstudio/web.py:36`  ·  **Lens:** concurrency-resources  ·  **Verifier confidence:** high
- **Problem:** Jobs are registered in the in-memory _JOBS dict (web.py:214-216) and never removed: a long-running portal leaks one dict (with up to 400 retained log lines, web.py:49) per job forever. Job state is process-memory only, so a server restart orphans everything — /job/<jid> 404s (web.py:228) and /download/<jid> becomes unreachable (web.py:246 requires j['output'] from _JOBS) even though the finished MP4 still exists under _ROOT. Meanwhile nothing ever cleans per-job project directories (each holding the downloaded HD sources, index keyframes, clips, and output — typically multiple GB), so ~/Desktop/clipstudio_output/portal fills the disk silently. The polling JS compounds the restart case: `catch(e){}` (web.py:166) swallows the 404 and the page polls forever showing 'working…'.
- **Verified because:** Every cited line is accurate in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/web.py. (1) _JOBS (line 36) is only ever written at line 215 (`_JOBS[jid] = {...}`) and read at lines 46, 62, 69, 228, 236, 245 — grep over the whole repo finds no del/pop/TTL/eviction, so a long-running process accumulates one job dict (with up to 400 log lines, line 49) per job forever. (2) State is process-memory only with no persistence or startup rehydration, so after a restart /job/<jid> aborts 404 (line 228), /status returns 404 (lines 236-238), and /download aborts 404 (line 246: `if not j or not j.get("output") or not Path(j["output"]).exists(): abort(404)`) even though the finished MP4 still sits under _ROOT/<jid> on disk — inaccessible via the portal, recoverable only by browsing the folder manually. (3) `proj = _ROOT / jid; proj.mkdir(...)` (lines 204-205) has no corresponding cleanup anywhere in the clipstudio package (only a temp-file unlink in build.py:156 and a single rejected-source unlink in download.py:171); download.py:51 writes HD source videos into proj.sources_dir and orchestrate.py:177 writes keyframe references into proj.index_dir, so multi-GB-per-job growth under ~/Desktop/clipstudio_output/portal is realistic and unbounded. (4) The poll loop behavior is as claimed in effect, with one mechanical nuance: fetch() does not throw on HTTP 404 — /status returns valid JSON {"error":"unknown job"}, r.json() parses it, j.done/j.phase are undefined, and the unchecked response status (not the catch at line 166) makes the page show 'running…' forever while re-polling every 2.5s (line 167); the `catch(e){}` swallows the connection-refused errors during the restart window itself. Severity is overstated if read as high: this is a localhost-default (127.0.0.1:5151) single-user tool with no security impact, the memory leak is slow, and finished MP4s are not lost (just unreachable via the UI). The unbounded disk growth is the substantive harm — medium.
- **Fix:** Three small changes in vidlore/clipstudio/web.py: (a) persist job state — at job completion in _run_job, write a status.json (done/ok/output/summary) into the project dir, and make /job, /status, and /download fall back to reading _ROOT/<jid>/status.json (or globbing the output MP4) when jid is missing from _JOBS, so restarts no longer orphan finished videos; (b) add eviction — after a job finishes, schedule removal of its _JOBS entry after a grace period (e.g. 1h) or cap the registry (keep last N done jobs, oldest-first pop), and on startup or job completion run a retention sweep that deletes intermediates (sources_dir, index_dir, clips) once the final MP4 exists, optionally pruning whole project dirs older than a configurable retention window (env var, default e.g. 7 days); (c) fix the poll JS — check `if(!r.ok){ show 'job not found (server restarted?)'; done=true; return; }` before r.json(), so a 404 stops polling and tells the user instead of showing 'running…' forever.

### No sid de-duplication before concurrent submission — duplicate specs/candidates download to the same file simultaneously
- **Where:** `vidlore/clipstudio/download.py:162`  ·  **Lens:** concurrency-resources  ·  **Verifier confidence:** high
- **Problem:** download_candidates and ingest_sources submit every entry to the ThreadPool without de-duplicating by the deterministic sid first. A duplicate URL (same URL twice in a user's sources.json with the same title, or duplicate candidates from a library caller — discover dedupes internally, but download_candidates is a public API) yields the same sid, so two workers run yt-dlp concurrently against the same output stem: both write/resume the same `.part` file (interleaved writes → corrupt media), and the merge step at download.py:184-186 then inserts the surviving SourceVideo twice into proj.sources because `order` contains the sid twice (`[by_id[i] for i in order if i in by_id]` repeats the lookup), duplicating it through indexing and the ledger.
- **Verified because:** Every cited line checks out. (1) download.py:160-164 submits each allowed entry with no seen-sid guard (`for (c, sid, perm, note) in allowed: futs[ex.submit(_download_one, c, sid, perm, note, proj, cfg)] = sid`), and ingest.py:251-255 has the identical pattern. (2) _cand_id (download.py:40-42) is `_slug(title)_sha1(url)[:8]` and _source_id (ingest.py:48-51) is the same over spec.ref, so the same URL with the same/empty title yields the same sid, hence the same `dest_stem = proj.sources_dir / sid` (download.py:51, ingest.py:141). With download_concurrency defaulting to 2 (config.py:98) and only a 0.5s submit stagger, two yt-dlp instances with `continuedl: True` and identical `outtmpl` run concurrently against the same `.part` file — no lock exists anywhere in the package. (3) The merge at download.py:184-189 (`order = [_cand_id(c) for c in queue]; new_in_order = [by_id[i] for i in order if i in by_id]`) re-emits the same SourceVideo object once per duplicate occurrence; ingest.py:263-265 does the same. Additionally, the checksum dedup at download.py:168-176 makes it worse: the second completion unlinks sv.local_path, which is the SAME file the first OK record points to, deleting the only downloaded copy while by_id keeps the later duplicate-status record. (4) Reachability: the auto pipeline is guarded — discover_sources dedupes by (provider, id-or-url) and normalized title (discover.py:322-332) — but the manual path is not: cli.py:64-68 concatenates load_specs(sources.json) and --add refs with no dedup, load_specs (ingest.py:270-290) has none, and orchestrate.produce (line 46) passes specs straight to ingest_sources. A user's sources.json containing the same URL twice reproduces the race directly; download_candidates is also a documented public API whose only internal protection comes from a different module's caller. The claim is accurate, including its acknowledgment that discover dedupes internally.
- **Fix:** Dedupe by sid before submission in both functions. In download_candidates (vidlore/clipstudio/download.py), add a `seen_sids: set[str]` when building `allowed` (lines 146-154): skip (or record as status "duplicate") any candidate whose `_cand_id(c)` is already seen, and dedupe the merge order with `order = list(dict.fromkeys(_cand_id(c) for c in queue))` at line 185 so the same SourceVideo is never inserted twice into proj.sources. In ingest_sources (vidlore/clipstudio/ingest.py), do the same: filter `specs` through a seen set keyed on `_source_id(sp)` before splitting into locals/urls (lines 236-238), and dedupe `order` at line 264 with `dict.fromkeys`. Optionally also dedupe in load_specs / cli.py for friendlier UX, but the sid-level guard in the two batch functions is the authoritative fix since they are public APIs. Also reconsider the checksum-dedup unlink at download.py:171 — it should not unlink when the path equals another kept source's local_path.

### No CSRF protection on state-changing POST /create — a visited webpage can trigger downloads with the owner's cookies
- **Where:** `vidlore/clipstudio/web.py:189`  ·  **Lens:** security  ·  **Verifier confidence:** high
- **Problem:** POST /create is a state-changing, side-effectful endpoint (spawns a job that runs yt-dlp with the owner's Chrome cookies and spends LLM quota) with no CSRF token, no Origin/Referer check, and no SameSite/auth gate. The form is a plain multipart form (web.py:111). Even when the server is bound to 127.0.0.1 (the default), any web page the user opens in their browser can auto-submit a cross-site form POST to http://127.0.0.1:5151/create (multipart/form-data form posts are not blocked by the same-origin policy), causing the local server to download YouTube content via the user's logged-in session and consume API credits without the user's knowledge.
- **Verified because:** The code facts all check out. web.py:189 `@app.route("/create", methods=["POST"])` and the handler reads `request.form.get("topic"/"script"/...)` directly (lines 191-199) and `request.files.get("voiceover")` (line 208) with zero token validation. The form at web.py:111 `<form class=card method=post action="/create" enctype="multipart/form-data" id=f>` carries no CSRF token field. A grep of the whole clipstudio package found no CSRFProtect/flask_wtf/before_request/Origin/Referer/SECRET_KEY/session usage anywhere — there is no auth or anti-CSRF guard at any layer. The side effect is genuinely reachable: create() spawns _run_job → produce_auto (orchestrate.py:107), which calls download_candidates (orchestrate.py:165) using yt-dlp with `--cookies-from-browser chrome` (hd_download.py:137,213; COOKIES_BROWSER default "chrome" at hd_download.py:54) and runs LLM analysis (analyze_script, orchestrate.py:145). The default bind is 127.0.0.1 (web.py:254). The CSRF mechanics are correct: multipart/form-data is a CORS "simple" content type, so a cross-site auto-submitting HTML form POST to http://127.0.0.1:5151/create is NOT blocked by same-origin policy and triggers no preflight; the required `topic`/`script` fields can be set to arbitrary attacker values, so the required-field check (line 193) is not a barrier. One framing nuance worth flagging: this app has NO authentication or session cookies, so the attack is not browser cookie-replay of an "owner's logged-in session" as literally worded — the ambient authority abused is the local server's own server-side access to Chrome cookies (consumed by yt-dlp) and API keys. The net impact described — a visited webpage silently making the local server download YouTube content and burn LLM/API quota — is accurate, though the attack is blind (the attacker cannot read the response).
- **Fix:** Add an Origin/Referer allow-list gate for the state-changing route. Register a `@app.before_request` that, for non-safe methods (POST), rejects (abort 403) any request whose `Origin`/`Referer` host is not the server's own host:port (i.e. require Origin to be one of http://127.0.0.1:5151 / http://localhost:5151, and reject when Origin is present but mismatched). Because the threat is cross-origin form submission, a simple same-origin Origin check fully neutralizes it with no token plumbing. For defense in depth, additionally bind the form to a per-session CSRF token: set a SECRET_KEY, use Flask-WTF's CSRFProtect (or a hidden token compared against a signed cookie), and add a hidden `<input type=hidden name=csrf_token value=...>` field to _FORM (web.py:111). Either fix alone closes the hole; the Origin check is the minimal change. The severity is medium rather than high/critical because the server is loopback-bound by default (an attacker must guess the fixed port 5151 and the victim must have the portal running), the attack is blind/no-data-exfiltration, and the worst outcome is unwanted resource/quota consumption and downloads rather than data theft or RCE.

### Unauthenticated /status endpoint discloses raw exception tracebacks and absolute filesystem paths
- **Where:** `vidlore/clipstudio/web.py:74`  ·  **Lens:** security  ·  **Verifier confidence:** high
- **Problem:** On job failure the worker appends the full Python traceback (last 1200 chars) and the exception string to the job's log list (web.py:73-74). GET /status/<jid> returns that log verbatim as JSON to any unauthenticated caller (web.py:239 returns the "log" key). Tracebacks from the download/index/LLM stack leak absolute server paths (e.g. .hdvenv, .clipstudio_libs, GCP credential resolution paths from llm._gcp_cred, project directories under the home folder) and internal stack structure to anyone who can reach the port. Combined with the lack of auth, this is an information-disclosure primitive useful for further attack and reveals the operator's directory layout and credential file locations.
- **Verified because:** The code matches the claim exactly. In vidlore/clipstudio/web.py the failure handler at lines 72-74 does `j["error"] = str(e)[:600]; j["log"].append("FATAL: " + str(e)[:300]); j["log"].append(traceback.format_exc()[-1200:])`. The /status/<jid> route at line 239 returns `jsonify({k: j[k] for k in ("phase", "log", "done", "ok", "error", "summary")})` — i.e., the full log (with the traceback) AND the error string, verbatim, as JSON. The route is unauthenticated: the entire 260-line module defines only `/`, `/create`, `/job/<jid>`, `/status/<jid>`, `/download/<jid>` and has no `@app.before_request`, no auth decorator, and no token/session check anywhere. The failure path is reachable: produce_auto() is wrapped in try/except (line 67) and any exception in the download/index/LLM stack lands in this handler. Python's `traceback.format_exc()` necessarily embeds absolute source-file paths, and the codebase confirms the specific sensitive locations cited: `.clipstudio_libs` is appended to sys.path (llm.py:25-27) and `_gcp_cred()` resolves credential paths such as `~/Desktop/vidrush-clone/.secrets/gcp_vidlore.json` (llm.py:37-47), with `_gemini_client()` doing `json.load(open(cred))` and raising RuntimeError on a path that would appear in the trace (llm.py:62-74). So the directory-layout and credential-file-location disclosure is genuine. One correction to the claim's framing: by default the server binds to 127.0.0.1 (web.py:254) and the shipped launcher ClipStudio-Portal.command does not set VIDLORE_CLIPSTUDIO_HOST, so out of the box exposure is localhost-only — the host is operator-configurable to 0.0.0.0, which is when 'anyone who can reach the port' literally applies. The disclosure leaks paths and stack structure, not credential contents, so it is not critical; medium is the honest severity.
- **Fix:** Stop returning raw exception detail to clients. In the /status handler (web.py:239) drop the `error` and `log` traceback content from the client-facing payload, or sanitize it: keep `phase`, `done`, `ok`, `summary` and a generic `error` like "job failed — see server logs". In _run_job (web.py:72-74), write the full `traceback.format_exc()` to a server-side logger (logging.exception / a job logfile) instead of into `j["log"]`, and store only a short, non-path-bearing user-facing message on the job (e.g. a category/error code). If detailed per-job diagnostics must remain retrievable, gate them behind authentication (a per-job secret token issued at /create and required on /status, plus an operator auth check), and keep the public bind on 127.0.0.1. This removes the absolute-path/credential-location and stack-structure disclosure while preserving the progress UI.

### Anchor-source detection uses substring containment, granting the 0.45 bonus and relax exemptions to wrong sources
- **Where:** `vidlore/clipstudio/match.py:304`  ·  **Lens:** matching-quality  ·  **Verifier confidence:** high
- **Problem:** A source is classified as an anchor source if ≥2 anchor-scene tokens appear in its title via raw substring tests ('w in tl'), with tokens as short as 3 chars. 'ice' matches 'service', 'red' matches 'hundred', 'arya' matches 'caryatid', 'inn' matches 'inning/winner'. A falsely matched source gets the anchor-continuity bonus added into base/confidence for every beat (match.py:251-252) AND, in single-scene mode, full relax-mode exemptions from reuse/source/recency penalties plus a tripled per-shot cap (match.py:355-359) — so an unrelated source can dominate the whole cut while reporting inflated confidence.
- **Verified because:** The quoted code is verbatim accurate. In /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/match.py lines 302-305: `tl = (src.title or "").lower()` is a plain string, and `sum(1 for w in ts if w in tl) >= 2` uses Python substring containment, not word-boundary matching. Tokens are filtered only by `len(w) > 2 and w not in mtoks and w not in _STOP` (lines 298-299), and _STOP (segment.py:16-21) contains only function words — content words like 'red', 'ice', 'inn', 'war' pass. So 'red' in 'hundred', 'ice' in 'service', 'inn' in 'winner'/'dinner'/'beginning' are all mechanically true matches. The failure is reachable: analyze.py guarantees non-empty anchor_scenes (LLM output with a heuristic `_derive_anchors` fallback at analyze.py:295-304), so the anchor-detection block runs on every analysis-driven run; anchor_bonus defaults to 0.45 in single-scene mode (match.py:306, env-overridable). Both claimed impact paths are confirmed: (a) match.py:251-252 adds the bonus into `base`, which feeds both candidate ranking and the reported confidence (`confidence=round(min(1.0, base_q),4)` at line 427 and candidate score at line 407) — so a falsely-anchored source's clips get inflated confidence, which drives the review surface coloring (review.py:36,69) and mean-confidence stats, defeating low-confidence flagging; (b) match.py:355-364, 382, 403 grant relax exemptions in single-scene mode keyed solely on `ps.sid in anchor_sids`: tripled per-shot cap, reuse penalty x0.25, skipped consecutive-source / dup / source-recency penalties, halved same-shot recency. No guard elsewhere validates anchor membership — the _load_pool watermark/static gates are orthogonal, and discover.py uses the same substring pattern itself (_relevance lines 211/242, anchor-coverage line 416), so upstream does not correct it. Realistic trigger needs only 1 genuine + 1 spurious hit within one scene's token set (e.g. anchor tokens {arya, hound, inn}: a generic 'The Hound — Best Moments Winner' title hits 'hound' genuinely and 'inn' inside 'Winner'). Two mild overstatements in the claim: a falsely-anchored source competes on EQUAL footing with genuine anchor sources (which also get +0.45), so 'dominate the whole cut' holds only against correctly-non-anchored footage — though with w_clip=0.80 (config.py:45) a +0.45 bonus does let mediocre wrong-scene shots outrank near-perfect non-anchor shots; and verify.py's LLM keep/replace pass plus the human review surface provide partial downstream mitigation. Those mitigations cap severity at medium rather than refuting the bug.
- **Fix:** Tokenize the source title into a word set and require whole-word overlap, mirroring how the rest of the pipeline builds token sets. In match.py replace lines 302-305 with: `for src in proj.sources: ttoks = {w for w in re.findall(r"\w+", (src.title or "").lower()) if len(w) > 2}; if any(len(ts & ttoks) >= 2 for ts in atok_sets): anchor_sids.add(src.id)`. Optionally raise the minimum token length to 4 for anchor tokens (3-char words like 'red'/'ice'/'inn' are weak scene discriminators even as whole words) or require one hit to be a token of length >= 5. Apply the same word-set fix to the identical substring patterns in discover.py: `_relevance` (lines 199, 201, 211, 217, 226, 242) and the anchor-coverage block (line 416), since they grant the equivalent +0.45 relevance boost and force-inclusion on the same flawed test.

### Dark-scene guard triggers on movie-title words anywhere in any beat's scene_query, penalizing correct daytime footage project-wide
- **Where:** `vidlore/clipstudio/match.py:309`  ·  **Lens:** matching-quality  ·  **Verifier confidence:** high
- **Problem:** dark_scene is computed once for the whole project by regex-searching _DARK_SCENE (\b(night|dark|cell|chamber|inn|interior|...)\b) over the anchor description concatenated with EVERY segment's scene_query. The analyze prompt explicitly instructs scene_query to start with 'movie + character + action' (analyze.py:163-165), so any title or character containing these words — 'The Dark Knight', 'Night King', 'Night's Watch', an 'inn' or 'chamber' location — flips the global flag in single_scene mode. Every bright shot in the pool then takes a selection penalty up to 0.6 (match.py:367-368) on every beat, steering the entire cut away from correct daytime footage even when the actual anchor scene is bright (e.g. a daylight battle in a video that mentions the Night King once).
- **Verified because:** Confirmed against the real code. match.py:309-310 computes a single project-wide boolean: dark_scene = single_scene and bool(_DARK_SCENE.search(anchor_desc + " " + " ".join(scene_query for every segment))). _DARK_SCENE (match.py:57-58) is \b(tavern|night|candlelit|candle|dark|dungeon|cell|torch|firelit|interior|indoor|brothel|crypt|throne room|chamber|inn|cellar|nighttime)\b with re.I. I ran the regex: 'The Dark Knight ...' matches ('Dark'), 'Night King', "Night's Watch", and 'Inn at the Crossroads' all match. analyze.py:163-165 explicitly instructs each beat's scene_query to be 'movie + character + the specific action/location' (its example begins with the show title), and analyze.py:287 stores it on every beat, so a dark word in the title or in any single beat's query flips the global flag. The penalty (match.py:365-368) then applies pen += min(0.6, (b-0.44)*2.4) to every candidate brighter than luma 0.44 on every beat, with no relax exemption for anchor sources. Magnitude is significant: w_clip=0.80, w_dialogue=0.55, single_scene anchor bonus=0.45 (config.py), so up to 0.6 can flip selection toward dark or wrong-scene shots; it only affects adj (which clip is picked), not confidence, so the steering is silent. No guard exists: no env override, no title-token stripping (the code strips movie-title tokens mtoks at match.py:295 for anchor matching but not for this regex), and the docstring at match.py:55-56 says the check is meant for 'an anchor scene's description' — the implementation scanning all per-beat queries exceeds the documented intent. Reachable via the main pipeline (orchestrate.py:187 passes analysis; single_scene set by LLM or the heuristic at analyze.py:219). Severity is moderated to medium because it is a selection-quality bias, not a crash or hard exclusion (a clearly more relevant bright shot can still win), it fires only in single_scene mode, and the verify/repair stage downstream may fix some picks.
- **Fix:** Restrict the dark-scene detection to the anchor scene's own description, not every beat's scene_query: compute dark_scene from anchor_desc only, after stripping movie-title tokens (reuse the mtoks set already built at match.py:295, e.g. remove \b-bounded title words before running _DARK_SCENE). If broader coverage is desired, require corroboration rather than a single hit — e.g. count beats whose title-stripped scene_query contains a dark word and enable the guard only when a meaningful fraction (say >=25%) of beats agree. Best of all, ground the flag in actual footage: compute the median _shot_brightness of shots from anchor_sids and only enable the penalty when the textual signal AND the anchor footage's median luma (< ~0.35) both indicate a dark scene; this makes the guard self-correcting when the anchor scene is actually a bright daylight moment.

### _derive_anchors 'cluster' is a union of up to 8 independent tokens and overrides the LLM's explicit multi_scene verdict
- **Where:** `vidlore/clipstudio/analyze.py:214`  ·  **Lens:** matching-quality  ·  **Verifier confidence:** high
- **Problem:** The single-scene safety net computes top = up to 8 tokens that each appear in ≥25% of beats — with no co-occurrence requirement — then declares single_scene if ≥60% of beats touch ANY of those tokens. A character retrospective (the module's own example of multi_scene, analyze.py:33) has the character name in most beats' scene_queries/keywords, so the union coverage trivially exceeds 0.6 and the video is misclassified single_scene. Two unrelated recurring threads ('jon' in half the beats, 'daenerys' in the other half) also union to full coverage. Worse, when the LLM returned video_type='multi_scene' but omitted anchor_scenes (explicitly called common 'under load', analyze.py:295-297), line 301 overwrites the LLM's verdict with the heuristic one. A wrong single_scene classification then activates the 0.45 anchor bonus, relax-mode reuse exemptions and setting-bias query pollution in match.py plus the cinematic letterbox in build.py:633.
- **Verified because:** Every quoted line matches the real code and the failure mode is reachable. (1) analyze.py:214 builds `top = [w for w, c in freq.most_common(8) if c >= max(2, nbeats * 0.25)]` — up to 8 tokens that each independently appear in >=25% of beats, with no co-occurrence requirement — and analyze.py:218-219 computes `covered = sum(1 for ts in per_beat if ts and (set(top) & ts)) / nbeats; vtype = "single_scene" if covered >= 0.6 else "multi_scene"`. Coverage is intersection with the UNION of top tokens, so two disjoint recurring threads (e.g. 'jon' in half the beats, 'daenerys' in the other half) each pass the 25% bar and union to ~100% coverage -> single_scene. (2) The character-retrospective case is essentially guaranteed by the pipeline's own prompt: analyze.py:163-165 instructs every beat's scene_query to be "movie + character + the specific action/location". Movie-title tokens are stripped (mvtoks, lines 199/207) and franchise words are in _ANCHOR_STOP (line 186-189), but character names are not stripped — so a retrospective (the module's own multi_scene example, line 33) has the character name in nearly every beat, making covered ~1.0 -> single_scene. (3) The override is real and asymmetric: line 266-267 coerces the LLM verdict to exactly "single_scene" or "multi_scene" (never empty), so in `if not analysis.video_type or analysis.video_type == "multi_scene": analysis.video_type = vt` (lines 301-302) the first clause is dead and the only live effect is replacing the LLM's explicit "multi_scene" with the heuristic's "single_scene". Reachability is documented by the code itself: the comment at lines 295-297 says the LLM "returns anchor_scenes inconsistently under load", which is exactly the gate (line 298) for the override; and the no-LLM path (lines 245-246) always uses _derive_anchors. No guard elsewhere corrects the misclassification. (4) All claimed downstream effects confirmed: match.py:306 anchor_bonus = 0.45 when single_scene (vs 0.12); match.py:334-335 appends anchor_desc to EVERY beat's CLIP query; match.py:355-359 relaxes reuse (shot_cap x3, reuse penalty x0.25) for anchor sources; build.py:631-633 turns on the cinematic letterbox by default when video_type == "single_scene". Compounding it, the derived anchor query (analyze.py:220-222) is a bag of the 6 most frequent unrelated tokens, and match.py:304 marks any source whose title contains >=2 of those tokens as an anchor source — so unrelated sources can receive the 0.45 bonus and reuse exemptions.
- **Fix:** Three coordinated changes in vidlore/clipstudio/analyze.py. First, make _derive_anchors measure an actual cluster instead of a union: seed with the single most frequent token, define the cluster as tokens that co-occur with the seed in a majority of the seed's beats, and compute coverage only over beats containing the seed (or >=2 cluster tokens) — disjoint threads then no longer sum to high coverage. Second, strip known entity names from the token pool the same way movie-title tokens are stripped: pass analysis.characters/actors (LLM path) or beat entities (heuristic path) into _derive_anchors and exclude them alongside mvtoks, so a character retrospective is not collapsed onto the character's name. Third, never let the heuristic overrule an explicit LLM verdict: keep a flag for whether the LLM actually returned a parseable video_type (before the coercion at line 266), and at line 301 only assign the heuristic vtype when that flag is false — when the LLM said multi_scene but omitted anchor_scenes, fill anchor_scenes from the heuristic but keep video_type = multi_scene (or require a much stricter co-occurring coverage threshold, e.g. >=0.8, to flip it).

### Energy-floor scenes still desync from assemble — capped clip count below assemble's beat count triggers the first-clip replay
- **Where:** `vidlore/clipstudio/build.py:506`  ·  **Lens:** matching-quality+integration  ·  **Verifier confidence:** high
- **Problem:** The visual-budget loop lowers a scene's energy only while _energies_eff[_pos] > 1. plan_beats' per-scene beat count is primarily duration-driven (k ≈ d/target, assemble.py:909+), so a long scene with 1-2 distinct looks can still receive k > _D beats even at energy 1. The loop then exits with _nb[_pos] > _D, and line 519 caps build's clip count (nbeats = min(_nb, _D)) BELOW the k that assemble — given the same _energies_eff — will compute. Build pads clips_for_scene only to its own capped k (build.py:576), so assemble's excess beats fall past the supplied beat_clips into 'item = base' (assemble.py:8819 'if m < len(clips):' else assemble.py:8832 'item = base'), replaying the scene's first clip from frame 0 — exactly the desync the comments at build.py:470-479 and 521-525 claim is prevented.
- **Verified because:** Confirmed against the real code. (1) The energy-lowering loop at /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/build.py:506-507 reads "if _nb[_pos] > _D[_seg.index] and _energies_eff[_pos] > 1: _energies_eff[_pos] = max(1, _energies_eff[_pos] - 1)" — once a scene's energy hits 1, an over-budget scene no longer sets _over, so the loop exits with _nb[_pos] > _D. Line 519 then caps build's clip count: "nbeats = [min(_nb[_pos], _D[segments[_pos].index]) ...]", and lines 529/576/581 cut and pad clips_for_scene only to that capped k (beat_clips[seg.index] = clips_for_scene[:k]). (2) Reachability verified two ways. Both sides plan with identical inputs: build.py:496 calls plan_beats(_durs, target=3.4, bmin=2.4, energies=_energies_eff, roles=_rol); assemble.py:8718-8724 calls plan_beats(durs, target=getattr(_sty,'beat_target',3.4), bmin=getattr(_sty,'beat_min',2.4), energies=[_en(i)...]) where _sty is STANDARD (beat_target=3.4, beat_min=2.4 in vidlore/style_modes.py:26-27, build never passes style=), durs comes from the same narration object, and build passes energies=_energies_eff at build.py:656 — so assemble computes the same uncapped _nb. I ran the repo's plan_beats directly: with all energies forced to the floor of 1, scenes of 8/18/9/14s still get 3/6/3/5 beats (k = round(d/eff_t) at assemble.py:1161 is duration-driven, with a hard floor k >= ceil(d/12) for d>12 at assemble.py:1166-1167). Meanwhile _D (build.py:473-481 _scene_distinct) is frequently 1-2 — it returns max(1, clusters) and returns 1 for any scene whose embeddings fail to load (_emb_b returns None on exception) or whose beat_windows all look alike. (3) No guard exists downstream: assemble.py:8817-8832 does "clips = beat_clips.get(ns.index) or []; if m < len(clips): ... else: item = base", and base = by_idx[ns.index] is the FootageItem built from clips_for_scene[0] at build.py:580 — every excess beat re-encodes the scene's first clip from its start. This is precisely the first-clip replay the comments at build.py:466-479 and 520-525 claim is prevented. Severity is medium, not higher: A/V sync, narration, and captions are unaffected (beat durations still sum exactly to scene durations); the impact is a visible quality defect — repeated identical footage within affected scenes — which is the artifact this subsystem exists to eliminate.
- **Fix:** Stop capping build's clip count below assemble's beat count. After the energy-lowering loop in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/build.py, replace line 519's "nbeats = [min(_nb[_pos], _D[...])]" with "nbeats = list(_nb)" (the loop has already minimized _nb as far as energy allows; _D should only drive the lowering loop, not a hard cap). Then for each scene build will attempt k = _nb[pos] clips: beats beyond the distinct windows fall through to the existing Option-B strided fill (build.py:551-555), which cuts the chosen source at different in-points, and any remaining shortfall hits the deliberate last-clip padding at build.py:576-577 — so len(beat_clips[idx]) always equals assemble's k and no beat ever falls into the "item = base" first-clip replay at assemble.py:8832. Optionally, when _nb[pos] > _D at energy 1, log it (extending the existing line 523-525 message) so over-budget floor-energy scenes are observable. A belt-and-braces alternative inside assemble.py:8831 would be "item = FootageItem(ns.index, clips[m % len(clips)], ...)" when clips is non-empty (cycle the supplied distinct clips instead of replaying clip 0), but the build-side fix is the correct primary repair since padding is documented there as "the ONLY place a repeat can re-enter".

### Recency history is never pruned — O(segments^2 × pool) candidate scoring with per-pair numpy dots
- **Where:** `vidlore/clipstudio/match.py:378`  ·  **Lens:** matching-quality  ·  **Verifier confidence:** high
- **Problem:** Every selected beat appends to `recent` (match.py:449) and nothing is ever evicted; the candidate loop iterates the FULL history for every pool shot of every segment (entries older than recency_cooldown are skipped only after being visited, and the source_recency check at line 382 runs before the cooldown skip). For each non-phash-matching pair it can execute a numpy dot (line 399). For a long script (150+ beats) over a few thousand pool shots this is tens of millions of iterations — a real, superlinear slowdown in the match stage with zero behavioral benefit beyond the last recency_cooldown=48 entries.
- **Verified because:** Confirmed against match.py. (1) `recent.append({...})` at lines 449-451 is the only mutation of `recent` — no eviction anywhere (verified by grep: no pop/del/slice/deque). (2) The inner loop `for h in recent:` (line 378) sits inside the per-candidate loop `for base, sig, ps in scored:` (line 348) where `scored` is essentially the full pool, and the stale-entry skip `if gap >= cfg.recency_cooldown: continue` (line 384) runs AFTER the gap computation (379-381) and the source-recency check (382-383), so every stale history entry is visited for every pool shot of every segment. Since source_recency_window=7 < recency_cooldown=48 (config.py:81,85), entries with gap >= 48 contribute nothing — pruning is exactly behavior-preserving because h["pos"] strictly increases per append. (3) Reachable: segment_script (segment.py:99) imposes no beat cap (beats scale with script length; long-form is a supported path per orchestrate.py:187 and test_longform_validation.yaml), and _load_pool has no shot cap, so 150+ beats x thousands of pool shots gives ~tens of millions of wasted inner iterations, growing quadratically with script length. One correction to the claim's headline: the np.dot at line 399 only executes for entries within the cooldown window (gap < 48, after the line-384 skip), so the dot-product cost is O(segments x pool x 48) and is NOT removed by pruning — the unbounded O(segments^2 x pool) portion consists of cheap dict-lookup/compare iterations. The claim's evidence text states this correctly; only the title conflates them. Performance-only (no correctness impact, no crash), so medium rather than high severity: several extra seconds at 150 beats, tens of seconds at 300+, in a batch pipeline whose other stages (download/ASR/CLIP/ffmpeg) take minutes.
- **Fix:** Two-line fix in match.py: after the append at line 449-451, prune the history to the active window, e.g. `keep = max(cfg.recency_cooldown, cfg.source_recency_window); if len(recent) > keep: del recent[:-keep]` (or make `recent` a `collections.deque(maxlen=keep)` built before the segment loop). This is provably behavior-preserving since h["pos"] is strictly increasing, so any entry more than `keep` positions back has gap > recency_cooldown and gap > source_recency_window and currently contributes zero penalty. Optionally, also hoist the window filter out of the pool loop — `recent` only changes between segments, so compute `active = [h for h in recent if 0 < seg.index - h["pos"] < keep]` once per segment before line 348 and iterate that inside the candidate loop, eliminating the per-pool-shot re-scan entirely. The same unbounded-append pattern applies to `recent_sources` (line 448, read only via tail slice at line 361) and can be capped the same way for tidiness, though its cost is just memory.

### HD download path is structurally impossible on Windows (POSIX-only venv path + hardcoded /tmp)
- **Where:** `vidlore/clipstudio/hd_download.py:45`  ·  **Lens:** config-platform  ·  **Verifier confidence:** high
- **Problem:** The project explicitly targets Windows (windows-diagnose.bat at the repo root invokes .venv\Scripts\python.exe), but the SABR/HD downloader can never activate there. HD_PY is resolved only from the POSIX venv layout '.hdvenv/bin/python'; on Windows a venv puts the interpreter at '.hdvenv\Scripts\python.exe', so HD_PY resolves to '' and available() (line 68) is False — every YouTube download and every discover.resolve_quality probe silently falls back to the legacy in-process yt-dlp that the module's own docstring says 'silently degrades to the one legacy muxed 360p stream'. Even a Windows user who sets VIDLORE_HD_PYTHON/VIDLORE_HD_NODE/VIDLORE_HD_DENO/VIDLORE_HD_POT_DIR correctly is still blocked: ensure_pot_server opens its log at the hardcoded POSIX path '/tmp' (line 87); on Windows that open() raises, is swallowed by the except at line 93, and the function returns False — so the PO-token server can never start and download_hd always returns None. Net effect: Windows users always get 360p footage with no error, only the absence of 'hd:' log lines.
- **Verified because:** Confirmed against the real code. (1) hd_download.py:45-46 resolves HD_PY only from POSIX venv layouts: _firstpath(str(_ROOT / ".hdvenv/bin/python"), str(_ROOT / ".hdvenv/bin/python3")) — there is no .hdvenv/Scripts/python.exe candidate, and _firstpath (lines 39-43) returns "" when nothing exists, so available() (line 68: bool(HD_ENABLED and HD_PY and NODE_BIN and DENO_BIN and POT_SERVER_DIR)) is False on any Windows venv. (2) Line 87 is exactly as quoted: log = open(os.path.join("/tmp", f"clipstudio_pot_{POT_PORT}.log"), "ab"); it sits inside the try whose except at line 93 swallows the exception and returns False, so even with VIDLORE_HD_PYTHON/NODE/DENO/POT_DIR set, ensure_pot_server fails on Windows (open in "ab" mode cannot create the missing \tmp directory) and download_hd (line 204) / probe_max_height (line 134) bail to the legacy path. There is no os.name/sys.platform guard anywhere in the module and no tempfile.gettempdir(). (3) The fallback is silent: download.py:57-61 and ingest.py:145-149 call download_hd with progress=None and fall through to in-process yt-dlp, which the module docstring (lines 4-6) and both callers' comments ("legacy path; YouTube ≤360p") document as capped at the muxed 360p stream; discover.py:281 likewise keeps the legacy estimate when probe_max_height returns 0. Windows targeting is genuine: windows-diagnose.bat (.venv\Scripts\python.exe), tools/run_windows_gpu_benchmark.bat, tools/enable_windows_directml.bat, C:/Windows/Fonts font candidates in themes.py and footage.py, and tts_setup.py promising identical macOS/Windows behavior. Two narrow overstatements in the claim that do not refute it: the /tmp open would succeed if a \tmp directory happens to exist on the current drive, and a user who manually starts the bgutil server on :4416 passes the _pot_alive() early-return (lines 82-83) before the /tmp open is reached. Severity is the only dishonest part of the framing: this never crashes — graceful 360p degradation is the module's explicitly stated design (docstring lines 22-24), and no Windows setup script provisions .hdvenv/node/deno/bgutil anyway, so it is a silent quality-degradation bug on a secondary platform, not a critical/high failure.
- **Fix:** Three small changes in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/hd_download.py. First, add the Windows venv layout to the HD_PY candidates: HD_PY = os.environ.get("VIDLORE_HD_PYTHON", "") or _firstpath(str(_ROOT / ".hdvenv/bin/python"), str(_ROOT / ".hdvenv/bin/python3"), str(_ROOT / ".hdvenv/Scripts/python.exe")). Second, replace the hardcoded "/tmp" on line 87 with the platform temp dir: import tempfile and use os.path.join(tempfile.gettempdir(), f"clipstudio_pot_{POT_PORT}.log"). Third, make the detached Popen portable: pass start_new_session=True only on POSIX and use creationflags=subprocess.CREATE_NEW_PROCESS_GROUP | subprocess.DETACHED_PROCESS on Windows (e.g. kwargs = {"start_new_session": True} if os.name != "nt" else {"creationflags": subprocess.CREATE_NEW_PROCESS_GROUP | subprocess.DETACHED_PROCESS}). Optionally, have the callers (download.py:_download_one, ingest.py:_ingest_url) pass a progress callback so the "hd: ... fallback" messages actually surface, eliminating the fully silent degradation, and extend windows-diagnose.bat / vidlore.diagnose to report hd_download.available() and the resolved HD_PY/NODE_BIN/DENO_BIN/POT_SERVER_DIR values.

### os.environ.setdefault cannot repair a broken GOOGLE_APPLICATION_CREDENTIALS — Gemini fails despite a valid resolved credentials file
- **Where:** `vidlore/clipstudio/llm.py:67`  ·  **Lens:** config-platform  ·  **Verifier confidence:** high
- **Problem:** _gcp_cred() deliberately skips a set-but-nonexistent GOOGLE_APPLICATION_CREDENTIALS path (the os.path.exists check at line 45) and resolves the clone-local .secrets/gcp_vidlore.json instead. But _gemini_client then only does os.environ.setdefault('GOOGLE_APPLICATION_CREDENTIALS', cred), which is a no-op when the env var is already set — so google-genai's Vertex auth chain still reads the original broken path and raises DefaultCredentialsError/FileNotFoundError on every call. Meanwhile gemini_available() returns True (it trusts _gcp_cred), so active_provider() can report 'gemini' in the web portal header and analyze logs while every actual Gemini call dies. The auth error contains none of _is_transient's keywords, so the provider aborts on attempt 1 with zero logging, and complete() returns '' — analyze then mislabels the failure as 'heuristic (no LLM key)'. A stale env var pointing at a deleted/moved JSON is exactly the scenario this resolver was written to handle, and it half-handles it.
- **Verified because:** Confirmed against real code. (1) llm.py:39-46 (_gcp_cred) skips a set-but-nonexistent GOOGLE_APPLICATION_CREDENTIALS via `if c and os.path.exists(c): return c` and returns the clone-local .secrets/gcp_vidlore.json (which exists at /Users/hussnain/Desktop/vidlore-clipstudio/.secrets/gcp_vidlore.json). (2) llm.py:66-67 does `os.environ.setdefault("GOOGLE_APPLICATION_CREDENTIALS", cred)` — a documented no-op when the var is already set — and llm.py:74 builds `genai.Client(vertexai=True, project=proj, location=...)` with NO credentials argument; proj is parsed from the resolved valid file so the RuntimeError guard at line 72 does not trip and the client constructs successfully. (3) The installed google-genai 1.47.0 (.clipstudio_libs/google/genai/_api_client.py:937-951) lazily calls google.auth.default(), and google-auth 2.50.0's first ADC checker _get_explicit_environ_credentials calls load_credentials_from_file on the env path, which raises DefaultCredentialsError("File {} was not found.") for a missing file — it does NOT fall through to other credential sources. So every generate_content call raises. (4) The error string/type ("file ... was not found" / "defaultcredentialserror") matches none of _is_transient's keywords (llm.py:151-152: 429, rate, overload, 529, timeout, timed out, connection, 503, 502, 500, unavailable, resourceexhausted), so _with_retry (llm.py:173-177) returns None on attempt 1 with zero logging, and complete() returns ''. (5) gemini_available() (llm.py:50-59) only checks _gcp_cred() + SDK import, so it returns True; has_llm() is True; active_provider() reports "gemini (..., Vertex)" (displayed in clipstudio/web.py:177). (6) In analyze.py, _llm_analyze gets '' from complete(), re.search finds no JSON, returns None (line 131-133), and analyze_script line 255 recurses with eng_cfg=None, landing on the heuristic branch that logs "analyze: heuristic (no LLM key)" (line 247) — the mislabel claimed. (7) No guard elsewhere: the only other code touching the env var is footage.py:4276, which also uses setdefault with the same skip-broken-path resolver (_imagen_creds_path, footage.py:4178-4181); nothing in the codebase ever overwrites the stale value. The claimed severity is somewhat overstated though: triggering requires a pre-broken environment (env var set to a deleted/moved path — currently unset on this machine), and the pipeline only fully degrades when Gemini is the serving provider (no ANTHROPIC_API_KEY or VIDLORE_CLIPSTUDIO_LLM_PROVIDER=gemini); in the default Claude-primary config with a working key, the impact is a silently broken fallback, not a dead pipeline.
- **Fix:** Minimal one-line fix in _gemini_client (llm.py:67): replace `os.environ.setdefault("GOOGLE_APPLICATION_CREDENTIALS", cred)` with `os.environ["GOOGLE_APPLICATION_CREDENTIALS"] = cred`. This assignment is always safe because _gcp_cred() returns the env var's own value whenever it points to an existing file (it is cands[0]), so the only time the value actually changes is when the env var was broken — exactly the repair intended. Guard the empty case: `if cred: os.environ["GOOGLE_APPLICATION_CREDENTIALS"] = cred`. A more robust variant that avoids mutating process-global env entirely: build explicit credentials and pass them to the client — `from google.oauth2 import service_account; creds = service_account.Credentials.from_service_account_file(cred, scopes=["https://www.googleapis.com/auth/cloud-platform"]); _CLIENT = genai.Client(vertexai=True, project=proj, location=_GEMINI_LOCATION, credentials=creds)` (google-genai's BaseApiClient accepts credentials, see _api_client.py:590). Apply the same fix to the parallel Imagen path at vidlore/footage.py:4276, which has the identical setdefault-after-skipping-broken-env pattern.

### Permission assertions are never validated against the PERMISSIONS enum — any non-'unverified' string ingests and lands in the compliance ledger
- **Where:** `vidlore/clipstudio/ingest.py:220`  ·  **Lens:** config-platform  ·  **Verifier confidence:** high
- **Problem:** models.py defines a controlled vocabulary PERMISSIONS = ('owner','licensed','public_domain','cc','fair_use_claim','unverified'), and the ingest error message tells the user to 'set owner|licensed|public_domain|cc|fair_use_claim'. But the gate only checks membership in PERMISSION_BLOCKS = {'unverified'} (or empty). Any other arbitrary string — 'yes', 'Public_Domain', the typo 'pubic_domain' — passes the gate, downloads content, and is written verbatim into ledger.jsonl as the machine-readable rights assertion. The compliance layer's whole value is the per-source permission record; an unvalidated free-text field means the ledger can carry meaningless or misspelled assertions while looking authoritative, and any downstream consumer switching on the enum values treats them as unknown states.
- **Verified because:** Confirmed against the real code. ingest.py:220 (`if spec.permission in PERMISSION_BLOCKS or not spec.permission:`) is the only permission gate in the ingest path, and PERMISSION_BLOCKS is just {"unverified"} (models.py:25). The PERMISSIONS tuple (models.py:24) is referenced for validation nowhere except argparse `choices` on the CLI `--add` flag (cli.py:37) — which does NOT cover the documented `--sources sources.json|yaml` batch path: load_specs (ingest.py:284-289) does `permission=it.get("permission", "unverified")` with no vocabulary check, and SourceSpec/SourceVideo have no __post_init__ validation (verified by grep). So "yes", "Public_Domain", or "pubic_domain" passes the gate, triggers a download, and ledger.py:108 writes `src.permission` verbatim into ledger.jsonl while the error message (ingest.py:225) promises an enum. Aggravating detail the claim missed: a case variant of the blocking value itself ("Unverified") bypasses the block, because ingest.py:220, SourceVideo.is_blocked (models.py:74), and ledger summary's `permission == "unverified"` check (ledger.py:178) are all exact string compares — defeating the gate's single purpose for the value most likely to be mistyped in hand-written YAML. Programmatic callers of orchestrate.produce(source_specs=...) are equally unguarded. The claimed framing is honest: no overstated attacker scenario, and the impact (meaningless free text in a compliance ledger that looks authoritative, plus enum-switching consumers treating values as unknown states) matches the code.
- **Fix:** Validate the assertion against the controlled vocabulary at the gate. In ingest.py, import PERMISSIONS alongside PERMISSION_BLOCKS, and in ingest_one() normalize then validate before the block check: `perm = (spec.permission or "").strip().lower()`; if `perm not in PERMISSIONS`, return a SourceVideo with status=SOURCE_BLOCKED and error like `f"unknown permission {spec.permission!r} — must be one of {'|'.join(PERMISSIONS)}"`; then apply the existing PERMISSION_BLOCKS check on the normalized value (this also fixes the "Unverified" case-bypass). Use the normalized value when constructing the SourceVideo so the ledger only ever carries canonical enum values. Additionally validate in load_specs() (ingest.py:284-289) to fail fast with the offending item's ref in the message, and consider the same normalization in SourceVideo.is_blocked (models.py:74) for defense in depth on manifests edited by hand.

### Face-ID ONNX models are required from ~/.cache/clipstudio_models but nothing provisions or documents them — feature silently dead on fresh machines
- **Where:** `vidlore/clipstudio/faceid.py:17`  ·  **Lens:** config-platform  ·  **Verifier confidence:** high
- **Problem:** faceid.available() requires yunet.onnx and sface.onnx to already exist in ~/.cache/clipstudio_models (or VIDLORE_CLIPSTUDIO_MODELS). A repo-wide search shows faceid.py is the ONLY file that mentions these model files — no download script, no setup step, no README/DESIGN.md mention, and VIDLORE_CLIPSTUDIO_MODELS is absent from config.py, which DESIGN.md:206 calls the 'full list' of env flags. On any machine except the one where the files were manually placed, available() returns False and orchestrate.produce_auto (line 175: 'if _faceid.available():') silently skips reference building with no log of why — Face-ID identities, the identity-unconfirmed compliance flag, and the OCR-corroboration signal all quietly disable, and the user has no way to discover what's missing or where to put it.
- **Verified because:** Core claim verified against the code. faceid.py:17-26 reads VIDLORE_CLIPSTUDIO_MODELS (default ~/.cache/clipstudio_models) and available() is literally 'return YUNET.exists() and SFACE.exists()' with no download path. Repo-wide grep (excluding vendored venvs) shows yunet/sface and VIDLORE_CLIPSTUDIO_MODELS appear ONLY in faceid.py — no provisioning script, no README/requirements/CURRENT_STATE mention. DESIGN.md section 8 says the 'full list' of VIDLORE_CLIPSTUDIO_* flags is in config.py, and config.py omits VIDLORE_CLIPSTUDIO_MODELS, so the env var is undocumented as claimed. orchestrate.py:172-179 logs '4/9 · build Face-ID references' then, when available() is False, silently leaves faceid_obj=None/refs={} with no skip message; index.py:237 sets do_faceid=False so no shot gets face_ids. However, two of the three claimed downstream losses are overstated: (1) the identity-unconfirmed compliance flag does NOT disable — ledger.py:58-62 appends FLAG_IDENTITY when signals['faceid'] is 0 and the verifier did not confirm, so it fires MORE often (conservative, routes clips to human review); (2) OCR corroboration does NOT disable — index.py:238/278-282 runs OCR independently of faceid (gated by cfg.detect_ocr, roster, ocr.available()), and match.py:238-239 still awards faceid=0.8 from OCR name-card hits. The LLM verifier remains the 'PRIMARY gate' per ledger.py's comment. Net effect: real silent feature death on fresh machines (the models on this machine are manually placed in ~/.cache/clipstudio_models), degraded matching quality and inflated review queue, undiscoverable cause — but a graceful-degradation/provisioning gap, not a correctness or safety failure.
- **Fix:** Three-part fix. (1) Observability: in orchestrate.py produce_auto step 4, add an else branch that logs e.g. 'faceid unavailable — yunet.onnx/sface.onnx not found in {MODELS_DIR}; identity recognition skipped (set VIDLORE_CLIPSTUDIO_MODELS or download models)' so the skip is never silent; same for any other available() call sites. (2) Provisioning: add an ensure_models() helper in faceid.py that downloads the two small official OpenCV Zoo models (face_detection_yunet_2023mar.onnx and face_recognition_sface_2021dec.onnx from github.com/opencv/opencv_zoo) into MODELS_DIR on first use, with try/except falling back to the current unavailable behavior plus a logged reason; call it from available() or from orchestrate before checking. (3) Documentation: add VIDLORE_CLIPSTUDIO_MODELS to config.py (e.g. a models_dir field read via the same _s() helper, with faceid.py consuming it) so DESIGN.md's 'full list in config.py' statement is true again, and add a short 'Face-ID models' setup note to README.md/DESIGN.md listing the two filenames, the cache path, and the download URLs.

### Coverage/anchor force-includes from discover are silently truncated by the download limit — the 'must download' guarantee never holds under defaults
- **Where:** `vidlore/clipstudio/orchestrate.py:163`  ·  **Lens:** integration  ·  **Verifier confidence:** high
- **Problem:** discover_sources returns up to discover_target (default 18) ranked candidates, THEN appends entity-coverage extras (up to discover_coverage_extra=6) and anchor-coverage extras (2-4 per anchor, 'the scene(s) the whole video is ABOUT must be downloaded') at the END of the list. But orchestrate caps downloads at `dl_limit = max_sources + cfg.discover_coverage_extra` (CLI default 6+6=12; portal 8+6=14) and download_candidates truncates `candidates[:limit]`. Since 12/14 < 18, every appended coverage and anchor candidate — plus ranked positions 12..18 — is never even considered for download. Both the orchestrate comment ('Download the full discovered set INCLUDING the entity-coverage overflow') and discover's anchor comment are false under default config: the core scene of a single-scene deep-dive can be discovered, force-included, logged ('+anchor ...'), and then silently dropped.
- **Verified because:** Confirmed against the real code. orchestrate.py:154 sets cfg.discover_target = max(18, max_sources) — with CLI default max_sources=6 (cli.py:55) or portal default 8 (web.py:199), discover_target stays 18. discover.py fills `final` with up to 18 ranked candidates (break at `len(final) >= cfg.discover_target`, lines 370–371), then APPENDS entity-coverage extras (`final.append(extra)`, line 395, up to discover_coverage_extra=6) and anchor force-includes (`final.append(c)`, line 426, want=4 for single_scene else 2 per anchor, line 405) at positions >= 18. orchestrate.py:163 then computes `dl_limit = max_sources + cfg.discover_coverage_extra` = 12 (CLI) or 14 (portal), and download.py:142 truncates: `queue = candidates if not limit else candidates[:limit]`. So ranked positions 12–17 and EVERY appended coverage/anchor candidate are silently dropped. There is no guard or re-sort anywhere: download_candidates has exactly one caller (orchestrate.py:165) and the list passes straight from discover_sources to the truncation. The comments at orchestrate.py:161–162 ("Download the full discovered set INCLUDING the entity-coverage overflow") and discover.py:400–402 ("the scene(s) the whole video is ABOUT must be downloaded") are both false under all default configurations, and discover's `+coverage`/`+anchor` log lines claim inclusions that never download. The bug fires on the ordinary happy path (any topic where search fills 18 slots). One partial mitigation keeps this from being higher severity: the anchor relevance boost (discover.py:241–244, up to +0.45) often ranks the best anchor uploads inside the top 12 on merit, so the core scene is frequently downloaded via ranking anyway — but the force-include safety net (the entire stated purpose of the appended code) is dead code under defaults, and entity-coverage extras for under-represented characters are always truncated.
- **Fix:** Make the download limit derive from what discover actually selected instead of an independent formula. Simplest correct fix in orchestrate.py produce_auto: change line 154 to size the ranked base list to the user's request (`cfg.discover_target = max_sources`) so discover returns max_sources ranked candidates plus its small coverage/anchor overflow, then download the whole returned list (`download_candidates(..., limit=0)` or `limit=len(candidates)`) — discover already enforces the cap, so the force-included extras always survive. If a hard ceiling on downloads is still desired, compute `dl_limit = len(candidates)` capped at `max_sources + cfg.discover_coverage_extra + 4*len(anchor_scenes)`, or alternatively have discover_sources truncate the ranked middle of the list itself and tag force-included candidates so download_candidates never drops them. Also fix the now-misleading comment at orchestrate.py:161–162.

### DESIGN.md documents only the 6-stage manual pipeline — produce_auto, the portal, and 8 modules are undocumented; the matching model is under-described
- **Where:** `vidlore/clipstudio/DESIGN.md:35`  ·  **Lens:** integration  ·  **Verifier confidence:** high
- **Problem:** The design doc's pipeline diagram (§1) and stage list cover only produce(): ingest→index→segment→match→cut→ledger→build, with 'web (later)' as a review surface. The implementation's primary path is the 9-stage produce_auto (orchestrate.py:107-220) — analyze/discover/download/hd_download/faceid/verify/ocr/llm and the port-5151 creation portal (web.py) appear nowhere in the doc. §4's scoring formula also omits w_dialogue (default 0.55, a dominant signal), subtitle_penalty, the anchor-continuity bonus, and the recency/near-dup machinery (config.py:49-86, match.py:243-259). Anyone trusting DESIGN.md ('read before trusting any output') gets a materially wrong picture of what the tool does — including its most consequential behaviour, autonomous downloading.
- **Verified because:** Confirmed against the actual files. DESIGN.md §1 (lines 35-68) documents only ingest→index→segment→match→cut→ledger→build with '[8 REVIEW] web (later)' as a future review-only surface; grep for produce_auto/discover/verify/5151/faceid/hd_download/ocr/w_dialogue/subtitle_penalty in DESIGN.md returns nothing. Meanwhile orchestrate.py:107-220 implements the 9-stage produce_auto with the exact docstring 'FULL AUTO: ... analyze → discover → download(policy) → Face-ID refs → deep index → match → cut → AI verify → ledger + QC report → assemble'; the module contains 9 undocumented files (analyze.py, discover.py, download.py, hd_download.py, faceid.py, verify.py, ocr.py, llm.py, plus web.py repurposed); web.py:7/253 is a creation portal on port 5151 that calls produce_auto (web.py:55), not the documented review surface. 'Primary path' is accurate: cli.py:81 defaults to produce_auto whenever no --sources/--add are given, and the portal only drives auto mode. The scoring claims also hold: config.py:49 sets w_dialogue=0.55 (second-largest weight), config.py:55 subtitle_penalty, config.py:80-86 the recency/near-dup machinery; match.py:245 applies w_dialogue, match.py:248-252 the anchor-continuity bonus, match.py:259 subtitle_penalty — none in DESIGN.md §4/§8. README.md and CURRENT_STATE.md don't cover clipstudio either, so DESIGN.md is the module's only doc. Severity caveat: this is documentation drift, not a code defect — the auto download path remains permission-policy gated (policy='block' default), so the doc's §5 safety posture isn't actually violated by the code; the reviewer's framing slightly overstates the danger of 'autonomous downloading', which is blocked by default.
- **Fix:** Update /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/DESIGN.md to match the implementation: (1) add a second pipeline diagram in §1 for the 9-stage produce_auto path (analyze → discover → download[policy-gated, default block] → Face-ID refs → deep index → match → cut → AI verify+repair → ledger/QC → assemble), citing orchestrate.py:107; (2) replace '[8 REVIEW] web (later)' with a §on the actual web portal — python -m vidlore.clipstudio.web on port 5151 (VIDLORE_CLIPSTUDIO_PORT), which runs produce_auto in a background thread; (3) extend §4's score formula with the implemented signals: + w_dialogue*dialogue_lock (default 0.55, dominant when an iconic quote is spoken in ASR), + anchor_bonus for anchor-scene sources (VIDLORE_CLIPSTUDIO_ANCHOR_BONUS, 0.45 single-scene / 0.12), − subtitle_penalty (0.06), and the recency/near-dup anti-repetition window (recency_weight 0.7, cooldown 48 beats, near_dup_cos 0.90, scene_gap_sec 8.0); (4) update §8's flag list with W_DIALOGUE, SUBTITLE_PENALTY, WATERMARK_MODE, ANCHOR_BONUS, RECENCY_*, NEAR_DUP_COS, and the auto-mode flags (--topic/--movie/--policy/--max-sources/--no-verify per cli.py); (5) list the new modules (analyze, discover, download, hd_download, faceid, verify, ocr, llm, web) in §6/§9, and state explicitly that auto-discovered sources are gated by the download policy (block | open_only | approved_testing) so the §5 permission story stays accurate.

### Theme→music bucket map keys don't match the engine's theme names — most buckets are unreachable, 'crime' gets history music
- **Where:** `vidlore/clipstudio/build.py:202`  ·  **Lens:** integration  ·  **Verifier confidence:** high
- **Problem:** _MUSIC_BUCKET keys are {history, true_crime, mystery, war, tech, nature, finance, survival}, but theme_name comes from the CLI/portal whose vocabulary is the parent engine's theme_names() = [crime, history, modern, minimalist, standard] (themes.py:260-261; the portal form offers exactly these five). Only 'history' overlaps, so a 'crime' video gets the default 'historical_epic' bucket instead of the intended 'dark_investigation' (which exists on disk under vidlore/assets/music/), and seven mapped buckets are dead keys that no caller can select. Drifted vocabulary between clipstudio and the engine's themes.
- **Verified because:** Verified against the real code. build.py:202-206 defines _MUSIC_BUCKET with keys {history, true_crime, mystery, war, tech, nature, finance, survival}, and build.py:221 does `bucket = _MUSIC_BUCKET.get(theme_name, "historical_epic")`. But theme_name's vocabulary is the engine's: themes.py:260-262 theme_names() returns ["crime", "history", "modern", "minimalist", "standard"]; clipstudio cli.py:42-43 documents --theme as crime|history|modern|minimalist|standard; clipstudio web.py:126-127 form offers exactly those five. orchestrate.py:92-93 and 207-209 (the only build_video callers) pass theme_name=theme verbatim with no normalization and never pass music=, so the bucket lookup runs on every default render — no guard exists. Only "history" overlaps; "crime" falls to the default "historical_epic" instead of "dark_investigation", which exists on disk at vidlore/assets/music/dark_investigation/ but is reachable only via the dead key "true_crime". The other six mapped keys (mystery, war, tech, nature, finance, survival) are likewise unreachable. The only correction: severity is medium, not high — compose_score still produces an audible, valid music bed (historical_epic has tracks), so the failure is silently wrong music genre on 4 of 5 themes, not a crash or missing audio.
- **Fix:** Rekey _MUSIC_BUCKET in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/build.py:202-206 on the engine's actual theme vocabulary, choosing from buckets that exist under vidlore/assets/music/: {"crime": "dark_investigation", "history": "historical_epic", "modern": "tech_cyber", "minimalist": "ambient", "standard": "neutral"} (the disk has dark_investigation, historical_epic, tech_cyber, ambient, neutral, mystery, suspense, etc.). Keep the "historical_epic" default in the .get() for safety. To prevent future drift, add a small test or import-time assertion that set(_MUSIC_BUCKET) == set(vidlore.themes.theme_names()), and optionally that each mapped bucket is in vidlore.musiclib.CATEGORIES.

### plan_beats failure path crashes with ZeroDivisionError; the advertised '1 clip/scene' fallback is dead code
- **Where:** `vidlore/clipstudio/build.py:500`  ·  **Lens:** ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** If plan_beats raises (e.g. an older engine whose plan_beats lacks the energies/roles kwargs -> TypeError), the first call at build.py:383 is caught and nbeats is set to all-1s with the log 'plan_beats unavailable — 1 clip/scene' (build.py:387-389). But the budget loop then calls plan_beats again; the same exception hits 'except Exception: break' on iteration 0, leaving _nb = [0]*len(segments) (initialized at build.py:494), and line 519 UNCONDITIONALLY overwrites the good nbeats with min(_nb, _D) = all zeros. At line 530 'slice_dur = ndur.get(...) / k' divides by k=0 and build_video crashes after the entire download/index/match/verify pipeline has completed. The graceful-degradation path the code documents never executes.
- **Verified because:** Confirmed against /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/build.py. (1) Lines 381-389: first plan_beats call is wrapped in try/except; on failure it logs "plan_beats unavailable (...) — 1 clip/scene" and line 389 sets nbeats to all 1s. (2) Lines 493-501: the visual-budget loop calls plan_beats again with identical arguments on iteration 0 (_energies_eff = list(_eng)); a deterministic exception (e.g. TypeError from an older vidlore.assemble whose plan_beats lacks the energies/roles kwargs — the current assemble.py:909 has them, so the trigger is version skew or an internal plan_beats error) hits "except Exception: break" with _nb = [0]*len(segments) (line 494), and the break skips line 502's max(1, kk) normalization, leaving _nb all zeros. (3) Line 519 is unconditional straight-line code (verified indentation): nbeats = [min(_nb[_pos], _D[segments[_pos].index]) ...]; _scene_distinct returns max(1, len(cl)) >= 1, so min(0, >=1) = 0 — nbeats becomes all zeros and the line-389 fallback is clobbered. The fallback value is dead in every path since 519 always reassigns. (4) Lines 529-530: k = nbeats[pos]; slice_dur = ndur.get(...) / k raises ZeroDivisionError with k=0; no guard exists between 519 and 530 (line 523 only logs). (5) No caller guard: orchestrate.py:92 and orchestrate.py:207 call build_video bare as the final stage (6/6 and 9/9) after the full download/index/match/cut/verify pipeline, so the exception kills the run at its most expensive point. The only softening is reachability: with the bundled assemble.py, plan_beats raising is an exceptional condition (engine skew or internal error), but the 387 handler exists precisely for that condition and whenever it fires the 530 crash deterministically follows — the documented graceful-degradation path can never execute.
- **Fix:** Make the budget loop's failure path preserve the line-389 fallback instead of clobbering it. Track success explicitly: before the loop set _planned = False; inside the loop set _planned = True after the plan_beats call succeeds (right after the inner for completes, before the normalization at line 502); change line 519 to only overwrite when planning succeeded: "if _planned: nbeats = [min(_nb[_pos], _D[segments[_pos].index]) for _pos in range(len(segments))]" — otherwise keep the existing nbeats (the all-1s fallback or the first call's successful plan). As a belt-and-braces guard, also clamp at line 519 with max(1, min(...)) so k can never be 0 regardless of future refactors; this is semantics-preserving because on every successful iteration _nb >= 1 (line 502) and _D >= 1, so min(_nb, _D) >= 1 already. No change needed at line 530 once nbeats is guaranteed >= 1.

### Watermark punch-in crop hard-scales to 1280x720 — downscales 1080p footage in a 1920x1080 pipeline and stretches non-16:9 sources
- **Where:** `vidlore/clipstudio/build.py:43`  ·  **Lens:** ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** _watermark_crop_filter ends every watermark-cropped beat clip with 'scale=1280:720', but the engine renders at 1920x1080 (assemble.py _scene_video: 'scale=1920:1080:force_original_aspect_ratio=increase,crop=1920:1080'). Two consequences: (1) a 1080p watermarked source is first punched-in (already losing 16% per axis of detail), then downscaled to 720p, then re-upscaled to 1080p by assemble — watermarked sources come out visibly softer than every other clip in the same video; (2) the bare 'scale=1280:720' has no force_original_aspect_ratio, so a non-16:9 source (e.g. a 4:3 archive.org film, which discover tags and downloads) is anamorphically stretched to 16:9 — crop=iw*0.84:ih*0.84 preserves the source AR, then the fixed-size scale distorts it, and assemble's subsequent 16:9-to-16:9 scale preserves the distortion.
- **Verified because:** Verified against the real code. (1) build.py:43 ends the watermark punch-in filter with a bare 'scale=1280:720' (no force_original_aspect_ratio, no setsar), exactly as claimed, while the engine canvas is 1920x1080 (assemble.py:7207-7211 '_scene_video' non-archival branch: 'scale=1920:1080:force_original_aspect_ratio=increase,crop=1920:1080,setsar=1'). (2) The path is reachable in the DEFAULT configuration: watermark_mode defaults to "crop" (build.py:394 getattr default and match.py:96 env default), RapidOCR is an integral pipeline dependency (clipstudio/ocr.py exists to manage it), and the filter is applied to every beat clip of a watermarked source via _recut_to_duration's '-vf' (build.py:123-124, 564-565). Downloads default to max_height=1080 (config.py:100) and hd_download.py explicitly works around yt-dlp's SABR 720p degradation to fetch true 1080p, so the 1080p→(crop to 1613x907)→720p→1080p round-trip is the normal case — and because non-watermarked clips get NO '-vf' in _recut_to_duration (they keep native resolution into assemble), watermarked beats come out uniquely softer than adjacent clips in the same video. (3) The aspect-ratio stretch is also real and unguarded: 'crop=iw*0.84:ih*0.84' preserves the source AR (a 4:3 archive.org film stays 4:3, e.g. 1440x1080→1209x907), then 'scale=1280:720' stretches it anamorphically; discover.py lists archive.org as a first-class provider (lines 160-170). The only AR-preserving path in assemble (the 'archival' pillar-box branch, assemble.py:7188-7204) is gated on shot_types[i]=="archival" (assemble.py:8933), and build.py's _assign_editorial only assigns ["establishing","detail","reaction","wide","tracking","portrait"] (build.py:266) — never "archival" — so the distorted 16:9 clip passes through assemble's AR-neutral scale unchanged. Without the buggy scale, assemble would center-crop a 4:3 clip (no distortion), so the line makes things strictly worse. One caveat on the claim's framing: the "16% per axis" punch-in loss is inherent to the watermark-crop feature itself (by design); the bug proper is the extra 720p bottleneck and the AR stretch.
- **Fix:** In _watermark_crop_filter (build.py:43), stop hard-scaling to 1280x720. Simplest correct fix: drop the trailing scale entirely and return just the crop ('crop=iw*{k}:ih*{k}:{x}:{y}') — _recut_to_duration re-encodes at the cropped native resolution, and assemble's _scene_video already normalizes every clip to 1920x1080 with force_original_aspect_ratio=increase+crop, which preserves AR (center-cropping non-16:9 sources exactly like their non-watermarked siblings) and keeps a 1080p source's full 1613x907 cropped detail through a single upscale instead of a 720p round-trip. If pre-normalization is desired for encoder uniformity, use 'scale=1920:1080:force_original_aspect_ratio=increase,crop=1920:1080,setsar=1' instead of 'scale=1280:720' to match the engine canvas. Either way, also consider rounding crop dimensions to even values (e.g. 'floor(iw*0.84/2)*2') since libx264 yuv420p requires even dimensions once the fixed scale is removed.

### Logo-corner vote counts bottom-center burned subtitles as corner votes — crop can be steered to the wrong corner, leaving the watermark on screen
- **Where:** `vidlore/clipstudio/build.py:74`  ·  **Lens:** ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** _detect_logo_corner's near-edge test is 'cx < 0.3 or cx > 0.7 or cy < 0.3 or cy > 0.7', so bottom-CENTER text (cy ~ 0.85, cx ~ 0.5 — i.e. ordinary burned-in dialogue subtitles, which the OR on 'len(re.findall(r"[A-Za-z]", txt)) >= 6' explicitly admits) passes the filter and votes 'bl' or 'br' purely on the knife-edge cx > 0.5 comparison of its centroid jitter. Across the 3 sampled frames, subtitle reads typically outnumber the actual corner-logo reads, so the winning corner is decided by subtitle centroid noise, not the logo. The resulting 16% punch-in then (a) crops the WRONG corner, so the rival-channel watermark survives and is ENLARGED by the zoom, and (b) cannot remove a full-width subtitle in any case. The whole point of watermark_mode='crop' (keeping the source instead of dropping it) is defeated for any watermarked source that also has burned subtitles.
- **Verified because:** Confirmed against the real code. In /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/build.py, _detect_logo_corner (lines 46-84) does exactly what the claim says: line 70 admits any OCR read with >= 6 Latin letters ('if not (_OCR_JUNK.search(txt) or len(_re3.findall(r"[A-Za-z]", txt)) >= 6): continue'), and _OCR_JUNK (match.py:202-207) is keyword-based with an explicit design comment (match.py:197-201) that ordinary dialogue subtitles "must pass" it — so burned subtitles reach the vote. Line 74's near-edge test 'if cx < 0.3 or cx > 0.7 or cy < 0.3 or cy > 0.7' uses OR across the two axes, which only excludes the central 0.3-0.7 x 0.3-0.7 box; a bottom-CENTER subtitle (cx~0.5, cy~0.85) satisfies cy > 0.7 and votes on line 75, where the horizontal half '("r" if cx > 0.5 else "l")' is decided by centroid jitter around 0.5. The inline comment "near an edge = logo, not center content" proves the intent was a corner test, but OR instead of AND between axes fails to implement it. Reachability is confirmed: watermark_mode defaults to "crop" (build.py:394, match.py:96), _watermarked_source_corners (build.py:401, 87-106) calls _detect_logo_corner for every watermarked source, and match.py:68-69 itself documents that corner logos are "only OCR-legible on SOME frames" — so across the 3 sampled frames (5s/20s/45s) subtitle boxes can plausibly outnumber logo reads. There is no downstream guard: the voted corner feeds _watermark_crop_filter directly at build.py:564, and nothing verifies the crop removed the text (the later _clip_has_burned_text pass at build.py:283-314 only suppresses the project's own captions, it never re-checks the watermark). I traced the consequence: with a real logo at top-right and a vote of "bl", _watermark_crop_filter (lines 34-43) keeps x in [0.16*iw, iw], y in [0, 0.84*ih] — a region that contains the top-right logo — then scale=1280:720 enlarges it ~19%. One nuance the claim overstates slightly: subtitle votes can split between bl and br across frames (cx jitter goes both ways), and "b"+"r" coincides with the most common watermark corner and the "br" default, so not every watermarked-plus-subtitled source ends up wrong — but any top-corner logo (tr/tl) is steered wrong whenever bottom subtitles win the vote, which is the mechanism claimed.
- **Fix:** Make the near-edge test an actual corner test by requiring BOTH axes to be off-center: change line 74 in build.py to 'if (cx < 0.3 or cx > 0.7) and (cy < 0.3 or cy > 0.7):' so bottom-center/top-center/edge-center strips (subtitles, lower-thirds, tickers) cannot vote. Additionally harden the vote with two cheap filters that match how logos differ from subtitles: (1) skip boxes wider than ~45% of frame width ('if (max(xs)-min(xs))/W > 0.45: continue'), since a corner logo is compact while a subtitle line spans the frame; (2) optionally restrict votes to reads matching _OCR_JUNK (the watermark signature that put the source on this path in the first place) and only fall back to the >=6-letters heuristic when no junk-matching read is found in any frame. As a final safety net, after cutting a cropped clip, run the existing _clip_has_burned_text check on a corner-cropped thumbnail of the kept region and log a warning (or retry the opposite corner) if confident corner text survives.

## LOW (15)

### Confidence has no lower clamp and can go negative — ledger then mislabels a real selection as FLAG_NO_CANDIDATE
- **Where:** `vidlore/clipstudio/match.py:407`  ·  **Lens:** correctness+correctness+matching-quality+matching-quality+matching-quality  ·  **Verifier confidence:** high
- **Problem:** The module docstring claims the score is 'clamped to [0,1]', but only the upper bound is applied: base can go negative via the subtitle penalty (match.py:258-259 'base -= cfg.subtitle_penalty') when all signals are 0, and qual = min(1.0, base) keeps the negative value. ledger.evaluate_flags treats 'confidence <= 0.0' as no candidate and returns early (ledger.py:40-41), so a segment that DID get a clip selected and cut (clip_path set) is recorded in the ledger/review queue as FLAG_NO_CANDIDATE, and its identity/overuse checks are skipped entirely.
- **Verified because:** Confirmed against the real code. match.py:4 promises "clamped to [0,1]" but match.py:407 (qual = round(min(1.0, base), 4)) and match.py:427 (confidence=round(min(1.0, base_q), 4)) apply only the upper bound; the sole lower clamp, match.py:406, is on `adj` which drives pick order only. base goes negative exactly as claimed: all five signal components are individually non-negative, so base >= 0 until match.py:257-259 subtracts cfg.subtitle_penalty (default 0.06, config.py:55) for any non-junk on-frame text >= 10 chars. Reachability verified: when CLIP is unavailable (match.py:281, vr=None when not clip_available()) or embeds are missing, clip01=0 for every shot; with no transcript/face/tag/quote overlap, every hardsubbed shot scores -0.06. The selection loop still picks one — `if best is None or adj > best[0]` (match.py:412) seats the first eligible candidate even at adj=0.0 — whenever the whole eligible pool carries burned-in subtitles (the exact case the _OCR_JUNK comment says must pass the gate) or clean shots are exhausted by the per-shot reuse cap (match.py:357). cut.py:54 cuts every selection with source_id regardless of confidence (clip_path set). ledger.py:40-41 then early-returns [FLAG_NO_CANDIDATE] on confidence <= 0.0, and ledger.py:90 (sel.flag_reasons = reasons) overwrites even a "verifier_failed" reason verify.py had appended, skipping identity/overuse/verifier checks. Also verify.py:140 (sel.confidence = alt.score) can propagate a negative alternate score. The claim's only overstatement is impact: FLAG_NO_CANDIDATE sets flagged=True, so the segment still enters the human review queue — it is mislabeled, with specific flags suppressed and flag_breakdown stats wrong, but never escapes review. Severity is therefore low, not higher.
- **Fix:** Clamp the lower bound where the score is stored: in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/match.py change line 407 to `qual = round(max(0.0, min(1.0, base)), 4)` and line 427 to `confidence=round(max(0.0, min(1.0, base_q)), 4)`, which also fixes the negative alternate score that verify.py:140 can promote. Additionally, make the ledger's no-candidate test structural rather than score-based: in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/ledger.py line 40 use `if not sel.source_id or sel.shot_index < 0: return [FLAG_NO_CANDIDATE]`, so a real selection that legitimately scores 0.0 falls through to the verifier/identity/overuse/low-confidence checks (it will pick up FLAG_LOW_CONFIDENCE via cfg.min_confidence when the verifier never ran) instead of being mislabeled as having no candidate.

### Manual pipeline gates LLM enrichment on engine has_llm (Anthropic-only) while the rest of the system accepts Gemini
- **Where:** `vidlore/clipstudio/orchestrate.py:67`  ·  **Lens:** correctness+config-platform+integration  ·  **Verifier confidence:** high
- **Problem:** produce() gates segment enrichment on getattr(eng, 'has_llm', False) — the engine Config property which is bool(anthropic_api_key) only (vidlore/config.py:64-65). But enrich_with_llm itself guards with clipstudio.llm.has_llm (segment.py:154), which also counts Gemini, and the auto pipeline's analyze_script does the same (analyze.py:235). A Gemini-only installation (the documented fallback configuration) silently skips beat enrichment in manual mode — degrading expected_visual/entities/keywords to heuristics — while the auto pipeline happily uses Gemini for the same work. Same brain, inconsistent gate.
- **Verified because:** Confirmed against the actual code. orchestrate.py:67-68 gates manual-mode enrichment on `getattr(eng, "has_llm", False)` where `eng = engine_config()` (line 28) is the engine Config whose has_llm property is `bool(self.anthropic_api_key)` (vidlore/config.py:64-65) — Anthropic-only. The callee enrich_with_llm guards itself with the broader `_llm.has_llm(engine_cfg)` (segment.py:154), and clipstudio/llm.py:196-201 returns True for `gemini_available()` OR an Anthropic key. The auto pipeline gates identical work on the broad check (analyze.py:235), as does verify.py:91, and llm.py's docstring documents Gemini as a supported bidirectional fallback (VIDLORE_CLIPSTUDIO_LLM_PROVIDER=gemini), so Gemini-only is a legitimate configuration. Reachability: manual mode runs from clipstudio/cli.py:70-78 whenever --sources/--add is supplied; enrich_with_llm has exactly one caller repo-wide, no other path re-enriches, and the skip emits no log message — segments silently keep heuristic defaults (expected_visual = raw line text, regex keywords/entities, segment.py:123-131). The outer gate is also redundant since the callee already no-ops safely without a brain, so the strict check provides no protection. However, it is a silent quality degradation (worse matching inputs) only in Gemini-only manual mode, with working heuristic fallback and no crash or data loss — so severity is low, not medium/high.
- **Fix:** In /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/orchestrate.py, replace the engine-Config gate with the clipstudio brain check used everywhere else: add `from . import llm as _llm` near the imports and change line 67 from `if enrich and getattr(eng, "has_llm", False):` to `if enrich and _llm.has_llm(eng):` (or simply `if enrich:`, since enrich_with_llm already self-guards via _llm.has_llm at segment.py:154 and no-ops safely when no provider is available). Optionally add a progress log when enrichment is skipped so the degradation is no longer silent, mirroring analyze.py's "heuristic (no LLM key)" message.

### min_shot_sec (1.0) < min_clip_sec (1.2): cut_selection extends past the shot boundary into the next, unrelated shot
- **Where:** `vidlore/clipstudio/cut.py:28`  ·  **Lens:** correctness+matching-quality+ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** Shots can legitimately be 1.0–1.2s (config.py:92 min_shot_sec=1.0). match._trim_window clamps the window to the whole shot (match.py:176-183), but cut_selection then forces dur = max(cfg.min_clip_sec, out-in) = 1.2s, so the encoded clip runs ~0.2s past the detected shot boundary into the NEXT shot — a visible flash of an unrelated scene at the end of the clip the matcher scored.
- **Verified because:** The cited code is exactly as claimed and the failure mode is reachable. (1) /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/index.py:69 merges shots with `_merge_short(scenes, cfg.min_shot_sec)` where min_shot_sec=1.0 (config.py:92), so shots with duration in [1.0, 1.2) legitimately survive into the pool. (2) match.py:176-183 `_trim_window` computes L = max(min_clip_sec, min(max_clip_sec, shot.duration, est+0.6)) = 1.2 for such a shot, but then clamps both ends to [shot.start, shot.end], returning the whole shot — e.g. for a 1.05s shot it returns (start, end) with out-in = 1.05. (3) cut.py:28 then sets `dur = max(cfg.min_clip_sec, sel.out_point - sel.in_point)` = 1.2 and runs ffmpeg `-ss in_p -t 1.2`, encoding up to 0.2s past the detected shot boundary into the next shot. The clamps at cut.py:29-31 only bound against the source FILE end (src.duration), never the shot end — there is no guard anywhere (grepped all min_clip_sec/min_shot_sec uses). However, the claimed user-visible impact is overstated: the primary render path (build.py:527-581) does NOT play these seg_NNN.mp4 clips — it re-cuts beats directly from the source via _recut_to_duration with start = in_p - 0.2 and duration per_beat = max(min_clip_sec, slice_dur) + 0.5 (>= 1.7s), routinely crossing shot boundaries by far more than 0.2s by design. cut.py's clips are consumed only by the human review surface (review.py:35), the fallback at build.py:572-575 when beat re-cuts fail, and verify.py:146 re-cuts. So the bug is real (the encoded clip does contain a tail of the next, unrelated shot) but its blast radius is the review/compliance artifact and a fallback path, not the main rendered output.
- **Fix:** Fix at the root by making the shot-merge floor at least the minimum clip length: in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/index.py:69 change `_merge_short(scenes, cfg.min_shot_sec)` to `_merge_short(scenes, max(cfg.min_shot_sec, cfg.min_clip_sec))` (or raise the min_shot_sec default to 1.2 in config.py:92), so every indexed shot is at least min_clip_sec long and _trim_window's window always satisfies out-in >= min_clip_sec, making cut.py's `max(cfg.min_clip_sec, ...)` a no-op. Defensively, also change cut.py:28 to trust the matcher's clamped window — `dur = max(0.1, sel.out_point - sel.in_point)` — so cut_selection never extends a window past what match._trim_window deliberately confined to one shot (note this requires re-indexing existing projects since shots.json is cached/resumed).

### ffmpeg return code never checked on cut/recut paths — truncated encodes pass as valid clips
- **Where:** `vidlore/clipstudio/cut.py:43`  ·  **Lens:** robustness+ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** cut_selection runs ffmpeg and infers success purely from 'output file exists and size > 0'. ffmpeg that exits non-zero mid-encode (corrupt source region, disk full, killed) often leaves a non-empty partial file, which is then recorded as sel.clip_path and consumed by build/assemble — a corrupt or moov-less clip surfaces only as a renderer failure far downstream. The same pattern exists in build.py:_recut_to_duration (line 130) and _apply_slow_motion (line 172); only _apply_cinematic_letterbox checks r.returncode.
- **Verified because:** The cited code is exactly as claimed. cut.py:42-48 runs `subprocess.run(cmd, capture_output=True, timeout=120)` and discards the CompletedProcess — success is inferred solely from `out.exists() and out.stat().st_size > 0`. The identical pattern exists in build.py:_recut_to_duration (lines 129-133, `return dest if (dest.exists() and dest.stat().st_size > 0) else None`) and build.py:_apply_slow_motion (lines 171-175), while _apply_cinematic_letterbox (build.py:148-149) is the only helper checking `r.returncode == 0`. The failure mode is reachable: the try/except only catches exceptions (TimeoutExpired, spawn errors); a non-zero exit (ENOSPC, mid-stream encoder error) or a signal kill (negative returncode) returns normally, and ffmpeg's mp4 muxer writes the moov atom at trailer time, so an aborted encode leaves a non-empty moov-less file that passes the size check and is persisted as sel.clip_path (cut.py:64) and used as a beat clip in build_video (build.py:565-571). verify.py:146 re-cuts through the same unchecked path. HOWEVER, the claimed downstream impact is overstated: the consuming renderer, vidlore/assemble.py lines 6857-7184, contains an explicit "ENCODE-POOL RELIABILITY (2026-05-31)" layer built for exactly this input — _clip_ready() validates every clip before encode (size stability, stream probe, 3-frame decode, plus a strong mode with full decode and multi-point seek tests catching "moov atom"/truncated-mdat corruption) and substitutes a never-raising graded slate (_safe_slate) for any failing clip, logging the reason. So a truncated cut does NOT surface as a renderer failure; it surfaces as a silently-slated beat in the final video plus misleading "cut: done N/N clips" success counts. The defect is real (the claim's code-level facts are correct), but its blast radius is bounded by a downstream guard the review missed.
- **Fix:** Capture and check the return code in all three helpers, mirroring the existing _apply_cinematic_letterbox pattern. In cut.py:cut_selection: `r = subprocess.run(cmd, capture_output=True, timeout=120)` then `if r.returncode != 0: out.unlink(missing_ok=True); return None` before the size check (unlinking prevents a stale partial seg_NNN.mp4 from lingering in clips_dir). Same change in build.py:_recut_to_duration (return None and unlink dest on non-zero rc). In build.py:_apply_slow_motion, on non-zero rc unlink dest and return the original clip_path (the function's existing fallback). Optionally log the last line of r.stderr through the progress callback so a failed cut is visible at the stage that caused it instead of as a slate substitution deep in assemble's render log.

### Nonexistent local path is classified as a URL and handed to yt-dlp; the 'local file not found' branch is unreachable
- **Where:** `vidlore/clipstudio/ingest.py:40`  ·  **Lens:** robustness  ·  **Verifier confidence:** high
- **Problem:** SourceSpec.is_local requires Path(self.ref).exists(), so a typo'd local path returns False and ingest routes it to _ingest_url, where yt-dlp fails with a confusing 'is not a valid URL' extractor error recorded as the source error — after cfg.download_retries attempts with backoff sleeps. The dedicated `if not path.exists(): sv.status, sv.error = SOURCE_FAILED, "local file not found"` branch in _ingest_local (ingest.py:126-128) is consequently dead code and the clear error message it was written for can never be produced.
- **Verified because:** Confirmed against the real code and empirically. ingest.py:36-40 is exactly as quoted: `is_local` returns `Path(self.ref).expanduser().exists()` for any ref without a `scheme://` prefix, so a nonexistent local path is classified as a URL (verified live: is_local==False for a nonexistent path, True for an existing one). ingest_one (ingest.py:227-229) then routes it to _ingest_url; no caller validates paths first (cli.py:68 wraps raw --add strings into SourceSpec, load_specs at ingest.py:280-290 does no validation, orchestrate.py:46 passes specs straight to ingest_sources). The HD branch cannot intercept (hd_download.is_youtube line 61-63 matches only youtube domains), so the path hits the yt-dlp loop, which raises — verified live with the project's installed yt_dlp — `DownloadError: ERROR: [generic] '/Users/.../typo_does_not_exist.mp4' is not a valid URL`. The retry loop (ingest.py:184-198) catches all exceptions and sleeps min(2**attempt,10) even after the last attempt: with download_retries=3 (config.py:99 default) that is 3 futile attempts plus 2+4+8=14s of sleeps, after which sv.error is set to the confusing extractor message (ingest.py:202-203). The dedicated `if not path.exists(): ... "local file not found"` branch (ingest.py:126-128) is reachable only from ingest_one:228, which requires is_local==True, which requires exists()==True — so it can fire only via a TOCTOU race (file deleted between classification and ingest), never for the typo scenario it was written for. Every element of the claim holds. Severity is overstated only insofar as the outcome is still a SOURCE_FAILED record either way — this is an error-reporting/UX defect (misleading message, ~14s wasted per typo, needless thread-pool routing), not a correctness or security bug, so it rates low rather than medium.
- **Fix:** Classify by ref shape, not existence. In SourceSpec.is_local (vidlore/clipstudio/ingest.py:36-40), keep the scheme check (`^[a-z][a-z0-9+.-]*://` → not local) and `exists()` → local, but for refs that have no scheme and do not exist, return True when the ref is path-shaped — e.g. starts with "/", "./", "../", "~", a Windows drive letter, contains an OS path separator, or ends in one of _VIDEO_EXTS — instead of unconditionally False. That routes typo'd paths into _ingest_local, whose existing exists() check (ingest.py:126-128) immediately produces the clear "local file not found" error with no retries or backoff sleeps. Optionally tighten the message to include the resolved path. A one-line minimal variant: `return p.exists() or not re.match(r"^[^\s/]+\.[a-z]{2,}(/|$)", self.ref)` after the scheme check, so only domain-looking refs fall through to yt-dlp. Add a unit test asserting that SourceSpec(ref="/no/such/file.mp4").is_local is True and that ingest_one returns SOURCE_FAILED with error "local file not found".

### HD_ENABLED env parsing is case-sensitive and inverted for empty string ('FALSE' enables, '' disables)
- **Where:** `vidlore/clipstudio/hd_download.py:56`  ·  **Lens:** robustness+config-platform+config-platform+config-platform  ·  **Verifier confidence:** high
- **Problem:** The HD kill-switch compares the raw env value (not lowercased) against ('0', 'false', 'no', ''): setting VIDLORE_HD_DOWNLOAD=FALSE or False ENABLES the HD path (contradicting the comment 'turn the whole HD path off with VIDLORE_HD_DOWNLOAD=0' and the user's intent), while exporting the variable as an empty string DISABLES it — the opposite of every other boolean parse in the codebase (config._b lowercases; match.py/build.py gates treat '' as enabled-default). A user who 'disables' HD with False silently keeps spawning the Deno PO-token server and Chrome-cookie subprocesses.
- **Verified because:** The mechanical claim is verified: hd_download.py:56 reads `HD_ENABLED = os.environ.get("VIDLORE_HD_DOWNLOAD", "1").strip() not in ("0", "false", "no", "")` with no .lower(), so VIDLORE_HD_DOWNLOAD=FALSE/False/NO leaves HD enabled, and the failure mode is reachable: available() (line 68) gates only on HD_ENABLED plus toolchain paths, and both callers (ingest.py:146, download.py:57) then invoke download_hd, which spawns the Deno PO-token server via ensure_pot_server() (subprocess.Popen, line 88) and runs yt-dlp with --cookies-from-browser chrome (lines 137, 213); no other guard reads the env var. However, two parts of the claim are overstated or false. (1) The empty-string behavior is NOT "inverted" or "the opposite of every other boolean parse in the codebase": match.py:93, match.py:220, and build.py:592 use the identical tuple `("0", "false", "no", "")`, so set-but-empty disabling IS the prevailing gate convention in this codebase; only web_footage.py:167-169 lowercases and treats empty as default. (2) Nothing is "inverted" — uppercase disable values simply fall through to the enabled default; the documented kill-switch form (comment on line 55: "VIDLORE_HD_DOWNLOAD=0") and lowercase false/no all work correctly. What remains real is a narrow case-sensitivity robustness gap: a user disabling with the Python-style "False" or "FALSE" silently keeps the HD path (and its Deno + Chrome-cookie subprocesses) active. That is a genuine but minor config-parsing bug, not the codebase-wide inversion described.
- **Fix:** In /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/hd_download.py line 56, lowercase the value before comparing: `HD_ENABLED = os.environ.get("VIDLORE_HD_DOWNLOAD", "1").strip().lower() not in ("0", "false", "no", "off", "")`. Keep "" in the disable tuple deliberately (it matches the existing gate convention in match.py:93/220 and build.py:592 where set-but-empty disables) or remove it if set-but-empty should mean default-on — but document the choice in the line-55 comment. For consistency, apply the same .lower() to the identical gates in match.py:93, match.py:220, and build.py:592, or extract a shared bool-env helper (web_footage.py's _b at line 167 is the existing model) and use it in all four places.

### ensure_pot_server: check-then-act race from ThreadPool workers, orphaned Deno server, leaked log fd
- **Where:** `vidlore/clipstudio/hd_download.py:79`  ·  **Lens:** concurrency-resources  ·  **Verifier confidence:** high
- **Problem:** ensure_pot_server has no lock: download.py:160-164 and ingest.py:251-255 invoke download_hd from ThreadPoolExecutor workers staggered only 0.5s apart, while the server's first boot takes up to ~15s (loop at hd_download.py:97-102). Worker A spawns the Deno server; worker B's `_pot_alive()` is still False, so B spawns a second server on the same port — the loser fails to bind and crash-loops into the shared log, and the racy `_POT_PROC` global is overwritten so the first Popen handle is lost (never reaped → zombie until process exit). Additionally the log file handle opened at line 87 is never closed in the parent (one leaked fd per spawn attempt), and the detached server (start_new_session=True) is never terminated by anything — every CLI run / portal session leaves a Deno process running on :4416 indefinitely, and /tmp/clipstudio_pot_<port>.log grows without rotation.
- **Verified because:** The core check-then-act race is real and reachable: hd_download.py:79-103 has no lock around `if _pot_alive(): return True` followed by `subprocess.Popen(...)`, and both download.py:160-164 and ingest.py:251-255 invoke download_hd → ensure_pot_server (hd_download.py:204) from ThreadPoolExecutor workers (default concurrency 2, config.py:98) staggered only 0.5s while first boot takes up to ~15s (lines 97-102). On a cold start via the ingest path (orchestrate.py:46, no warmup) with ≥2 YouTube URLs, worker B will spawn a second Deno. However, every claimed consequence is exaggerated or wrong: (1) the loser does NOT "crash-loop" — Popen runs it once, it fails to bind, writes one error line, and exits; (2) "every CLI run leaves a Deno running indefinitely" is false — the port admits one server, later runs reuse it via _pot_alive(), and the detached persistence is explicitly intentional (line 91: "detach; survive our process" — a deliberate shared daemon design); (3) the "leaked log fd" is closed by CPython refcount GC when ensure_pot_server returns (the child keeps its inherited dup as intended) — unclean style, not a persistent leak; (4) the overwritten _POT_PROC is functionally inert — grep shows it is assigned but never read anywhere (no teardown exists), and a dropped running Popen is parked in subprocess._active and reaped on subsequent subprocess creation; at most one transient zombie from the bind-loser. The race also self-heals: worker B's 15s poll loop detects worker A's server and returns True, so all downloads succeed. Additionally, the main pipeline path (orchestrate.py:165) is usually pre-warmed because discover_sources → resolve_quality (discover.py:342-343, default on) sequentially calls probe_max_height → ensure_pot_server before the thread pool starts. Net real impact: one extra short-lived Deno process, one log error line, one briefly-zombied child, only on a cold concurrent start. That is a real but minor concurrency bug, not the high-impact failure described.
- **Fix:** Add a module-level `_POT_LOCK = threading.Lock()` in hd_download.py and wrap the body of ensure_pot_server in `with _POT_LOCK:` with a double-check — re-test `_pot_alive()` (or that `_POT_PROC` is alive) inside the lock before spawning, so concurrent workers serialize and the second one either sees the live server or waits on the first spawner's boot poll. While there, close the parent's copy of the log fd right after Popen (Popen dups it for the child): `log = open(...); try: _POT_PROC = subprocess.Popen(..., stdout=log, stderr=log, ...) finally: log.close()`. The detached never-terminated server and the unrotated /tmp log are intentional design (reused daemon, append-mode tiny token-server log) and need no change; optionally truncate the log when it exceeds a few MB.

### _BRIGHT_CACHE is an unbounded module-global keyed by path — grows across portal jobs and can serve stale brightness
- **Where:** `vidlore/clipstudio/match.py:30`  ·  **Lens:** concurrency-resources  ·  **Verifier confidence:** high
- **Problem:** The keyframe-brightness cache is a process-lifetime module-global dict with no eviction. In the long-lived web portal it accumulates an entry per shot keyframe per job indefinitely (slow memory growth), and because the key is the file path, any flow that regenerates a keyframe at the same path with different content (force re-index after re-download, re-running a CLI project in the same process) gets the old footage's brightness — the dark-scene penalty is then applied against stale data. It is also written from whatever thread runs match with no lock; benign for dict-set races in CPython, but the staleness and growth are real.
- **Verified because:** The code citation is exact: vidlore/clipstudio/match.py:30 declares `_BRIGHT_CACHE: dict = {}` at module scope, lines 39-40 return cached values keyed by raw path, line 51 writes with no size bound, no mtime/content check, and no lock anywhere in the module. The unbounded-growth half is real: the ClipStudio portal (vidlore/clipstudio/web.py) is a long-lived Flask process (app.run(..., threaded=True), line 256) that runs each job in-process via threading.Thread(target=_run_job) (lines 218-222), so the module global persists and only grows for the life of the server. However, two corrections shrink the severity. (1) The stale-brightness scenario — the scary half of the claim — is unreachable in every flow that exists: each portal job gets a fresh uuid project dir (web.py:203-204, `proj = _ROOT / uuid.uuid4().hex[:10]`) and keyframe paths are `<root>/index/<sid>/keyframes/shot_NNNN.jpg` (models.py:271-272, index.py:250), so paths never collide across portal jobs; the portal calls produce_auto without force_index (web.py:55-59, default False at orchestrate.py:26), and the only force-reindex flow is the CLI's --force-index (cli.py:49), which always runs in a brand-new process where _BRIGHT_CACHE starts empty. A repo-wide grep confirms no caller invokes match twice in one process against regenerated keyframes. (2) Growth is slower than claimed: _shot_brightness is only called under `if dark_scene:` (match.py:365-366), which requires single_scene video type plus the _DARK_SCENE regex match (match.py:309-310) — most jobs add zero cache entries, and a worst-case dark job (max_sources clamped to 40, web.py:199) adds low-thousands of ~150-300-byte entries, i.e. sub-MB per job. The no-lock point is benign (CPython dict get/set; worst case a duplicate computation), as the claim itself concedes. Net: a real but minor slow memory leak in the long-lived portal; the staleness and correctness-impact framing is refuted.
- **Fix:** Scope the cache to a single match run instead of the process: create the dict inside match_segments (e.g., `bright_cache: dict[str, float] = {}` next to the other per-run state at match.py:315-319) and pass it to _shot_brightness as a parameter, deleting the module-global _BRIGHT_CACHE. Within one run the cache still pays off — the same pool shot's keyframe is scored once per segment (pool × segments lookups) — while cross-job growth and any theoretical path-reuse staleness become structurally impossible. Alternative one-liner: decorate _shot_brightness with functools.lru_cache(maxsize=4096), which bounds memory and is thread-safe, though it keys by path and so would not address the (currently unreachable) staleness case; the per-run dict is the cleaner fix.

### produce_auto mutates the caller's ClipConfig in place — discover_target ratchets across runs sharing a config
- **Where:** `vidlore/clipstudio/orchestrate.py:154`  ·  **Lens:** concurrency-resources  ·  **Verifier confidence:** high
- **Problem:** produce_auto writes `cfg.discover_target = max(cfg.discover_target, max_sources)` directly onto the caller-supplied ClipConfig. Any long-lived caller that reuses one ClipConfig across runs (the cfg parameter exists precisely for that) sees discover_target ratchet monotonically upward: one run with max_sources=40 permanently inflates discovery breadth (and thus yt-dlp search volume and HD probes) for every subsequent run with that config object. The web portal and CLI escape only because they construct a fresh config per invocation.
- **Verified because:** The core defect is confirmed in the real code. orchestrate.py:154 reads exactly `cfg.discover_target = max(cfg.discover_target, max_sources)`, where `cfg` is the caller-supplied parameter (`cfg: Optional[ClipConfig] = None`, line 109) used without any copy (`cfg = cfg or load_clip_config()`, line 124). ClipConfig (config.py) is a plain non-frozen dataclass, so the assignment mutates the caller's object in place and persists across calls — a genuine violation of the function's contract, and the ratchet (max() never lowers the value) is real for any caller that reuses one config. There is no guard: load_clip_config() returns a fresh ClipConfig() per call (config.py:124-125), and produce_auto never copies the supplied cfg. However, two parts of the claim are weaker than stated. (1) Reachability: the only callers in the repo — web.py:55 and cli.py:81 — both omit cfg entirely, so every shipped invocation gets a fresh config; even the long-lived Flask server (multiple jobs per process via threads) is unaffected. The bug is latent, triggerable only by an external library consumer passing a long-lived cfg. (2) Impact: the claimed inflation of 'yt-dlp search volume and HD probes' is mostly wrong — search volume is governed by query count × discover_per_query (discover.py:316-319) and runs before discover_target is consulted; HD probes are capped by discover_resolve_limit (discover.py:278, default 28); and downloads in a subsequent run are capped by dl_limit = max_sources + discover_coverage_extra (orchestrate.py:163) using that run's own max_sources. A ratcheted discover_target mainly enlarges the final candidate list at discover.py:370, a modest effect. Real bug, but latent and with exaggerated downstream cost — severity low.
- **Fix:** Stop mutating the caller's object. Either (a) compute a local override and thread it through: in produce_auto replace line 154 with `target = max(cfg.discover_target, max_sources)` and add a keyword to discover_sources, e.g. `def discover_sources(analysis, cfg, *, segments=None, target=None, progress=None)` with `target = target if target is not None else cfg.discover_target` used at discover.py:370, then call `discover_sources(analysis, cfg, segments=segs, target=target, progress=progress)`; or (b) the one-line variant: defensively copy at entry by changing orchestrate.py:124 to `cfg = dataclasses.replace(cfg) if cfg is not None else load_clip_config()` (ClipConfig is a flat dataclass of scalars, so replace() is a safe shallow copy), leaving line 154 untouched. Option (a) is cleaner because it removes the hidden config rewrite entirely; option (b) is the minimal-diff fix.

### ffprobe_exe checks a guessed path before the explicit VIDLORE_FFPROBE override, hardcodes a personal machine path, and is .exe-blind on Windows
- **Where:** `vidlore/clipstudio/config.py:152`  ·  **Lens:** config-platform+integration  ·  **Verifier confidence:** high
- **Problem:** Three config/platform defects in one resolver. (1) Precedence: the ffmpeg-adjacent guess (line 152) is tried BEFORE the user's explicit VIDLORE_FFPROBE env override (line 153), so if any ffprobe binary happens to sit next to the resolved ffmpeg, the user's override silently loses — e.g. a user pointing VIDLORE_FFPROBE at a newer build still gets the stale adjacent one, changing probe behavior in ingest.probe()'s fallback. (2) Line 154 hardcodes '/Users/hussnain/pinokio/bin/miniconda/bin/ffprobe' — a machine-specific personal path searched ahead of homebrew on every install. (3) Every candidate is the bare name 'ffprobe' with no '.exe', so on Windows (a supported platform per windows-diagnose.bat) candidates 1-5 can never exist; the function degrades to the bare string 'ffprobe', and if ffprobe isn't on PATH, ingest.probe()'s subprocess fails and probe() returns {} — durations become 0.0, which index.detect_shots' fallback chunking turns into 0-shot sources.
- **Verified because:** The code claims are verified verbatim at /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/config.py:151-157: cands[0] is _op.join(_op.dirname(ffmpeg_exe()), "ffprobe") (a guess), cands[1] is the explicit VIDLORE_FFPROBE override, cands[2] is the hardcoded personal path "/Users/hussnain/pinokio/bin/miniconda/bin/ffprobe", and no candidate has an .exe suffix; first-exists-wins, then bare "ffprobe". The precedence inversion is genuinely reachable: vidlore/ffmpeg_tool.py:_resolve_ffmpeg prefers shutil.which("ffmpeg") over the imageio bundle, so on any machine with a system/homebrew ffmpeg, an adjacent ffprobe exists and silently shadows the user's explicit override. The hardcoded personal path is literally present (and windows-diagnose.bat shows the project is distributed to other users). HOWEVER, the claimed severity is inflated on two counts. First, ffprobe_exe() has exactly one caller — the fallback branch of ingest.probe() (ingest.py:94-103) — and the primary probe path is PyAV, which is a hard dependency of faster-whisper (requirements.txt line 8; av 15.1.0 confirmed installed in the venv), so ffprobe is only consulted when PyAV fails on a specific file; the subprocess is wrapped in try/except returning {}, so nothing crashes. The "0-shot sources" chain requires PyAV failure AND scenedetect failure AND ffprobe missing from PATH — a triple failure. Second, the Windows sub-claim that "candidates 1-5 can never exist" is wrong for candidate 2: VIDLORE_FFPROBE is user-supplied and would include .exe, so it works on Windows; and the bare "ffprobe" fallback resolves ffprobe.exe via PATH in CreateProcess, the standard mechanism. So Windows is only degraded to PATH resolution, not broken. The defects are real config/portability bugs in a fallback-of-a-fallback, not a pipeline-breaking issue.
- **Fix:** Reorder and harden the resolver in ffprobe_exe() (config.py:147-161): put the explicit override first, delete the personal machine path, make the adjacent guess platform-aware, and use shutil.which before degrading to a bare name. Concretely: exe = "ffprobe.exe" if sys.platform == "win32" else "ffprobe"; cands = [os.environ.get("VIDLORE_FFPROBE", "").strip(), _op.join(_op.dirname(ffmpeg_exe()), exe), "/opt/homebrew/bin/ffprobe", "/usr/local/bin/ffprobe"]; return the first that exists, else `shutil.which("ffprobe") or "ffprobe"`. This makes the documented env override authoritative, removes the hardcoded /Users/hussnain/... path from distributed code, and lets Windows find both an ffmpeg-adjacent ffprobe.exe and a PATH-installed one.

### discover_prefer_height is a dead config knob — the documented HD preference is hardcoded elsewhere
- **Where:** `vidlore/clipstudio/config.py:111`  ·  **Lens:** config-platform  ·  **Verifier confidence:** high
- **Problem:** ClipConfig.discover_prefer_height (env VIDLORE_CLIPSTUDIO_DISCOVER_PREFER_HEIGHT, default 720) is defined with an explanatory comment ('PREFER >= prefer_height (720) in ranking + download') and DESIGN.md:206 points at config.py as the authoritative list of tunables — but the field is never read anywhere: the actual HD preference is the hardcoded _hd_bonus tier function in discover.py (1080→0.22, 720→0.12, 480→0.05) and the hardcoded -S sort in hd_download. A user setting the env var to e.g. 1080 to bias discovery toward 1080p sees zero behavior change, with no warning — a documented config key that silently does nothing.
- **Verified because:** Confirmed against the real code. config.py:111 defines discover_prefer_height (env VIDLORE_CLIPSTUDIO_DISCOVER_PREFER_HEIGHT, default 720) under the comment at lines 107-109 promising 'PREFER >= prefer_height (720) in ranking + download'. A whole-repo grep for discover_prefer_height/PREFER_HEIGHT matches only the definition and that comment — no code reads the field. The actual ranking preference is the hardcoded _hd_bonus tier function in discover.py:349-358 (h>=1080→0.22, h>=720→0.12, h>=480→0.05, used in the sort at line 360), and the download-side preference is hd_download.py:123-125's _format_sort returning f"res:{max_h},fps,vcodec:h264,acodec:m4a,br" driven by cfg.max_height, not the prefer knob. DESIGN.md section 8 ('full list in config.py') does present config.py fields as the authoritative tunables. The sibling field discover_min_height (config.py:110, same comment block) IS consumed at discover.py:255, making the dead knob extra misleading. No guard or warning exists; setting the env var to 1080 changes nothing. Severity is honest at the low end: it is a silent no-op config/documentation defect, not a runtime failure — default behavior coincidentally matches the documented 720, so only non-default values are silently ignored.
- **Fix:** Wire the knob into _hd_bonus in discover.py: give discover_sources access to cfg inside _hd_bonus and derive the tiers from cfg.discover_prefer_height while preserving today's defaults, e.g. p = cfg.discover_prefer_height; if h >= max(p, round(p*1.5)): return 0.22; if h >= p: return 0.12; if h >= round(p*2/3): return 0.05; return 0.0 — with p=720 this reproduces the exact 1080/720/480 tiers, and setting p=1080 shifts the full bonus to 1080p+. Optionally also clamp the resolve/probe target: pass max(cfg.discover_prefer_height, ...) through resolve_quality/hd_download where appropriate, or, if the team decides the knob is not worth supporting, delete the field at config.py:111 and rewrite the comment at lines 107-109 to say the tiers are fixed (1080/720/480), so config.py stops advertising a no-op env var.

### Direct unguarded int()/float() env parses bypass the tolerant config convention and crash the pipeline mid-run
- **Where:** `vidlore/clipstudio/discover.py:130`  ·  **Lens:** config-platform  ·  **Verifier confidence:** high
- **Problem:** Three knobs are read straight from os.environ with bare numeric conversion: VIDLORE_CLIPSTUDIO_QUERY_CAP (discover.py:130), VIDLORE_CLIPSTUDIO_VISUAL_SIM (build.py:423), and VIDLORE_CLIPSTUDIO_LETTERBOX_PX (build.py:634). A malformed value ('60 queries', '0,86') raises ValueError at that point in the pipeline — for the build.py pair this is after potentially an hour of downloading/indexing/verifying, losing the whole run to a typo. Every ClipConfig knob deliberately tolerates this (config._i/_f catch TypeError/ValueError and return the default), so users have been trained that bad VIDLORE_CLIPSTUDIO_* values are harmless. These three are also invisible to anyone auditing config.py, which DESIGN.md calls the full env-flag list — undocumented env vars with a different (crashy) parsing contract.
- **Verified because:** Every factual element of the claim verifies against the real code. (1) The three lines exist verbatim: discover.py:130 `_qcap = int(_os.environ.get("VIDLORE_CLIPSTUDIO_QUERY_CAP", "60"))`; build.py:423 `_SIMT = float(_os3.environ.get("VIDLORE_CLIPSTUDIO_VISUAL_SIM", "0.86"))`; build.py:634 `_bar_h = int(os.environ.get("VIDLORE_CLIPSTUDIO_LETTERBOX_PX", "132") or 132)` — and the `or 132` only rescues empty string, since `int("100px")` still raises; the nearby try block starts at line 636, after the parse. (2) Reachable with no guards: build_queries is called unconditionally at discover.py:305; build_video is stage 9/9 in produce_auto (orchestrate.py:207), executed after discover/download/index/match/cut/verify, called bare. cli.py has no try/except around produce/produce_auto, so the ValueError is an unhandled traceback; web.py catches Exception (web.py:67) but only marks the job FATAL — the run still fails. (3) The convention contrast is real: config.py:13-24 `_f`/`_i` deliberately catch (TypeError, ValueError) and return the default, and all ~35 ClipConfig knobs use them. (4) DESIGN.md:206 literally says "full list in config.py", and grep confirms QUERY_CAP/VISUAL_SIM/LETTERBOX_PX appear nowhere in config.py or DESIGN.md — they are undocumented with a different, crashy parsing contract. One overstatement: "losing the whole run" is too strong — orchestrate saves project state after each stage and downloads/index artifacts persist on disk, so a rerun after fixing the typo largely resumes from cache. The trigger also requires the user to set a malformed value in an undocumented variable (defaults are always valid), so likelihood is low. Real bug, but low severity, not the implied higher tier.
- **Fix:** Route all three knobs through the existing tolerant config convention. Preferred: add three fields to ClipConfig in /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/config.py — `query_cap: int = field(default_factory=lambda: _i("VIDLORE_CLIPSTUDIO_QUERY_CAP", 60))`, `visual_sim: float = field(default_factory=lambda: _f("VIDLORE_CLIPSTUDIO_VISUAL_SIM", 0.86))`, `letterbox_px: int = field(default_factory=lambda: _i("VIDLORE_CLIPSTUDIO_LETTERBOX_PX", 132))` — then replace the inline parses: in build.py:423 and 634 use `getattr(cfg, "visual_sim", 0.86)` / `getattr(cfg, "letterbox_px", 132)` (cfg is already in scope in build_video), and in discover.py either pass cfg into build_queries from discover_sources (cfg is in scope at discover.py:305) or, minimally, replace the bare int() with `from .config import _i; _qcap = _i("VIDLORE_CLIPSTUDIO_QUERY_CAP", 60)`. This makes malformed values fall back to defaults exactly like every other VIDLORE_CLIPSTUDIO_* flag and makes the knobs visible in config.py, restoring DESIGN.md's "full list in config.py" guarantee.

### _CLAUDE_MODEL snapshotted from process env at import — .env's ANTHROPIC_MODEL is invisible to active_provider and eng_cfg-less calls
- **Where:** `vidlore/clipstudio/llm.py:32`  ·  **Lens:** config-platform  ·  **Verifier confidence:** high
- **Problem:** _CLAUDE_MODEL is read from os.environ at llm.py import time. The engine's ANTHROPIC_MODEL typically lives in the clone-root .env, which only enters os.environ when engine_config() → load_config() → _load_dotenv() runs (vidlore/config.py:154-159) — and that happens after llm is imported (web.py imports llm at line 25, calls engine_config() later; cli/orchestrate likewise call engine_config() inside produce). Consequences: (1) active_provider() (llm.py:209) reports the default 'claude-sonnet-4-6' in the portal's 'Brain:' header and analyze logs even when .env pins a different model, while actual calls use eng_cfg.anthropic_model (llm.py:128) — logs/UI name a model that isn't the one serving; (2) any complete() call made without eng_cfg uses the stale default rather than the .env-configured model. Same import-time snapshot applies to VIDLORE_CLIPSTUDIO_LLM_PROVIDER (line 29): changing it in a long-running portal process has no effect until restart.
- **Verified because:** The mechanism is real and the quoted code is accurate. llm.py:32 snapshots ANTHROPIC_MODEL (and line 29 snapshots VIDLORE_CLIPSTUDIO_LLM_PROVIDER) from os.environ at import time, while .env only enters os.environ when load_config() -> _load_dotenv() runs (vidlore/config.py:154-156). In the portal, vidlore/clipstudio/web.py:25 imports llm at module load and engine_config() first runs at request time (web.py:177, the 'Brain:' header), so the snapshot is taken before .env is loaded and never refreshes — active_provider (llm.py:209, claude_str = f"anthropic ({_CLAUDE_MODEL})") can name a model different from the one actually used by calls (llm.py:128: mdl = model or eng_cfg.anthropic_model or _CLAUDE_MODEL). However, the claim overstates scope on three points: (1) the CLI/orchestrate path is NOT affected — analyze.py/segment.py/verify.py import llm lazily inside functions, after engine_config() (orchestrate.py:28/125) has already loaded .env, so the snapshot there picks up the .env value; (2) 'analyze logs' don't show the model — analyze.py:278 uses active_provider(eng_cfg).split()[0], which is just the word 'anthropic'; (3) every complete() call site in the codebase (analyze.py:129,174; segment.py:165; verify.py:58) passes eng_cfg, so no real API call ever uses the stale default — that consequence is latent only. Additionally, the actual clone-root .env pins ANTHROPIC_MODEL=claude-sonnet-4-6, identical to the hardcoded default, so there is no observable mismatch today. Net: a genuine import-time-snapshot inconsistency, but currently display-only in one surface (the portal header), with no wrong model ever sent to the API.
- **Fix:** Resolve the model and provider at call time instead of import time. In vidlore/clipstudio/llm.py, replace the module-level _CLAUDE_MODEL/_PROVIDER constants with small helpers: def _claude_model(eng_cfg=None): return ((getattr(eng_cfg, "anthropic_model", "") if eng_cfg else "") or os.environ.get("ANTHROPIC_MODEL", "claude-sonnet-4-6").strip()) and def _provider(): return os.environ.get("VIDLORE_CLIPSTUDIO_LLM_PROVIDER", "anthropic").strip().lower(). Use _claude_model(eng_cfg) in _claude_complete (line 128) and in active_provider (line 209) — active_provider already receives eng_cfg, so the displayed model will then match the model actually used. Have _claude_first() call _provider() instead of reading the snapshot (line 137). Apply the same treatment to _GEMINI_MODEL/_GEMINI_LOCATION for consistency. This makes the portal header, any future eng_cfg-less complete() call, and provider switching all honor .env values loaded after llm import, with no behavior change in the CLI path.

### _apply_slow_motion silently returns the un-slowed clip on ffmpeg failure — held peak beat plays a 23%-short clip that loops
- **Where:** `vidlore/clipstudio/build.py:173`  ·  **Lens:** ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** For a peak scene's last beat, the caller cuts the source to src_need = per_beat / 1.3 (build.py:559-560) expecting the setpts=1.3*PTS re-encode to stretch it back to per_beat. _apply_slow_motion never checks the ffmpeg returncode and on any exception (or empty/missing output) returns the ORIGINAL clip_path — the un-stretched clip that is only ~77% of the intended length. Because assemble encodes beats with '-stream_loop -1' + '-frames:v', the dramatic held landing shot then loops back to frame 0 for its final ~23% — on the single most emphasized beat of the scene — with no log line. The caller also increments the 'slow-mo' counter (sm_n) regardless of success, so the build log claims N slow-mo clips even when every one of them failed.
- **Verified because:** Every code citation checks out. (1) /Users/hussnain/Desktop/vidlore-clipstudio/vidlore/clipstudio/build.py:162-175 — `_apply_slow_motion` runs `subprocess.run(cmd, capture_output=True, timeout=120)` with no `check=True` and never inspects returncode; `except Exception: return clip_path` and the final `return dest if (dest.exists() and dest.stat().st_size > 0) else clip_path` mean any failure silently yields the ORIGINAL un-stretched clip (and a non-zero-exit partial file >0 bytes would even return a corrupt dest). No log line exists on any failure path. (2) Caller build.py:559-569 — `factor = 1.30 if (is_peak and m == k - 1 and k >= 2)`, `src_need = per_beat / factor`, so the recut clip is per_beat/1.3 long and only the setpts re-encode restores full length; on fallback the short clip is appended. `sm_n += 1` executes unconditionally after the call (the function always returns a truthy Path), so the build log at build.py:582-583 reports N slow-mo even if all N fell back. (3) The looping consequence is real: build.py passes `beat_clips` to `assemble()` (build.py:649), and assemble.py encodes each beat via `_scene_video` to the PLANNED duration — `_enc(["-stream_loop", "-1", "-i", str(item.path)], vf)` (assemble.py:7212) with `-frames:v nframes` where `nframes = round(dur*FPS)` from `p["bd"] + p["pad"]` (assemble.py:7051, 7086, 9016) — so a clip shorter than the beat wraps to frame 0. Since `per_beat = max(min_clip_sec, slice_dur) + 0.5` (build.py:531) and plan_beats uses bmin=2.4, the fallback length (slice_dur+0.5)/1.3 is always < the beat whenever slice_dur > 1.67s — i.e., always. No guard elsewhere: assemble's `_clip_ready` strong gate (assemble.py:7150-7185) validates decodability, not duration. Two honest caveats on the review's framing: (a) the 0.5s headroom means the looped tail is ~10-18% of the beat, not the full 23%; (b) reachability is narrow — `_apply_slow_motion` only runs after `_recut_to_duration` just succeeded with the same ffmpeg binary on the same machine, so failure requires a >120s timeout on a few-second veryfast encode, disk-full, OOM-kill, or an ffmpeg crash on a file it just wrote. The bug is real but fires rarely, and when it fires the output is a cosmetic loop-back jump on one beat, not a crash.
- **Fix:** Make `_apply_slow_motion` report failure instead of masking it: capture `r = subprocess.run(...)` and return `dest` only if `r.returncode == 0 and dest.exists() and dest.stat().st_size > 0`, otherwise log one line (include r.stderr tail) and return None. In the caller (build.py:566-569), on None do NOT keep the short clip — re-cut the source at full length with `_recut_to_duration(src.local_path, start, per_beat, src.duration, dest, crop_filter=_cf)` (re-clamping start against src.duration for the longer cut) so the beat plays full-length un-slowed footage with no loop; increment `sm_n` only when the stretch actually succeeded so the "{sm_n} slow-mo" log line is truthful.

### Caption-dodge suppresses captions for the WHOLE scene when any single beat clip carries burned text
- **Where:** `vidlore/clipstudio/build.py:616`  ·  **Lens:** ffmpeg-media  ·  **Verifier confidence:** high
- **Problem:** The suppression window stored per scene is the scene's full word span (span_by_idx from the first to last word of the narration scene), and it is triggered if ANY ONE of the scene's k beat clips OCRs as text-bearing (the 'break' at build.py:619). assemble then drops every caption word whose midpoint falls inside that window (assemble.py:9533-9546), so a scene with one subtitled beat among four clean ones loses its narration captions for the entire 6-10s span — clean footage plays caption-less for most of the scene. The per-beat boundaries are knowable (the same plan_beats data build already iterates), so the suppression could be limited to the offending beat's time slice.
- **Verified because:** Confirmed against the real code. build.py:602-610 stores per-scene spans covering the scene's entire word range (a = min word start, b = max word end of the whole narration scene). build.py:615-621 OCRs each of the scene's beat clips and on the FIRST text-bearing hit ('break' at 618-619) appends the whole-scene span: 'suppress_wins.append(span_by_idx[seg.index])'. assemble.py:9533-9545 then drops every caption word whose midpoint falls inside any window ('cap_words = [w for w in words if not _under_card(w)]'); since the window equals the scene's full word span, all of the scene's caption words are dropped. The path is live: VIDLORE_CLIPSTUDIO_CAPTION_DODGE defaults on (build.py:592), captions default True (build.py:318), and rapidocr_onnxruntime is vendored in .clipstudio_libs and importable, so _ocr_eng is non-None. Multi-beat scenes are the norm (plan_beats target 3.4s/beat, build.py:496/530), and beat clips can come from different sources, so one subtitled beat among clean ones is realistic — that scene then plays caption-less for its full 6-10s. However, the per-scene granularity is explicitly the documented design (build.py:585-590 comment 'every text-bearing scene'), the OCR gate is conservative, there is an env kill-switch, and there is no sync/correctness impact — so this is a real but bounded quality-granularity issue, not the malfunction-level defect the framing implies. The reviewer's premise that per-beat boundaries are knowable is correct: plan_beats returns (sceneIndex, beatDuration, isLast, beatIndexInScene) (assemble.py:909-935) and build already iterates it; assemble holds frame-exact beat_durs (assemble.py:8734-8741).
- **Fix:** Make the suppression per-beat instead of per-scene. In build.py's caption-dodge pass (615-621), remove the 'break' and record the set of text-bearing beat indices m for each scene (OCR all k clips in beat_clips[seg.index]). Replay the same plan_beats call build already makes at line 496 (same durs/target/bmin/energies/roles) to get each scene's per-beat durations in order; for each scene, rescale the cumulative beat offsets to the scene's word span (a, b) from span_by_idx so drift is absorbed, and append only the flagged beats' sub-windows (a + cum[m]*scale, a + cum[m+1]*scale), padded ~0.2s on each side. Alternatively (cleaner), pass assemble a per-scene dict of flagged beat indices and have assemble build the windows itself from its frame-snapped beat_durs (assemble.py:8734-8741) intersected with the scene's word span — assemble's beat boundaries are exact, eliminating any build/assemble pacing mismatch from style-mode beat_target overrides (assemble.py:8718-8724 uses _sty.beat_target, while build hardcodes 3.4/2.4).

## Refuted (not real issues)

- Per-job pipeline code mutates process-global os.environ while concurrent jobs snapshot it (vidlore/clipstudio/build.py:335) — The cited code exists exactly as described (build.py:335 `os.environ.setdefault("VIDLORE_MUSIC_VOLUME", "1.15")`; hd_download.py:106-107 `env = dict(os.environ)` called from ThreadPool download worker
- GCP service-account secrets silently sourced from another project's directory via a hardcoded personal path (vidlore/clipstudio/llm.py:42) — The quoted code is accurate — llm.py:39-43 ends its candidate list with os.path.expanduser("~/Desktop/vidrush-clone/.secrets/gcp_vidlore.json") and _gcp_cred() never logs which candidate won. But ever
- Legacy yt-dlp option sets have drifted: ingest._ingest_url lacks the 403-workaround and quality tier added to download.py (vidlore/clipstudio/ingest.py:174) — The textual drift is real (ingest.py:174-182 lacks continuedl, socket_timeout, extractor_args player_client, and the >=720p format tier present in download.py:84-98), but every concrete harm the claim